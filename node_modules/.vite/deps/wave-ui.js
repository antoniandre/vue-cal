import {
  Fragment,
  KeepAlive,
  Teleport,
  Transition,
  TransitionGroup,
  createApp,
  createBaseVNode,
  createBlock,
  createCommentVNode,
  createElementBlock,
  createSlots,
  createTextVNode,
  createVNode,
  defineComponent,
  inject,
  mergeProps,
  normalizeClass,
  normalizeProps,
  normalizeStyle,
  openBlock,
  reactive,
  renderList,
  renderSlot,
  resolveComponent,
  resolveDirective,
  resolveDynamicComponent,
  toDisplayString,
  toHandlers,
  vModelDynamic,
  vModelText,
  vShow,
  withCtx,
  withDirectives,
  withKeys,
  withModifiers
} from "./chunk-HVUBFZF7.js";

// node_modules/.pnpm/wave-ui@3.16.2_vue@3.5.8/node_modules/wave-ui/dist/wave-ui.es.js
var _e = Object.defineProperty;
var de = (e) => {
  throw TypeError(e);
};
var $e = (e, t, i) => t in e ? _e(e, t, { enumerable: true, configurable: true, writable: true, value: i }) : e[t] = i;
var H = (e, t, i) => $e(e, typeof t != "symbol" ? t + "" : t, i);
var ue = (e, t, i) => t.has(e) || de("Cannot " + i);
var Y = (e, t, i) => (ue(e, t, "read from private field"), i ? i.call(e) : t.get(e));
var te = (e, t, i) => t.has(e) ? de("Cannot add the same private member more than once") : t instanceof WeakSet ? t.add(e) : t.set(e, i);
var ie = (e, t, i, s) => (ue(e, t, "write to private field"), s ? s.call(e, i) : t.set(e, i), i);
var Pe = reactive({
  on: "#app",
  // Sets the Wave UI root node. If not found, will default to `body`.
  breakpoints: {
    xs: 600,
    sm: 900,
    md: 1200,
    lg: 1700,
    xl: 9999
    // Xl only needs a greater value than lg but starts from lg and goes to infinity.
  },
  css: {
    // Generate shades for custom colors and status colors.
    // Note: the color palette shades are always generated separately from SCSS.
    colorShades: true,
    // Generate CSS variables for color shades.
    // Note: colorShades must be enabled for this to work.
    colorShadeCssVariables: false,
    // Generate palette colors and palette color shades.
    // Can't have this option: color palette is generated via SCSS in colors.scss.
    // colorPalette: true,
    breakpointSpaces: false,
    // Generate margin & padding classes for each breakpoint. E.g. `sm-ma2`.
    // Generate helper classes for each breakpoint.
    // E.g. `sm-text-left`, `xs-hide`.
    breakpointLayoutClasses: true,
    grid: 12
  },
  colors: {
    // Default colors of Wave UI. Can be overridden from the Wave UI user config on init.
    light: {
      primary: "#234781",
      secondary: "#d3ebff",
      info: "#3d9ff5",
      warning: "#f80",
      success: "#54b946",
      error: "#f65555"
    },
    dark: {
      primary: "#89b6d2",
      secondary: "#375b6a",
      info: "#3d9ff5",
      warning: "#f80",
      success: "#54b946",
      error: "#f65555"
    }
  },
  // The initial theme to use.
  // To switch theme while running, call the $waveui.switchTheme('light'|'dark') method.
  theme: "light",
  // One of 'light', 'dark', 'auto'.
  icons: [],
  iconsLigature: false,
  notificationManager: {
    align: "right",
    transition: "default"
    // Sliding from the side by default.
  },
  presets: {}
  // User presets for each component.
});
var Ce = (e, t = Pe) => {
  if (!Object.keys(t).length) t = Object.assign(t, e);
  else
    for (const i in e) {
      const s = e[i];
      typeof s == "object" && typeof t[i] == "object" ? Ce(e[i], t[i]) : t[i] = s;
    }
  return t;
};
var j = (e) => console.warn(`Wave UI: ${e}`);
var F = (e) => console.error(`Wave UI: ${e}`);
var Oe = (e) => {
  ["light", "dark"].forEach((t) => {
    var s, n, l;
    const i = e.colors[t];
    i.shades = {};
    for (const a in i) {
      if (a === "shades") continue;
      const r = i[a], d = { label: a, color: (l = (n = ((s = i[a]) == null ? void 0 : s.color) ?? i[a]) == null ? void 0 : n.replace) == null ? void 0 : l.call(n, "#", "") }, h = d.color;
      if (h) {
        h.length === 3 && (d.color = h[0] + "" + h[0] + h[1] + h[1] + h[2] + h[2]);
        for (let y = 1; y <= 6; y++) {
          const S = We(`#${d.color}`, y * ((r == null ? void 0 : r.lightIncrement) ?? 16) + ((r == null ? void 0 : r.lightOffset) ?? 0)), g = Ke(`#${d.color}`, y * ((r == null ? void 0 : r.darkIncrement) ?? 12.4) + ((r == null ? void 0 : r.darkOffset) ?? 0));
          i.shades[`${d.label}-light${y}`] = S, i.shades[`${d.label}-dark${y}`] = g;
        }
      }
    }
  });
};
var he = (e, t) => {
  const i = {
    ...t.reduce((s, n) => {
      s[n.label] = n.color;
      const l = (n.shades || []).reduce((a, r) => (a[r.label] = r.color, a), {});
      return { ...s, ...l };
    }, { ...e, ...e.shades })
  };
  return delete i.shades, i;
};
function G(e, t, i) {
  return Math.min(Math.max(e, t), i);
}
function J(e) {
  const t = e.toString(16);
  return t.length === 1 && `0${t}` || t.length === 2 && t || F(`expected value from 0~255, got: ${e}`) || "";
}
function ze(e) {
  return /^#[0-9a-f]{6}([0-9a-f]{2})?$/i.test(e);
}
function Me(e) {
  return /^#[0-9a-f]{3}([0-9a-f])?$/i.test(e);
}
function Ee(e) {
  return `#${e.substring(1).split("").map((t) => `${t}${t}`).join("")}`;
}
function Ae(e) {
  return ze(e) && e || Me(e) && Ee(e) || F(`expected color hex string, got '${e}'`) || "";
}
function ce(e) {
  const t = Ae(e), i = parseInt(t.substring(1, 3), 16), s = parseInt(t.substring(3, 5), 16), n = parseInt(t.substring(5, 7), 16), l = t.length === 9 ? parseInt(t.substring(7, 9), 16) / 255 : 1;
  return {
    red: i,
    green: s,
    blue: n,
    alpha: l,
    hasAlpha: t.length === 9
  };
}
function pe(e, t, i, s) {
  return `#${J(e)}${J(t)}${J(i)}${s ? J(Math.floor(s * 255)) : ""}`;
}
function Se(e, t, i = 50) {
  const s = ce(e), n = ce(t), l = G(i, 0, 100) / 100, a = 2 * l - 1, r = s.alpha - n.alpha, d = ((a * r === -1 ? a : (a + r) / (1 + a * r)) + 1) / 2, h = 1 - d, y = G(Math.round(s.red * d + n.red * h), 0, 255), S = G(Math.round(s.green * d + n.green * h), 0, 255), g = G(Math.round(s.blue * d + n.blue * h), 0, 255), V = s.alpha * l + n.alpha * (1 - l);
  return s.hasAlpha || n.hasAlpha || V !== 1 ? pe(y, S, g, V) : pe(y, S, g);
}
function We(e, t = 15) {
  return Se("#ffffff", e, t);
}
function Ke(e, t = 12.4) {
  return Se("#000000", e, t);
}
var fe = [
  {
    label: "pink",
    color: "#e91e63",
    shades: [
      { label: "pink-light6", color: "#fdebf1" },
      { label: "pink-light5", color: "#faccdc" },
      { label: "pink-light4", color: "#f7adc6" },
      { label: "pink-light3", color: "#f48eb1" },
      { label: "pink-light2", color: "#f16f9b" },
      { label: "pink-light1", color: "#ee5085" },
      { label: "pink-dark1", color: "#d31555" },
      { label: "pink-dark2", color: "#b6124a" },
      { label: "pink-dark3", color: "#990f3e" },
      { label: "pink-dark4", color: "#7c0c32" },
      { label: "pink-dark5", color: "#600927" },
      { label: "pink-dark6", color: "#43071b" }
    ]
  },
  {
    label: "purple",
    color: "#a741b9",
    shades: [
      { label: "purple-light6", color: "#f9f2fa" },
      { label: "purple-light5", color: "#eed9f2" },
      { label: "purple-light4", color: "#e3c1e9" },
      { label: "purple-light3", color: "#d8a8e1" },
      { label: "purple-light2", color: "#cd90d8" },
      { label: "purple-light1", color: "#c277cf" },
      { label: "purple-dark1", color: "#9239a2" },
      { label: "purple-dark2", color: "#7d318a" },
      { label: "purple-dark3", color: "#682873" },
      { label: "purple-dark4", color: "#53205b" },
      { label: "purple-dark5", color: "#3d1844" },
      { label: "purple-dark6", color: "#28102d" }
    ]
  },
  {
    label: "deep-purple",
    color: "#673ab7",
    shades: [
      { label: "deep-purple-light6", color: "#f1edf9" },
      { label: "deep-purple-light5", color: "#daceef" },
      { label: "deep-purple-light4", color: "#c2afe6" },
      { label: "deep-purple-light3", color: "#ab90dc" },
      { label: "deep-purple-light2", color: "#9471d2" },
      { label: "deep-purple-light1", color: "#7c52c8" },
      { label: "deep-purple-dark1", color: "#5b33a1" },
      { label: "deep-purple-dark2", color: "#4e2c8b" },
      { label: "deep-purple-dark3", color: "#422575" },
      { label: "deep-purple-dark4", color: "#351e5f" },
      { label: "deep-purple-dark5", color: "#291749" },
      { label: "deep-purple-dark6", color: "#1c1033" }
    ]
  },
  {
    label: "indigo",
    color: "#3f51b5",
    shades: [
      { label: "indigo-light6", color: "#eff1fa" },
      { label: "indigo-light5", color: "#d1d6ef" },
      { label: "indigo-light4", color: "#b3bae4" },
      { label: "indigo-light3", color: "#949fda" },
      { label: "indigo-light2", color: "#7684cf" },
      { label: "indigo-light1", color: "#5869c5" },
      { label: "indigo-dark1", color: "#37479f" },
      { label: "indigo-dark2", color: "#303e8a" },
      { label: "indigo-dark3", color: "#283474" },
      { label: "indigo-dark4", color: "#212a5f" },
      { label: "indigo-dark5", color: "#192149" },
      { label: "indigo-dark6", color: "#121734" }
    ]
  },
  {
    label: "blue",
    color: "#2196f3",
    shades: [
      { label: "blue-light6", color: "#e6f3fe" },
      { label: "blue-light5", color: "#c6e4fc" },
      { label: "blue-light4", color: "#a5d4fa" },
      { label: "blue-light3", color: "#84c5f8" },
      { label: "blue-light2", color: "#63b5f7" },
      { label: "blue-light1", color: "#42a6f5" },
      { label: "blue-dark1", color: "#0c85e5" },
      { label: "blue-dark2", color: "#0b72c4" },
      { label: "blue-dark3", color: "#095fa3" },
      { label: "blue-dark4", color: "#074c82" },
      { label: "blue-dark5", color: "#053961" },
      { label: "blue-dark6", color: "#032540" }
    ]
  },
  {
    label: "light-blue",
    color: "#03a9f4",
    shades: [
      { label: "light-blue-light6", color: "#e7f7ff" },
      { label: "light-blue-light5", color: "#c0ebfe" },
      { label: "light-blue-light4", color: "#98defe" },
      { label: "light-blue-light3", color: "#71d2fd" },
      { label: "light-blue-light2", color: "#4ac5fd" },
      { label: "light-blue-light1", color: "#22b9fc" },
      { label: "light-blue-dark1", color: "#0393d5" },
      { label: "light-blue-dark2", color: "#027eb6" },
      { label: "light-blue-dark3", color: "#026896" },
      { label: "light-blue-dark4", color: "#015277" },
      { label: "light-blue-dark5", color: "#013d58" },
      { label: "light-blue-dark6", color: "#012739" }
    ]
  },
  {
    label: "cyan",
    color: "#04cbe5",
    shades: [
      { label: "cyan-light6", color: "#e9fcff" },
      { label: "cyan-light5", color: "#baf6fe" },
      { label: "cyan-light4", color: "#8bf0fd" },
      { label: "cyan-light3", color: "#5ceafc" },
      { label: "cyan-light2", color: "#2ce3fb" },
      { label: "cyan-light1", color: "#04d8f4" },
      { label: "cyan-dark1", color: "#04b2c8" },
      { label: "cyan-dark2", color: "#0398ac" },
      { label: "cyan-dark3", color: "#037f8f" },
      { label: "cyan-dark4", color: "#026673" },
      { label: "cyan-dark5", color: "#024c56" },
      { label: "cyan-dark6", color: "#01333a" }
    ]
  },
  {
    label: "teal",
    color: "#1db3a8",
    shades: [
      { label: "teal-light6", color: "#e2faf9" },
      { label: "teal-light5", color: "#b8f3ef" },
      { label: "teal-light4", color: "#8eede6" },
      { label: "teal-light3", color: "#64e6dc" },
      { label: "teal-light2", color: "#39dfd3" },
      { label: "teal-light1", color: "#20c7bb" },
      { label: "teal-dark1", color: "#199b92" },
      { label: "teal-dark2", color: "#15847c" },
      { label: "teal-dark3", color: "#116c65" },
      { label: "teal-dark4", color: "#0e544f" },
      { label: "teal-dark5", color: "#0a3c39" },
      { label: "teal-dark6", color: "#062523" }
    ]
  },
  {
    label: "green",
    color: "#4caf50",
    shades: [
      { label: "green-light6", color: "#edf7ed" },
      { label: "green-light5", color: "#d2ebd3" },
      { label: "green-light4", color: "#b7e0b8" },
      { label: "green-light3", color: "#9bd49e" },
      { label: "green-light2", color: "#80c883" },
      { label: "green-light1", color: "#65bc69" },
      { label: "green-dark1", color: "#439b47" },
      { label: "green-dark2", color: "#3a863d" },
      { label: "green-dark3", color: "#327234" },
      { label: "green-dark4", color: "#295e2b" },
      { label: "green-dark5", color: "#204a22" },
      { label: "green-dark6", color: "#173518" }
    ]
  },
  {
    label: "light-green",
    color: "#90d73f",
    shades: [
      { label: "light-green-light6", color: "#f4fbec" },
      { label: "light-green-light5", color: "#e5f6d3" },
      { label: "light-green-light4", color: "#d7f1b9" },
      { label: "light-green-light3", color: "#c8eba0" },
      { label: "light-green-light2", color: "#b9e687" },
      { label: "light-green-light1", color: "#abe16d" },
      { label: "light-green-dark1", color: "#80cc2a" },
      { label: "light-green-dark2", color: "#70b225" },
      { label: "light-green-dark3", color: "#609820" },
      { label: "light-green-dark4", color: "#4f7d1a" },
      { label: "light-green-dark5", color: "#3f6315" },
      { label: "light-green-dark6", color: "#2e490f" }
    ]
  },
  {
    label: "lime",
    color: "#cee029",
    shades: [
      { label: "lime-light6", color: "#fafce9" },
      { label: "lime-light5", color: "#f4f8ce" },
      { label: "lime-light4", color: "#edf4b2" },
      { label: "lime-light3", color: "#e7f097" },
      { label: "lime-light2", color: "#e1ec7b" },
      { label: "lime-light1", color: "#dae85f" },
      { label: "lime-dark1", color: "#bbcc1e" },
      { label: "lime-dark2", color: "#a1b01a" },
      { label: "lime-dark3", color: "#889516" },
      { label: "lime-dark4", color: "#6f7912" },
      { label: "lime-dark5", color: "#565d0e" },
      { label: "lime-dark6", color: "#3c420a" }
    ]
  },
  {
    label: "yellow",
    color: "#ffe70f",
    shades: [
      { label: "yellow-light6", color: "#fffce0" },
      { label: "yellow-light5", color: "#fff9c4" },
      { label: "yellow-light4", color: "#fff6a8" },
      { label: "yellow-light3", color: "#fff38c" },
      { label: "yellow-light2", color: "#fff170" },
      { label: "yellow-light1", color: "#ffee54" },
      { label: "yellow-dark1", color: "#eed700" },
      { label: "yellow-dark2", color: "#cfba00" },
      { label: "yellow-dark3", color: "#af9e00" },
      { label: "yellow-dark4", color: "#908100" },
      { label: "yellow-dark5", color: "#706500" },
      { label: "yellow-dark6", color: "#504800" }
    ]
  },
  {
    label: "amber",
    color: "#ffc107",
    shades: [
      { label: "amber-light6", color: "#fffaed" },
      { label: "amber-light5", color: "#fff1c6" },
      { label: "amber-light4", color: "#ffe7a0" },
      { label: "amber-light3", color: "#ffde7a" },
      { label: "amber-light2", color: "#ffd454" },
      { label: "amber-light1", color: "#ffcb2d" },
      { label: "amber-dark1", color: "#e6ad00" },
      { label: "amber-dark2", color: "#c79500" },
      { label: "amber-dark3", color: "#a77d00" },
      { label: "amber-dark4", color: "#886600" },
      { label: "amber-dark5", color: "#684e00" },
      { label: "amber-dark6", color: "#483600" }
    ]
  },
  {
    label: "orange",
    color: "#ff9800",
    shades: [
      { label: "orange-light6", color: "#fff5e6" },
      { label: "orange-light5", color: "#ffe5bf" },
      { label: "orange-light4", color: "#ffd699" },
      { label: "orange-light3", color: "#ffc673" },
      { label: "orange-light2", color: "#ffb74d" },
      { label: "orange-light1", color: "#ffa726" },
      { label: "orange-dark1", color: "#df8500" },
      { label: "orange-dark2", color: "#c07200" },
      { label: "orange-dark3", color: "#a05f00" },
      { label: "orange-dark4", color: "#814d00" },
      { label: "orange-dark5", color: "#613a00" },
      { label: "orange-dark6", color: "#412700" }
    ]
  },
  {
    label: "deep-orange",
    color: "#ff6825",
    shades: [
      { label: "deep-orange-light6", color: "#fff0e9" },
      { label: "deep-orange-light5", color: "#ffd9c8" },
      { label: "deep-orange-light4", color: "#ffc2a8" },
      { label: "deep-orange-light3", color: "#ffac87" },
      { label: "deep-orange-light2", color: "#ff9566" },
      { label: "deep-orange-light1", color: "#ff7f46" },
      { label: "deep-orange-dark1", color: "#ff5205" },
      { label: "deep-orange-dark2", color: "#e54600" },
      { label: "deep-orange-dark3", color: "#c53d00" },
      { label: "deep-orange-dark4", color: "#a63300" },
      { label: "deep-orange-dark5", color: "#862900" },
      { label: "deep-orange-dark6", color: "#661f00" }
    ]
  },
  {
    label: "red",
    color: "#fa3317",
    shades: [
      { label: "red-light6", color: "#fee3df" },
      { label: "red-light5", color: "#fec6be" },
      { label: "red-light4", color: "#fdaa9e" },
      { label: "red-light3", color: "#fc8d7d" },
      { label: "red-light2", color: "#fc705d" },
      { label: "red-light1", color: "#fb543c" },
      { label: "red-dark1", color: "#ec2205" },
      { label: "red-dark2", color: "#cd1d04" },
      { label: "red-dark3", color: "#ae1904" },
      { label: "red-dark4", color: "#8f1403" },
      { label: "red-dark5", color: "#701002" },
      { label: "red-dark6", color: "#520c02" }
    ]
  },
  {
    label: "brown",
    color: "#845848",
    shades: [
      { label: "brown-light6", color: "#f2eae7" },
      { label: "brown-light5", color: "#e2d1ca" },
      { label: "brown-light4", color: "#d2b7ad" },
      { label: "brown-light3", color: "#c39e90" },
      { label: "brown-light2", color: "#b38473" },
      { label: "brown-light1", color: "#a16b58" },
      { label: "brown-dark1", color: "#744d3f" },
      { label: "brown-dark2", color: "#634236" },
      { label: "brown-dark3", color: "#53372d" },
      { label: "brown-dark4", color: "#422c24" },
      { label: "brown-dark5", color: "#32211b" },
      { label: "brown-dark6", color: "#211612" }
    ]
  },
  {
    label: "blue-grey",
    color: "#6c8693",
    shades: [
      { label: "blue-grey-light6", color: "#f0f3f4" },
      { label: "blue-grey-light5", color: "#dae1e4" },
      { label: "blue-grey-light4", color: "#c4cfd4" },
      { label: "blue-grey-light3", color: "#aebcc4" },
      { label: "blue-grey-light2", color: "#98aab3" },
      { label: "blue-grey-light1", color: "#8298a3" },
      { label: "blue-grey-dark1", color: "#5f7581" },
      { label: "blue-grey-dark2", color: "#51656f" },
      { label: "blue-grey-dark3", color: "#44545c" },
      { label: "blue-grey-dark4", color: "#36444a" },
      { label: "blue-grey-dark5", color: "#293338" },
      { label: "blue-grey-dark6", color: "#1c2226" }
    ]
  },
  {
    label: "grey",
    color: "#848484",
    shades: [
      { label: "grey-light6", color: "#f7f7f7" },
      { label: "grey-light5", color: "#e4e4e4" },
      { label: "grey-light4", color: "#d1d1d1" },
      { label: "grey-light3", color: "#bdbdbd" },
      { label: "grey-light2", color: "#aaaaaa" },
      { label: "grey-light1", color: "#979797" },
      { label: "grey-dark1", color: "#747474" },
      { label: "grey-dark2", color: "#646464" },
      { label: "grey-dark3", color: "#555555" },
      { label: "grey-dark4", color: "#454545" },
      { label: "grey-dark5", color: "#353535" },
      { label: "grey-dark6", color: "#252525" }
    ]
  },
  { label: "black", color: "#000" },
  { label: "white", color: "#fff" },
  { label: "transparent", color: "transparent" },
  { label: "inherit", color: "inherit" }
];
var U = {
  cssScope: ".w-app",
  baseIncrement: 4
};
var se = { keys: [], values: [] };
var me = null;
var Ne = (e, t) => {
  var V;
  let i = "";
  const s = {}, { info: n, warning: l, success: a, error: r, shades: d, ...h } = e, { cssScope: y } = U;
  for (const I in h)
    i += `${y} .${I}--bg{background-color:var(--w-${I}-color)}${y} .${I}{color:var(--w-${I}-color)}`;
  for (const I in d)
    i += `${y} .${I}--bg{background-color:${d[I]}}${y} .${I}{color:${d[I]}}`;
  const S = { ...h, info: n, warning: l, success: a, error: r };
  for (const I in S) s[I] = ((V = S[I]) == null ? void 0 : V.color) ?? S[I];
  if (t)
    for (const I in d) s[I] = d[I];
  let g = "";
  return Object.entries(s).forEach(([I, O]) => {
    g += `--w-${I}-color: ${O};`;
  }), `:root{${g}}${i}`;
};
var De = (e, t) => {
  let i = "";
  const { cssScope: s } = U;
  return e.forEach(({ min: n, label: l }) => {
    if (l === "xs")
      for (let a = 0; a < t; a++)
        i += `${s} .${l}${t - a}{width:${parseFloat(((t - a) * 100 / t).toFixed(4))}%;}`;
    else {
      i += `@media(min-width:${n}px){`;
      for (let a = 0; a < t; a++)
        i += `${s} .${l}${t - a}{width:${parseFloat(((t - a) * 100 / t).toFixed(4))}%;}`;
      i += "}";
    }
  }), i;
};
var He = (e) => {
  let t = "";
  const { cssScope: i, baseIncrement: s } = U, n = [
    "show{display:block}",
    "hide{display:none}",
    "d-flex{display:flex}",
    "d-iflex{display:inline-flex}",
    "d-block{display:block}",
    "d-iblock{display:inline-block}",
    "text-left{text-align:left}",
    "text-center{text-align:center}",
    "text-right{text-align:right}",
    "text-nowrap{white-space:nowrap}",
    "row{flex-direction:row}",
    "column{flex-direction:column}",
    "column-reverse{flex-direction:column-reverse}",
    "grow{flex-grow:1;flex-basis:auto}",
    "no-grow{flex-grow:0}",
    "shrink{flex-shrink:1;margin-left:auto;margin-right:auto}",
    "no-shrink{flex-shrink:0}",
    "wrap{flex-wrap: wrap}",
    "no-wrap{flex-wrap: nowrap}",
    "fill-width{width:100%}",
    "fill-height{height:100%}",
    "basis-zero{flex-basis:0}",
    "align-start{align-items:flex-start}",
    "align-center{align-items:center}",
    "align-end{align-items:flex-end}",
    "align-self-start{align-self:flex-start}",
    "align-self-center{align-self:center}",
    "align-self-end{align-self:flex-end}",
    "align-self-stretch{align-self:stretch}",
    "justify-start{justify-content:flex-start}",
    "justify-center{justify-content:center}",
    "justify-end{justify-content:flex-end}",
    "justify-space-between{justify-content:space-between}",
    "justify-space-around{justify-content:space-around}",
    "justify-space-evenly{justify-content:space-evenly}"
  ], l = Array(12).fill();
  return e.forEach(({ label: a, min: r }) => {
    a !== "xs" && (t += `@media(min-width:${r}px){` + n.map((d) => `${i} .${a}u-${d}`).join("") + // w-grid columns and gap.
    l.map((d, h) => `.w-grid.${a}u-columns${h + 1}{grid-template-columns:repeat(${h + 1},1fr);}`).join("") + l.map((d, h) => `.w-flex.${a}u-gap${h + 1},.w-grid.${a}u-gap${h + 1}{gap:${(h + 1) * s}px;}`).join("") + `.w-flex.${a}u-gap0,.w-flex.${a}u-gap0{gap:0}}`);
  }), e.forEach(({ label: a, min: r, max: d }) => {
    t += `@media (min-width:${r}px) and (max-width:${d}px){` + n.map((h) => `${i} .${a}-${h}`).join("") + // w-grid columns and gap.
    l.map((h, y) => `.w-grid.${a}-columns${y + 1}{grid-template-columns:repeat(${y + 1},1fr);}`).join("") + l.map((h, y) => `.w-flex.${a}-gap${y + 1},.w-grid.${a}-gap${y + 1}{gap:${(y + 1) * s}px;}`).join("") + `.w-flex.${a}-gap0,.w-flex.${a}-gap0{gap:0}}`;
  }), e.forEach(({ label: a, max: r }) => {
    a !== "xl" && (t += `@media (max-width:${r}px){` + n.map((d) => `${i} .${a}d-${d}`).join("") + // w-grid columns and gap.
    l.map((d, h) => `.w-grid.${a}d-columns${h + 1}{grid-template-columns:repeat(${h + 1},1fr);}`).join("") + l.map((d, h) => `.w-flex.${a}d-gap${h + 1},.w-grid.${a}d-gap${h + 1}{gap:${(h + 1) * s}px;}`).join("") + `.w-flex.${a}d-gap0,.w-flex.${a}d-gap0{gap:0}}`);
  }), t;
};
var ge = (e) => {
  const t = window.innerWidth, i = se.values.slice(0);
  i.push(t), i.sort((n, l) => n - l);
  const s = se.keys[i.indexOf(t)] || "xl";
  s !== me && (me = s, e.breakpoint = {
    name: s,
    xs: s === "xs",
    sm: s === "sm",
    md: s === "md",
    lg: s === "lg",
    xl: s === "xl",
    width: t
  }), e.breakpoint.width = window.innerWidth;
};
var Fe = (e) => {
  const { config: t } = e;
  if (se = { keys: Object.keys(t.breakpoints), values: Object.values(t.breakpoints) }, !document.getElementById("wave-ui-styles")) {
    const i = document.createElement("style");
    i.id = "wave-ui-styles", i.innerHTML = Ue(t);
    const s = document.head.querySelectorAll('style,link[rel="stylesheet"]')[0];
    s ? s.before(i) : document.head.appendChild(i);
  }
  ge(e), window.addEventListener("resize", () => ge(e));
};
var je = (e, t, i) => {
  if (!document.getElementById("wave-ui-colors")) {
    const s = document.createElement("style");
    s.id = "wave-ui-colors", s.innerHTML = Ne(e, t);
    const n = document.head.querySelectorAll('style,link[rel="stylesheet"]')[0];
    n ? n.before(s) : document.head.appendChild(s);
  }
};
var Ue = (e) => {
  const t = Object.entries(e.breakpoints), i = t.map(([l, a], r) => {
    const [, d = 0] = t[r - 1] || [];
    return { label: l, min: d ? d + 1 : 0, max: a };
  }), s = getComputedStyle(document.documentElement);
  U.cssScope = s.getPropertyValue("--w-css-scope"), U.baseIncrement = parseInt(s.getPropertyValue("--w-base-increment"));
  let n = "";
  return n += De(i, e.css.grid), e.css.breakpointLayoutClasses && (n += He(i)), n;
};
var qe = ["innerHTML"];
function Xe(e, t, i, s, n, l) {
  const a = resolveComponent("w-alert");
  return openBlock(), createBlock(Teleport, { to: ".w-app" }, [
    createVNode(TransitionGroup, {
      class: normalizeClass(["w-notification-manager", { "w-notification-manager--left": l.conf.align === "left" }]),
      tag: "div",
      name: l.transition,
      appear: ""
    }, {
      default: withCtx(() => [
        (openBlock(true), createElementBlock(Fragment, null, renderList(l.notifications, (r) => (openBlock(), createElementBlock(Fragment, null, [
          r._value ? (openBlock(), createBlock(a, mergeProps({
            class: "white--bg",
            key: r._uid,
            modelValue: r._value,
            "onUpdate:modelValue": (d) => r._value = d,
            onClose: r.dismiss,
            ref_for: true
          }, l.notifProps(r)), {
            default: withCtx(() => [
              createBaseVNode("div", {
                innerHTML: r.message
              }, null, 8, qe)
            ]),
            _: 2
          }, 1040, ["modelValue", "onUpdate:modelValue", "onClose"])) : createCommentVNode("", true)
        ], 64))), 256))
      ]),
      _: 1
    }, 8, ["class", "name"])
  ]);
}
var v = (e, t) => {
  const i = e.__vccOpts || e;
  for (const [s, n] of t)
    i[s] = n;
  return i;
};
var Ye = {
  name: "w-notification-manager",
  computed: {
    conf() {
      return this.$waveui.config.notificationManager;
    },
    notifications() {
      var e;
      return (e = this.$waveui._notificationManager) == null ? void 0 : e.notifications;
    },
    // Possible transitions: slide-fade-down, slide-fade-left, slide-fade-right,
    // slide-left, slide-right, bounce, twist, fade, scale, scale-fade.
    transition() {
      return this.conf.transition ? this.conf.transition.replace("default", `slide-${this.conf.align === "left" ? "right" : "left"}`) : "";
    }
  },
  methods: {
    notifProps(e) {
      const { _value: t, _uid: i, message: s, timeout: n, ...l } = e;
      return l.dismiss && (l.dismiss = true), l;
    }
  }
};
var Ge = v(Ye, [["render", Xe]]);
var D;
var K = class K2 {
  constructor() {
    H(this, "notifications");
    H(this, "_uid");
    H(this, "_notificationDefaults");
    if (Y(K2, D)) return Y(K2, D);
    this.notifications = [], this._uid = 0, this._notificationDefaults = {
      _uid: 0,
      _value: true,
      message: "",
      timeout: 4e3,
      dismiss: true
    }, ie(K2, D, this);
  }
  notify(...t) {
    let i = {
      ...this._notificationDefaults,
      _uid: this._uid++
    };
    if (typeof t[0] == "object") i = { ...i, ...t[0] };
    else {
      const [s, n, l] = t;
      i = {
        ...i,
        message: s || "",
        [n === void 0 ? "info" : n]: true,
        timeout: l || l === 0 ? parseFloat(l) : 4e3
      };
    }
    i.dismiss && (i.dismiss = () => this.dismiss(i._uid)), this.notifications.push(i), ~~i.timeout && setTimeout(() => this.dismiss(i._uid), i.timeout);
  }
  dismiss(t) {
    this.notifications = this.notifications.filter((i) => i._uid !== t);
  }
};
D = /* @__PURE__ */ new WeakMap(), te(K, D);
var oe = K;
var Je = (e, t, i) => {
  const s = document.createElement("div");
  e.appendChild(s);
  const n = createApp(defineComponent({
    ...Ge,
    inject: ["$waveui"]
  })).provide("$waveui", i);
  for (const l in t) {
    const a = t[l];
    n.component(a.name, { ...a, inject: ["$waveui"] });
  }
  n.mount(s), s.remove();
};
var be = false;
var Qe = (e) => {
  const t = window.matchMedia("(prefers-color-scheme: dark)");
  e.preferredTheme = t.matches ? "dark" : "light", e.switchTheme(e.preferredTheme), t.addEventListener("change", (i) => {
    e.preferredTheme = i.matches ? "dark" : "light", e.switchTheme(e.preferredTheme);
  });
};
var Ze = (e, t) => {
  var i, s;
  for (const n in t)
    if ((i = e.props) != null && i[n])
      e.props[n].default = t[n];
    else {
      let l = false;
      if (Array.isArray(e.mixins) && e.mixins.length) {
        for (const a of e.mixins)
          if ((s = a == null ? void 0 : a.props) != null && s[n]) {
            a.props[n].default = t[n], l = true;
            break;
          }
        l || j(`Attempting to set a preset on a prop that doesn't exist: \`${e.name}.${n}\`.`);
        continue;
      }
    }
};
var X;
var N = class N2 {
  constructor(t, i = {}) {
    H(this, "$waveui", {
      breakpoint: {
        name: "",
        xs: false,
        sm: false,
        md: false,
        lg: false,
        xl: false,
        width: null
      },
      config: {},
      colors: {},
      // Object of pairs of color-name => color hex.
      preferredTheme: null,
      // The user OS preferred theme (light or dark).
      theme: null,
      // The current theme (light or dark).
      _notificationManager: null,
      // Callable from this.$waveui.
      notify(...t2) {
        this._notificationManager.notify(...t2);
      },
      // Callable from this.$waveui.
      switchTheme(t2, i2 = false) {
        var n2, l2;
        this.theme = t2, document.documentElement.setAttribute("data-theme", t2), (l2 = (n2 = document.head.querySelector("#wave-ui-colors")) == null ? void 0 : n2.remove) == null || l2.call(n2);
        const s2 = this.config.colors[this.theme];
        je(s2, this.config.css.colorShadeCssVariables), this.colors = he(s2, fe);
      }
    });
    if (Y(N2, X)) return;
    if (this.$waveui._notificationManager = new oe(), i.theme || (i.theme = "light"), i.colors) {
      const a = { ...i.colors };
      i.colors.light || (i.colors.light = a), i.colors.dark || (i.colors.dark = a), i.colors = { light: i.colors.light, dark: i.colors.dark };
    }
    let { components: s, ...n } = i;
    n = this.$waveui.config = Ce(n), n.css.colorShades && Oe(n);
    const l = reactive(this.$waveui);
    t.config.globalProperties.$waveui = l, t.provide("$waveui", l), n.theme !== "auto" && (this.$waveui.colors = he(n.colors[n.theme], fe));
  }
  static install(t, i = {}) {
    var n;
    t.directive("focus", {
      // Wait for the next tick to focus the newly mounted element.
      mounted: (l) => setTimeout(() => l.focus(), 0)
    }), t.directive("scroll", {
      mounted: (l, a) => {
        const r = (d) => {
          a.value(d, l) && window.removeEventListener("scroll", r);
        };
        window.addEventListener("scroll", r);
      }
    });
    const { components: s = {} } = i || {};
    for (const l in s) {
      const a = s[l];
      (n = i.presets) != null && n[a.name] && Ze(a, i.presets[a.name]), t.component(a.name, a);
    }
    t.mixin({
      // Add a mixin to capture the first mounted hook, trigger the Wave UI init then unregister the mixin straight away.
      beforeMount() {
        if (!be) {
          be = true;
          const l = inject("$waveui"), { config: a } = l, r = document.querySelector(a.on) || document.body;
          r.classList.add("w-app"), a.theme === "auto" ? Qe(l) : l.switchTheme(a.theme, true), Fe(l), Je(r, s, l), t._context.mixins.find((d) => d.mounted && delete d.mounted);
        }
      }
    }), new N2(t, i), ie(N2, X, true);
  }
};
X = /* @__PURE__ */ new WeakMap(), te(N, X, false);
var q = N;
var et = ["aria-expanded"];
var tt = ["onClick", "onFocus", "onKeypress", "tabindex"];
var it = ["innerHTML"];
var lt = ["innerHTML"];
function st(e, t, i, s, n, l) {
  const a = resolveComponent("w-button"), r = resolveComponent("w-transition-expand");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-accordion", l.accordionClasses])
  }, [
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.accordionItems, (d, h) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["w-accordion__item", l.itemClasses(d)]),
      key: h,
      "aria-expanded": d._expanded ? "true" : "false"
    }, [
      createBaseVNode("div", {
        class: normalizeClass(["w-accordion__item-title", i.titleClass]),
        onClick: (y) => !d._disabled && l.toggleItem(d, y),
        onFocus: (y) => e.$emit("focus", l.getOriginalItem(d)),
        onKeypress: withKeys((y) => !d._disabled && l.toggleItem(d, y), ["enter"]),
        tabindex: !d._disabled && 0
      }, [
        i.expandIcon && !i.expandIconRight ? (openBlock(), createBlock(a, {
          key: 0,
          class: normalizeClass(["w-accordion__expand-icon", { "w-accordion__expand-icon--expanded": d._expanded, "w-accordion__expand-icon--rotate90": i.expandIconRotate90 }]),
          icon: d._expanded && i.collapseIcon || i.expandIcon,
          "icon-props": i.expandIconProps,
          disabled: d._disabled || null,
          tabindex: -1,
          text: "",
          onKeypress: t[0] || (t[0] = withModifiers(() => {
          }, ["stop"])),
          onClick: withModifiers((y) => !d._disabled && l.toggleItem(d, y), ["stop"])
        }, null, 8, ["icon", "icon-props", "disabled", "onClick", "class"])) : createCommentVNode("", true),
        e.$slots[`item-title.${d.id || h + 1}`] ? renderSlot(e.$slots, `item-title.${d.id || h + 1}`, {
          key: 1,
          item: l.getOriginalItem(d),
          expanded: d._expanded,
          index: h + 1
        }) : renderSlot(e.$slots, "item-title", {
          key: 2,
          item: l.getOriginalItem(d),
          expanded: d._expanded,
          index: h + 1
        }, () => [
          createBaseVNode("div", {
            class: "grow",
            innerHTML: d[i.itemTitleKey]
          }, null, 8, it)
        ]),
        i.expandIcon && i.expandIconRight ? (openBlock(), createBlock(a, {
          key: 3,
          class: normalizeClass(["w-accordion__expand-icon", { "w-accordion__expand-icon--expanded": d._expanded, "w-accordion__expand-icon--rotate90": i.expandIconRotate90 }]),
          icon: d._expanded && i.collapseIcon || i.expandIcon,
          text: "",
          onKeypress: t[1] || (t[1] = withModifiers(() => {
          }, ["stop"])),
          onClick: withModifiers((y) => !d._disabled && l.toggleItem(d, y), ["stop"])
        }, null, 8, ["icon", "onClick", "class"])) : createCommentVNode("", true)
      ], 42, tt),
      createVNode(r, {
        y: "",
        onAfterLeave: (y) => l.onEndOfCollapse(d),
        duration: i.duration
      }, {
        default: withCtx(() => [
          d._expanded ? (openBlock(), createElementBlock("div", {
            key: 0,
            class: normalizeClass(["w-accordion__item-content", i.contentClass])
          }, [
            e.$slots[`item-content.${d.id || h + 1}`] ? renderSlot(e.$slots, `item-content.${d.id || h + 1}`, {
              key: 0,
              item: l.getOriginalItem(d),
              expanded: d._expanded,
              index: h + 1
            }) : renderSlot(e.$slots, "item-content", {
              key: 1,
              item: l.getOriginalItem(d),
              expanded: d._expanded,
              index: h + 1
            }, () => [
              createBaseVNode("div", {
                innerHTML: d[i.itemContentKey]
              }, null, 8, lt)
            ])
          ], 2)) : createCommentVNode("", true)
        ]),
        _: 2
      }, 1032, ["onAfterLeave", "duration"])
    ], 10, et))), 128))
  ], 2);
}
var ot = {
  name: "w-accordion",
  props: {
    modelValue: { type: Array },
    color: { type: String },
    bgColor: { type: String },
    items: { type: [Array, Number], required: true },
    itemColorKey: { type: String, default: "color" },
    // Support a different color per item.
    itemTitleKey: { type: String, default: "title" },
    itemContentKey: { type: String, default: "content" },
    itemClass: { type: String },
    titleClass: { type: String },
    contentClass: { type: String },
    expandIcon: { type: [String, Boolean], default: "wi-triangle-down" },
    expandIconRight: { type: Boolean },
    expandIconRotate90: { type: Boolean },
    expandIconProps: { type: Object, default: () => ({}) },
    expandSingle: { type: Boolean },
    collapseIcon: { type: String },
    shadow: { type: Boolean },
    duration: { type: Number, default: 250 },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: ["input", "update:modelValue", "focus", "item-expand", "item-collapsed"],
  data: () => ({
    accordionItems: []
  }),
  computed: {
    accordionClasses() {
      return {
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-accordion--dark": this.dark,
        "w-accordion--light": this.light,
        "w-accordion--shadow": this.shadow,
        "w-accordion--no-icon": !this.expandIcon && !this.collapseIcon,
        "w-accordion--icon-right": this.expandIcon && this.expandIconRight,
        "w-accordion--rotate-icon": this.expandIcon && !this.collapseIcon
      };
    }
  },
  methods: {
    toggleItem(e, t) {
      e._expanded = !e._expanded, this.expandSingle && this.accordionItems.forEach((s) => s._index !== e._index && (s._expanded = false));
      const i = this.accordionItems.map((s) => s._expanded || false);
      this.$emit("update:modelValue", i), this.$emit("input", i), this.$emit("item-expand", { item: e, expanded: e._expanded }), t.target.blur(), setTimeout(() => t.target.focus(), 300);
    },
    onEndOfCollapse(e) {
      this.$emit("item-collapsed", { item: e, expanded: e._expanded });
    },
    // Return the original accordion item (so there is no `_index`, etc.).
    getOriginalItem(e) {
      return this.items[e._index];
    },
    itemClasses(e) {
      return {
        [this.itemClass]: this.itemClass || null,
        "w-accordion__item--expanded": e._expanded,
        "w-accordion__item--disabled": e._disabled,
        [e[this.itemColorKey]]: e[this.itemColorKey]
      };
    },
    updateItems() {
      const e = typeof this.items == "number" ? Array(this.items).fill({}) : this.items || [];
      this.accordionItems = e.map((t, i) => ({
        ...t,
        _index: i,
        _expanded: this.modelValue && this.modelValue[i],
        _disabled: !!t.disabled
      }));
    }
  },
  created() {
    this.updateItems();
  },
  watch: {
    modelValue() {
      this.updateItems();
    },
    items: {
      handler() {
        this.updateItems();
      },
      deep: true
    }
  }
};
var at = v(ot, [["render", st]]);
var nt = { class: "w-alert__content" };
function rt(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon"), r = resolveComponent("w-button");
  return n.show ? (openBlock(), createElementBlock("div", {
    key: 0,
    class: normalizeClass(["w-alert", l.classes])
  }, [
    l.type || i.icon || i.dismiss ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
      l.type || i.icon ? (openBlock(), createBlock(a, {
        key: 0,
        class: "w-alert__icon"
      }, {
        default: withCtx(() => [
          createTextVNode(toDisplayString(l.type ? l.typeIcon : i.icon), 1)
        ]),
        _: 1
      })) : createCommentVNode("", true),
      createBaseVNode("div", nt, [
        renderSlot(e.$slots, "default")
      ]),
      i.dismiss ? (openBlock(), createBlock(r, {
        key: 1,
        class: "w-alert__dismiss",
        onClick: t[0] || (t[0] = (d) => {
          e.$emit("update:modelValue", n.show = false), e.$emit("input", false), e.$emit("close", false);
        }),
        icon: "wi-cross",
        color: "inherit",
        sm: "",
        text: ""
      })) : createCommentVNode("", true)
    ], 64)) : renderSlot(e.$slots, "default", { key: 1 })
  ], 2)) : createCommentVNode("", true);
}
var dt = {
  name: "w-alert",
  props: {
    modelValue: { default: true },
    // Show or hide.
    color: { type: String },
    bgColor: { type: String },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    round: { type: Boolean },
    icon: { type: String },
    iconOutside: { type: Boolean },
    plain: { type: Boolean },
    dismiss: { type: Boolean },
    bold: { type: Boolean },
    // Types (with icon).
    success: { type: Boolean },
    info: { type: Boolean },
    warning: { type: Boolean },
    error: { type: Boolean },
    // Sizes.
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    // Borders.
    border: { type: Boolean },
    borderLeft: { type: Boolean },
    borderRight: { type: Boolean },
    borderTop: { type: Boolean },
    borderBottom: { type: Boolean },
    outline: { type: Boolean }
  },
  emits: ["input", "update:modelValue", "close"],
  data() {
    return {
      show: this.modelValue
    };
  },
  computed: {
    typeIcon() {
      return this.type === "success" && "wi-check-circle" || this.type === "warning" && "wi-warning-circle" || this.type === "error" && "wi-cross-circle" || this.type === "info" && "wi-info-circle";
    },
    type() {
      return this.success && "success" || this.info && "info" || this.warning && "warning" || this.error && "error" || null;
    },
    presetSize() {
      return this.xs && "xs" || this.sm && "sm" || this.md && "md" || this.lg && "lg" || this.xl && "xl" || null;
    },
    hasSingleBorder() {
      return this.borderLeft || this.borderRight || this.borderTop || this.borderBottom;
    },
    classes() {
      const e = this.bgColor || this.plain && this.type, t = this.color || !this.plain && this.type || !e && "primary";
      return {
        [`${e}--bg w-alert--bg`]: e,
        [t]: t,
        [`size--${this.presetSize}`]: this.presetSize,
        [`w-alert--${this.type}`]: this.type,
        "w-alert--has-icon": this.type || this.icon || this.dismiss,
        "w-alert--icon-outside": this.iconOutside,
        "w-alert--plain": this.type && this.plain,
        "w-alert--outline": this.outline,
        "w-alert--tile": this.tile,
        "w-alert--round": this.round,
        "w-alert--one-border": this.hasSingleBorder || this.iconOutside,
        "w-alert--border-left": this.borderLeft || this.iconOutside,
        "w-alert--border-right": this.borderRight,
        "w-alert--border-top": this.borderTop,
        "w-alert--border-bottom": this.borderBottom,
        "w-alert--border": this.border && !this.hasSingleBorder,
        "w-alert--shadow": this.shadow,
        "w-alert--bold": this.bold
      };
    }
  },
  watch: {
    modelValue(e) {
      this.show = e;
    }
  }
};
var ut = v(dt, [["render", rt]]);
var ht = { class: "w-autocomplete__selection" };
var ct = ["innerHTML"];
var pt = ["innerHTML"];
var ft = ["value"];
var mt = ["onClick"];
var gt = ["innerHTML"];
var bt = ["innerHTML"];
function yt(e, t, i, s, n, l) {
  const a = resolveComponent("w-button"), r = resolveComponent("w-transition-slide-fade");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-autocomplete", l.classes]),
    onClick: t[5] || (t[5] = (...d) => l.onClick && l.onClick(...d)),
    style: normalizeStyle(e.$attrs.style)
  }, [
    e.selection.length ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(e.selection, (d, h) => (openBlock(), createElementBlock("div", ht, [
      renderSlot(e.$slots, "selection", {
        item: d,
        unselect: (y) => l.unselectItem(y)
      }, () => [
        createBaseVNode("span", {
          innerHTML: d[i.itemLabelKey]
        }, null, 8, ct),
        createVNode(a, {
          onClick: withModifiers((y) => l.unselectItem(h), ["stop"]),
          icon: "wi-cross",
          xs: "",
          text: "",
          color: "currentColor"
        }, null, 8, ["onClick"])
      ])
    ]))), 256)) : createCommentVNode("", true),
    !e.selection.length && !e.keywords && i.placeholder ? (openBlock(), createElementBlock("div", {
      key: 1,
      class: "w-autocomplete__placeholder",
      innerHTML: i.placeholder
    }, null, 8, pt)) : createCommentVNode("", true),
    createBaseVNode("input", mergeProps({
      class: "w-autocomplete__input",
      ref: "input",
      value: e.keywords
    }, toHandlers(l.inputEventListeners, true), l.inputAttrs), null, 16, ft),
    createVNode(r, null, {
      default: withCtx(() => [
        e.menuOpen ? (openBlock(), createElementBlock("ul", {
          key: 0,
          class: "w-autocomplete__menu",
          ref: "menu",
          onMousedown: t[1] || (t[1] = (d) => e.menuIsBeingClicked = true),
          onMouseup: t[2] || (t[2] = (...d) => l.setEndOfMenuClick && l.setEndOfMenuClick(...d)),
          onTouchstart: t[3] || (t[3] = (d) => e.menuIsBeingClicked = true),
          onTouchend: t[4] || (t[4] = (...d) => l.setEndOfMenuClick && l.setEndOfMenuClick(...d))
        }, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(l.filteredItems, (d, h) => (openBlock(), createElementBlock("li", {
            key: h,
            onClick: withModifiers((y) => (l.selectItem(d), e.$emit("item-click", d)), ["stop"]),
            class: normalizeClass({ highlighted: e.highlightedItem === d.uid })
          }, [
            renderSlot(e.$slots, "item", {
              item: d,
              highlighted: e.highlightedItem === d.uid
            }, () => [
              createBaseVNode("span", {
                innerHTML: d[i.itemLabelKey]
              }, null, 8, gt)
            ])
          ], 10, mt))), 128)),
          l.filteredItems.length ? createCommentVNode("", true) : (openBlock(), createElementBlock("li", {
            key: 0,
            class: normalizeClass(["w-autocomplete__no-match", { "w-autocomplete__no-match--default": !e.$slots.noMatch }])
          }, [
            renderSlot(e.$slots, "no-match", {}, () => [
              createBaseVNode("div", {
                class: "caption",
                innerHTML: i.noMatch ?? "No match."
              }, null, 8, bt)
            ])
          ], 2)),
          e.$slots["extra-item"] ? (openBlock(), createElementBlock("li", {
            key: 1,
            class: normalizeClass(["w-autocomplete__extra-item", { highlighted: e.highlightedItem === "extra-item" }]),
            onClick: t[0] || (t[0] = (...d) => l.selectExtraItem && l.selectExtraItem(...d))
          }, [
            renderSlot(e.$slots, "extra-item")
          ], 2)) : createCommentVNode("", true)
        ], 544)) : createCommentVNode("", true)
      ]),
      _: 3
    })
  ], 6);
}
var wt = {
  name: "w-autocomplete",
  inheritAttrs: false,
  // The attrs should only be added to the input not the wrapper.
  props: {
    items: { type: Array, required: true },
    modelValue: { type: [String, Number, Array] },
    // String or Number if single selections, Array if multiple.
    placeholder: { type: String },
    openOnKeydown: { type: Boolean },
    // By default the menu is always open for selection.
    multiple: { type: Boolean },
    // When multiple is on, prevents duplicate items selections by default, unless this is set to true.
    allowDuplicates: { type: Boolean },
    noMatch: { type: String },
    // Contains the unique selection value for each item.
    // Can be a numeric ID, a slug, etc. (outside of Wave UI)
    itemValueKey: { type: String, default: "value" },
    // Contains the string to display for each item.
    itemLabelKey: { type: String, default: "label" },
    // Contains the string to search keywords into for each item.
    // This can for instance be an aggregation of multiple fields (outside of Wave UI).
    itemSearchableKey: { type: String, default: "searchable" }
  },
  // item-select is also from keyboard, 'item-click' may be useful for mouseenter mouseleave events.
  emits: ["update:modelValue", "input", "focus", "blur", "keydown", "item-click", "item-select", "extra-item-select"],
  data: () => ({
    keywords: "",
    selection: [],
    menuOpen: false,
    highlightedItem: null,
    // The focus-blur events occur more times than it should emit to the outside due to the menu
    // item clicking. So keep the focus on as long as the user is interacting with the autocomplete.
    menuIsBeingClicked: false
  }),
  computed: {
    // Keep the autocomplete matching as fast as possible by caching optimized search strings.
    normalizedKeywords() {
      return this.normalize(this.keywords);
    },
    // Keep the autocomplete matching as fast as possible by caching optimized search strings.
    optimizedItemsForSearch() {
      return this.items.map((e, t) => ({
        ...e,
        uid: t,
        searchable: this.normalize(e[this.itemSearchableKey] || "")
      }));
    },
    filteredItems() {
      let e = this.optimizedItemsForSearch;
      const t = (i) => !this.selection.find((s) => s.uid === i.uid);
      return this.keywords ? e = e.filter((i) => i.searchable.includes(this.normalizedKeywords) ? this.multiple && !this.allowDuplicates ? t(i) : true : false) : this.multiple && !this.allowDuplicates && (e = e.filter(t)), e;
    },
    highlightedItemIndex() {
      return this.highlightedItem === null ? -1 : this.highlightedItem === "extra-item" ? this.filteredItems.length : this.filteredItems.findIndex((e) => e.uid === this.highlightedItem);
    },
    wrapperAttrs() {
      const { style: e, class: t } = this.$attrs;
      return { style: e, class: t };
    },
    inputAttrs() {
      const { style: e, class: t, ...i } = this.$attrs;
      return i;
    },
    inputEventListeners() {
      return {
        input: (e) => {
          this.keywords = e.target.value;
        },
        focus: (e) => {
          this.menuIsBeingClicked || (this.onFocus(e), this.$emit("focus", e));
        },
        blur: (e) => {
          this.menuIsBeingClicked || this.$emit("blur", e);
        },
        keydown: (e) => {
          this.onKeydown(e), this.$emit("keydown", e);
        },
        drop: this.onDrop,
        compositionstart: this.onCompositionStart,
        compositionupdate: this.onCompositionUpdate
      };
    },
    classes() {
      return {
        "w-autocomplete--open": this.menuOpen,
        "w-autocomplete--filled": this.selection.length,
        "w-autocomplete--has-keywords": this.keywords,
        "w-autocomplete--empty": !this.selection.length && !this.keywords,
        // With the inheritAttrs set to false any class on the component would be lost, so add it back.
        [this.$attrs.class]: !!this.$attrs.class
      };
    }
  },
  methods: {
    // Replace all the accents and non-latin characters with equivalent letters. E.g. é -> e.
    normalize(e) {
      return e.toLowerCase().normalize("NFKD").replace(new RegExp("\\p{Diacritic}", "gu"), "").replace(/œ/g, "oe");
    },
    // Selection can be made from click or enter key.
    selectItem(e) {
      this.multiple || (this.selection = []), this.selection.push(e), this.highlightedItem = e.uid, this.keywords = "";
      const t = this.multiple ? this.selection.map((i) => i[this.itemValueKey]) : e[this.itemValueKey];
      this.$emit("item-select", e), this.$emit("update:modelValue", t), this.$emit("input", t), this.$refs.input.focus(), this.multiple || this.closeMenu();
    },
    unselectItem(e) {
      this.selection.splice(e ?? this.selection.length - 1, 1), this.highlightedItem = null, this.$emit("update:modelValue", null), this.$emit("input", null), this.$refs.input.focus();
    },
    selectExtraItem() {
      this.keywords = "", this.$emit("extra-item-select"), this.closeMenu();
    },
    setEndOfMenuClick() {
      setTimeout(() => this.menuIsBeingClicked = false, 100);
    },
    onClick() {
      this.openOnKeydown || this.openMenu(), this.$refs.input.focus();
    },
    onFocus() {
      this.openOnKeydown || this.openMenu();
    },
    // Can be triggered by a click outside the autocomplete, or by a tab key.
    // It should not be simply triggered by input blur, because when we click a menu item it will
    // blur the input for a few ms before we refocus it.
    // onBlur () {
    //   this.closeMenu()
    // },
    onKeydown(e) {
      var i;
      const t = this.filteredItems.length + (this.$slots["extra-item"] ? 1 : 0);
      if ((!this.openOnKeydown || (this.keywords || e.key.length === 1) && !this.menuOpen) && this.openMenu(), e.keyCode === 9) this.closeMenu();
      else if (e.keyCode === 8 && (!this.keywords || !e.target.selectionStart && !e.target.selectionEnd))
        this.unselectItem();
      else if (e.keyCode === 13)
        e.preventDefault(), this.highlightedItem === "extra-item" ? this.selectExtraItem() : this.highlightedItemIndex >= 0 && this.selectItem(this.filteredItems[this.highlightedItemIndex]);
      else if ([38, 40].includes(e.keyCode)) {
        e.preventDefault();
        let s = this.highlightedItemIndex;
        s === -1 ? s = e.keyCode === 38 ? t - 1 : 0 : s = (s + (e.keyCode === 38 ? -1 : 1) + t) % t, this.$slots["extra-item"] && s === t - 1 ? this.highlightedItem = "extra-item" : this.highlightedItem = ((i = this.filteredItems[s]) == null ? void 0 : i.uid) || 0;
        const n = this.$refs.menu;
        if (n)
          if (this.$slots["extra-item"] && s === t - 1) n.scrollTop = n.scrollHeight;
          else {
            const { offsetHeight: l, offsetTop: a } = n.childNodes[s] || {};
            n.scrollTop + n.offsetHeight - l < a ? n.scrollTop = a - n.offsetHeight + l : n.scrollTop > a && (n.scrollTop = a);
          }
      } else !this.multiple && this.selection.length && e.key.length === 1 && e.preventDefault();
    },
    // On drag & drop of a text in the input field, don't paste if single selection and already selected.
    onDrop(e) {
      !this.multiple && this.selection.length && e.preventDefault();
    },
    // When starting a sequence of keys that produces a character.
    onCompositionStart(e) {
      !this.multiple && this.selection.length && e.target.setAttribute("readonly", true);
    },
    onCompositionUpdate(e) {
      !this.multiple && this.selection.length && setTimeout(() => e.target.removeAttribute("readonly"), 200);
    },
    openMenu() {
      this.menuOpen || (this.menuOpen = true, document.addEventListener("click", this.onDocumentClick));
    },
    closeMenu() {
      this.menuOpen = false, document.removeEventListener("click", this.onDocumentClick);
    },
    onDocumentClick(e) {
      !this.$el.contains(e.target) && !this.$el.isSameNode(e.target) && this.closeMenu();
    }
  },
  created() {
    this.modelValue && (Array.isArray(this.modelValue) ? this.modelValue : [this.modelValue]).forEach((t) => {
      this.selection.push(this.optimizedItemsForSearch.find((i) => i[this.itemValueKey] === +t));
    });
  },
  beforeUnmount() {
    document.removeEventListener("click", this.onDocumentClick);
  },
  watch: {
    modelValue(e) {
      this.selection = [], e && (Array.isArray(e) ? e : [e]).forEach((i) => {
        this.selection.push(this.optimizedItemsForSearch.find((s) => s[this.itemValueKey] === +i));
      });
    }
  }
};
var kt = v(wt, [["render", yt]]);
var vt = { class: "w-app" };
function Ct(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", vt, [
    renderSlot(e.$slots, "default")
  ]);
}
var St = {
  name: "w-app",
  // Keep the props for the API documentation.
  props: {
    block: { type: Boolean },
    row: { type: Boolean },
    alignCenter: { type: Boolean },
    alignEnd: { type: Boolean },
    justifyCenter: { type: Boolean },
    justifyEnd: { type: Boolean },
    justifySpaceBetween: { type: Boolean },
    justifySpaceAround: { type: Boolean },
    justifySpaceEvenly: { type: Boolean },
    textCenter: { type: Boolean },
    textRight: { type: Boolean }
  }
};
var Bt = v(St, [["render", Ct]]);
var _t = { class: "w-badge-wrap" };
function $t(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", _t, [
    renderSlot(e.$slots, "default"),
    createVNode(Transition, {
      name: `${i.transition}`
    }, {
      default: withCtx(() => [
        i.modelValue ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass(["w-badge", l.classes]),
          style: normalizeStyle(l.styles),
          "aria-atomic": "true",
          "aria-label": "Badge",
          "aria-live": "polite",
          role: "status"
        }, [
          i.dot ? createCommentVNode("", true) : renderSlot(e.$slots, "badge", { key: 0 }, () => [
            createTextVNode(toDisplayString(i.modelValue === true ? "" : i.modelValue || ""), 1)
          ])
        ], 6)) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["name"])
  ]);
}
var It = {
  name: "w-badge",
  props: {
    modelValue: { default: true },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    top: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    bottom: { type: Boolean },
    overlap: { type: Boolean },
    inline: { type: Boolean },
    color: { type: String },
    size: { type: [Number, String] },
    bgColor: { type: String, default: "primary" },
    badgeClass: { type: String },
    outline: { type: Boolean },
    shadow: { type: Boolean },
    dot: { type: Boolean },
    round: { type: Boolean },
    transition: { type: String, default: "fade" },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  computed: {
    forcedSize() {
      return this.size && (isNaN(this.size) ? this.size : `${this.size}px`);
    },
    presetSize() {
      return this.xs && "xs" || this.sm && "sm" || this.md && "md" || this.lg && "lg" || this.xl && "xl" || "md";
    },
    position() {
      return [
        this.top && "top" || this.bottom && "bottom" || "top",
        this.left && "left" || this.right && "right" || "right"
      ];
    },
    classes() {
      const e = this.$slots.badge && this.$slots.badge().map((t) => t.children).join("");
      return {
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        [this.badgeClass]: this.badgeClass || null,
        "w-badge--round": this.round || (e || this.modelValue + "" || "").length < 2,
        "w-badge--dark": this.dark,
        "w-badge--light": this.light,
        "w-badge--outline": this.outline,
        "w-badge--inline": this.inline,
        "w-badge--shadow": this.shadow,
        "w-badge--overlap": this.overlap,
        "w-badge--dot": this.dot,
        [`size--${this.presetSize}`]: this.presetSize && !this.forcedSize,
        [`w-badge--${this.position.join(" w-badge--")}`]: true
      };
    },
    styles() {
      return this.forcedSize && `font-size: ${this.forcedSize}`;
    }
  }
};
var Vt = v(It, [["render", $t]]);
var Tt = ["innerHTML"];
function Lt(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-breadcrumbs", l.classes])
  }, [
    (openBlock(true), createElementBlock(Fragment, null, renderList(i.items, (r, d) => (openBlock(), createElementBlock(Fragment, null, [
      d && e.$slots.separator ? (openBlock(), createElementBlock("span", {
        class: normalizeClass(["w-breadcrumbs__separator", i.separatorColor]),
        key: `${d}a`
      }, [
        renderSlot(e.$slots, "separator", { index: d })
      ], 2)) : d ? (openBlock(), createBlock(a, {
        class: normalizeClass(["w-breadcrumbs__separator", i.separatorColor]),
        key: `${d}b`
      }, {
        default: withCtx(() => [
          createTextVNode(toDisplayString(i.icon), 1)
        ]),
        _: 2
      }, 1032, ["class"])) : createCommentVNode("", true),
      r[i.itemRouteKey] && (d < i.items.length - 1 || i.linkLastItem) ? (openBlock(), createElementBlock(Fragment, { key: 2 }, [
        e.$slots.item ? (openBlock(), createBlock(resolveDynamicComponent(l.hasRouter ? "router-link" : "a"), {
          class: normalizeClass(["w-breadcrumbs__item", i.color || null]),
          key: `${d}c`,
          to: l.hasRouter && r[i.itemRouteKey],
          href: r[i.itemRouteKey]
        }, {
          default: withCtx(() => [
            renderSlot(e.$slots, "item", {
              item: r,
              index: d + 1,
              isLast: d === i.items.length - 1
            })
          ]),
          _: 2
        }, 1032, ["to", "href", "class"])) : (openBlock(), createBlock(resolveDynamicComponent(l.hasRouter ? "router-link" : "a"), {
          class: normalizeClass(["w-breadcrumbs__item", i.color || null]),
          key: `${d}d`,
          to: l.hasRouter && r[i.itemRouteKey],
          href: r[i.itemRouteKey],
          innerHTML: r[i.itemLabelKey]
        }, null, 8, ["to", "href", "innerHTML", "class"]))
      ], 64)) : e.$slots.item ? renderSlot(e.$slots, "item", {
        key: `${d}e`,
        item: r,
        index: d + 1,
        isLast: d === i.items.length - 1
      }) : (openBlock(), createElementBlock("span", {
        key: `${d}f`,
        innerHTML: r[i.itemLabelKey]
      }, null, 8, Tt))
    ], 64))), 256))
  ], 2);
}
var Rt = {
  name: "w-breadcrumbs",
  props: {
    items: { type: Array, required: true },
    linkLastItem: { type: Boolean },
    color: { type: String },
    // Applies on links.
    separatorColor: { type: String, default: "grey-light1" },
    icon: { type: String, default: "wi-chevron-right" },
    itemRouteKey: { type: String, default: "route" },
    itemLabelKey: { type: String, default: "label" },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean }
  },
  emits: [],
  computed: {
    hasRouter() {
      return "$router" in this;
    },
    size() {
      return this.xs && "xs" || this.sm && "sm" || this.lg && "lg" || this.xl && "xl" || "md";
    },
    classes() {
      return {
        [`size--${this.size}`]: true
      };
    }
  }
};
var xt = v(Rt, [["render", Lt]]);
var Pt = {
  key: 0,
  class: "w-button__loader"
};
function Ot(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon");
  return openBlock(), createBlock(resolveDynamicComponent(!i.disabled && i.route ? "a" : "button"), mergeProps({
    class: ["w-button", l.classes],
    type: !i.route && i.type,
    href: !i.disabled && i.route && (l.externalLink ? i.route : l.resolvedRoute) || null,
    disabled: !!i.disabled || null
  }, l.attrs, { style: l.styles }), {
    default: withCtx(() => [
      i.icon ? (openBlock(), createBlock(a, normalizeProps(mergeProps({ key: 0 }, i.iconProps || {})), {
        default: withCtx(() => [
          createTextVNode(toDisplayString(i.icon), 1)
        ]),
        _: 1
      }, 16)) : renderSlot(e.$slots, "default", { key: 1 }),
      createVNode(Transition, { name: "scale-fade" }, {
        default: withCtx(() => [
          i.loading ? (openBlock(), createElementBlock("div", Pt, [
            renderSlot(e.$slots, "loading", {}, () => [
              t[0] || (t[0] = createBaseVNode("svg", { viewBox: "0 0 40 40" }, [
                createBaseVNode("circle", {
                  cx: "20",
                  cy: "20",
                  r: "18",
                  fill: "transparent",
                  stroke: "currentColor",
                  "stroke-width": "4",
                  "stroke-linecap": "round"
                })
              ], -1))
            ])
          ])) : createCommentVNode("", true)
        ]),
        _: 3
      })
    ]),
    _: 3
  }, 16, ["type", "href", "class", "disabled", "style"]);
}
var zt = {
  // Fully handle the attrs and listeners manually for the case of a router link that has both a
  // route and onClick.
  inheritAttrs: false,
  props: {
    color: { type: String },
    bgColor: { type: String },
    dark: { type: Boolean },
    light: { type: Boolean },
    outline: { type: Boolean },
    text: { type: Boolean },
    round: { type: Boolean },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    route: { type: [String, Object] },
    // Creates a link.
    // Force use of `a` instead of router-link.
    // Router link does not go to a url starting with `#` with history mode.
    forceLink: { type: Boolean },
    type: { type: String, default: "button" },
    disabled: { type: Boolean },
    loading: { type: Boolean },
    // If an icon is passed, no text will display.
    icon: { type: String, default: null },
    iconProps: { type: Object, default: () => ({}) },
    // Positions.
    absolute: { type: Boolean },
    fixed: { type: Boolean },
    top: { type: Boolean },
    bottom: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    zIndex: { type: [Number, String] },
    // Sizes.
    width: { type: [Number, String] },
    height: { type: [Number, String] },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean }
  },
  emits: [],
  computed: {
    hasRouter() {
      return "$router" in this;
    },
    resolvedRoute() {
      return this.hasRouter ? this.$router.resolve(this.route).href : this.route;
    },
    attrs() {
      const e = this.route && this.hasRouter && !this.forceLink && !this.externalLink, t = (i) => {
        this.$attrs.onClick && this.$attrs.onClick(i), this.$router.push(this.route), i.stopPropagation(), i.preventDefault();
      };
      return {
        ...this.$attrs,
        onClick: !this.disabled && (e ? t : this.$attrs.onClick)
      };
    },
    size() {
      return this.xs && "xs" || this.sm && "sm" || this.lg && "lg" || this.xl && "xl" || "md";
    },
    position() {
      return [
        this.top && "top" || this.bottom && "bottom" || "top",
        this.left && "left" || this.right && "right" || "right"
      ];
    },
    externalLink() {
      return /^(https?:)?\/\/|mailto:|tel:/.test(this.route);
    },
    classes() {
      return {
        // If no color / bg color is set, set a primary color by default.
        "primary--bg": !this.bgColor && !this.color && !(this.outline || this.text),
        primary: !this.bgColor && !this.color && !this.dark && (this.outline || this.text),
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-button--dark": this.dark,
        "w-button--light": this.light,
        "w-button--outline": this.outline,
        "w-button--text": this.text,
        "w-button--round": this.round,
        "w-button--tile": this.tile,
        "w-button--shadow": this.shadow,
        "w-button--loading": this.loading,
        "w-button--icon": this.icon,
        [`size--${this.size}`]: true,
        "w-button--absolute": this.absolute,
        "w-button--fixed": this.fixed,
        [`w-button--${this.position.join(" w-button--")}`]: this.absolute || this.fixed
      };
    },
    styles() {
      return {
        width: (isNaN(this.width) ? this.width : `${this.width}px`) || null,
        height: (isNaN(this.height) ? this.height : `${this.height}px`) || null,
        zIndex: this.zIndex || this.zIndex === 0 || null
      };
    }
  }
};
var Mt = v(zt, [["render", Ot]]);
var Et = ["innerHTML"];
function At(e, t, i, s, n, l) {
  const a = resolveComponent("button-partial");
  return i.tooltip ? (openBlock(), createBlock(resolveDynamicComponent("w-tooltip"), normalizeProps(mergeProps({ key: 0 }, i.tooltipProps)), {
    activator: withCtx(({ on: r }) => [
      createVNode(a, mergeProps(l.buttonProps, toHandlers(r)), {
        default: withCtx(() => [
          renderSlot(e.$slots, "default")
        ]),
        _: 2
      }, 1040)
    ]),
    default: withCtx(() => [
      createBaseVNode("div", { innerHTML: i.tooltip }, null, 8, Et)
    ]),
    _: 3
  }, 16)) : (openBlock(), createBlock(a, normalizeProps(mergeProps({ key: 1 }, l.buttonProps)), {
    loading: withCtx(() => [
      renderSlot(e.$slots, "loading")
    ]),
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16));
}
var Wt = {
  name: "w-button",
  inheritAttrs: false,
  // The attrs are only bound to the button-partial, not the root.
  props: {
    color: { type: String },
    bgColor: { type: String },
    dark: { type: Boolean },
    outline: { type: Boolean },
    text: { type: Boolean },
    round: { type: Boolean },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    tooltip: { type: String },
    tooltipProps: { type: Object, default: () => ({}) },
    route: { type: [String, Object] },
    // Creates a link.
    // Force use of `a` instead of router-link.
    // Router link does not go to a url starting with `#` with history mode.
    forceLink: { type: Boolean },
    type: { type: String, default: "button" },
    disabled: { type: Boolean },
    loading: { type: Boolean },
    // If an icon is passed, no text will display.
    icon: { type: String, default: null },
    iconProps: { type: Object, default: () => ({}) },
    // Positions.
    absolute: { type: Boolean },
    fixed: { type: Boolean },
    top: { type: Boolean },
    bottom: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    zIndex: { type: [Number, String] },
    // Sizes.
    width: { type: [Number, String] },
    height: { type: [Number, String] },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean }
  },
  components: { ButtonPartial: Mt },
  emits: [],
  computed: {
    buttonProps() {
      const { tooltip: e, tooltipProps: t = {}, ...i } = this.$props;
      return { ...i, ...this.$attrs };
    }
  }
};
var Kt = v(Wt, [["render", At]]);
var M = (e = {}) => (typeof e == "string" ? e = { [e]: true } : Array.isArray(e) && (e = { [e.join(" ")]: true }), e);
var Nt = ["innerHTML"];
function Dt(e, t, i, s, n, l) {
  const a = resolveComponent("w-image");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-card", l.classes])
  }, [
    e.$slots.title ? (openBlock(), createElementBlock("div", {
      key: 0,
      class: normalizeClass(["w-card__title", { "w-card__title--has-toolbar": e.$slots.title && l.titleHasToolbar, ...l.titleClasses }])
    }, [
      renderSlot(e.$slots, "title")
    ], 2)) : i.title ? (openBlock(), createElementBlock("div", {
      key: 1,
      class: normalizeClass(["w-card__title", { "w-card__title--has-toolbar": e.$slots.title && l.titleHasToolbar, ...l.titleClasses }]),
      innerHTML: i.title
    }, null, 10, Nt)) : createCommentVNode("", true),
    i.image ? (openBlock(), createBlock(a, mergeProps({
      key: 2,
      class: "w-card__image",
      src: i.image
    }, l.imgProps), {
      default: withCtx(() => [
        renderSlot(e.$slots, "image-content")
      ]),
      _: 3
    }, 16, ["src"])) : createCommentVNode("", true),
    createBaseVNode("div", {
      class: normalizeClass(["w-card__content", l.contentClasses])
    }, [
      renderSlot(e.$slots, "default")
    ], 2),
    e.$slots.actions ? (openBlock(), createElementBlock("div", {
      key: 3,
      class: normalizeClass(["w-card__actions", { "w-card__actions--has-toolbar": l.actionsHasToolbar }])
    }, [
      renderSlot(e.$slots, "actions")
    ], 2)) : createCommentVNode("", true)
  ], 2);
}
var Ht = {
  name: "w-card",
  props: {
    color: { type: String },
    bgColor: { type: String },
    shadow: { type: Boolean },
    noBorder: { type: Boolean },
    tile: { type: Boolean },
    title: { type: String },
    image: { type: String },
    imageProps: { type: Object },
    titleClass: { type: [String, Object, Array] },
    contentClass: { type: [String, Object, Array] },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  computed: {
    titleClasses() {
      return M(this.titleClass);
    },
    contentClasses() {
      return M(this.contentClass);
    },
    titleHasToolbar() {
      const { title: e } = this.$slots;
      return e && e().map((t) => t.type.name).join("").includes("w-toolbar");
    },
    actionsHasToolbar() {
      const { actions: e } = this.$slots;
      return e && e().map((t) => t.type.name).join("").includes("w-toolbar");
    },
    imgProps() {
      return {
        tag: "div",
        ratio: 1 / 2,
        ...this.imageProps
      };
    },
    classes() {
      return {
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-card--no-border": this.noBorder,
        "w-card--tile": this.tile,
        "w-card--shadow": this.shadow,
        "w-card--dark": this.dark,
        "w-card--light": this.light
      };
    }
  }
};
var Ft = v(Ht, [["render", Dt]]);
var E = {
  inject: {
    // Used in each form component to determine whether to use the w-form-element wrap or not.
    // So, if a form component is in a form, use the w-form-element wrap.
    formRegister: { default: null },
    // If the form is disabled or readonly, apply to all the form components.
    formProps: { default: () => ({ disabled: false, readonly: false }) }
  },
  props: {
    name: { type: String },
    // When sending data through form.
    disabled: { type: Boolean },
    readonly: { type: Boolean },
    required: { type: Boolean },
    tabindex: { type: String },
    validators: { type: Array }
  },
  data: () => ({
    valid: null
    // Null is pristine (unknown), can also be true or false.
  }),
  computed: {
    inputName() {
      return this.name || `${this.$options.name}--${this._.uid}`;
    },
    isDisabled() {
      return this.disabled || this.formProps.disabled;
    },
    isReadonly() {
      return this.readonly || this.formProps.readonly;
    },
    validationColor() {
      return this.formProps.validationColor;
    },
    labelClasses() {
      return {
        [this.labelColor]: this.labelColor && this.valid !== false,
        [this.validationColor]: this.valid === false
      };
    }
  },
  methods: {
    // Allow triggering a particular field validation manually via `$refs.myField.validate()`.
    validate() {
      this.$refs.formEl.validate(this);
    }
  }
};
var jt = ["id", "name", "checked", "disabled", "required", "tabindex", "aria-checked"];
var Ut = ["for"];
var qt = ["for", "innerHTML"];
var Xt = ["for"];
var Yt = ["for", "innerHTML"];
function Gt(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister && !l.wCheckboxes ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: n.isChecked, disabled: e.isDisabled, readonly: e.isReadonly }, {
    valid: e.valid,
    "onUpdate:valid": t[5] || (t[5] = (a) => e.valid = a),
    onReset: t[6] || (t[6] = (a) => {
      e.$emit("update:modelValue", n.isChecked = null), e.$emit("input", null);
    }),
    class: l.classes
  }), {
    default: withCtx(() => [
      createBaseVNode("input", {
        ref: "input",
        id: `w-checkbox--${e._.uid}`,
        type: "checkbox",
        name: e.inputName,
        checked: n.isChecked || null,
        disabled: e.isDisabled || e.isReadonly || null,
        required: e.required || null,
        tabindex: e.tabindex || null,
        onFocus: t[0] || (t[0] = (a) => e.$emit("focus", a)),
        onBlur: t[1] || (t[1] = (a) => e.$emit("blur", a)),
        onChange: t[2] || (t[2] = (a) => l.onInput()),
        onKeypress: t[3] || (t[3] = withKeys((...a) => l.onInput && l.onInput(...a), ["enter"])),
        "aria-checked": n.isChecked || "false",
        role: "checkbox"
      }, null, 40, jt),
      l.hasLabel && i.labelOnLeft ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        e.$slots.default ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-checkbox__label w-form-el-shakable pr2", e.labelClasses]),
          for: `w-checkbox--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, Ut)) : i.label ? (openBlock(), createElementBlock("label", {
          key: 1,
          class: normalizeClass(["w-checkbox__label w-form-el-shakable pr2", e.labelClasses]),
          for: `w-checkbox--${e._.uid}`,
          innerHTML: i.label
        }, null, 10, qt)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true),
      createBaseVNode("div", {
        class: normalizeClass(["w-checkbox__input", this.color]),
        onClick: t[4] || (t[4] = (a) => {
          e.$refs.input.focus(), e.$refs.input.click();
        })
      }, t[7] || (t[7] = [
        createBaseVNode("svg", { viewBox: "-0.5 0 12 10" }, [
          createBaseVNode("polyline", { points: "1 5 4 8 10 2" })
        ], -1)
      ]), 2),
      l.hasLabel && !i.labelOnLeft ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        e.$slots.default ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-checkbox__label w-form-el-shakable pl2", e.labelClasses]),
          for: `w-checkbox--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, Xt)) : i.label ? (openBlock(), createElementBlock("label", {
          key: 1,
          class: normalizeClass(["w-checkbox__label w-form-el-shakable pl2", e.labelClasses]),
          for: `w-checkbox--${e._.uid}`,
          innerHTML: i.label
        }, null, 10, Yt)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 16, ["valid", "class"]);
}
var Jt = {
  name: "w-checkbox",
  mixins: [E],
  inject: {
    wCheckboxes: { default: null }
  },
  props: {
    modelValue: { default: false },
    // v-model to check or uncheck.
    // When `value` is taken by a v-model and multiple w-checkbox are plugged on
    // the same v-model, this allow returning to the v-model a custom value.
    returnValue: {},
    label: { type: String },
    labelOnLeft: { type: Boolean },
    color: { type: String, default: "primary" },
    labelColor: { type: String, default: "primary" },
    noRipple: { type: Boolean },
    indeterminate: { type: Boolean },
    round: { type: Boolean },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, tabindex, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus", "blur"],
  data() {
    return {
      isChecked: this.modelValue,
      ripple: {
        start: false,
        end: false,
        timeout: null
      }
    };
  },
  computed: {
    hasLabel() {
      return this.label || this.$slots.default;
    },
    classes() {
      return {
        [`w-checkbox w-checkbox--${this.isChecked ? "checked" : "unchecked"}`]: true,
        "w-checkbox--disabled": this.isDisabled,
        "w-checkbox--readonly": this.isReadonly,
        "w-checkbox--indeterminate": this.indeterminate,
        "w-checkbox--ripple": this.ripple.start,
        "w-checkbox--rippled": this.ripple.end,
        "w-checkbox--round": this.round,
        "w-checkbox--dark": this.dark,
        "w-checkbox--light": this.light
      };
    }
  },
  methods: {
    onInput() {
      this.isChecked = !this.isChecked;
      const e = this.isChecked && this.returnValue !== void 0 ? this.returnValue : this.isChecked;
      this.$emit("update:modelValue", e), this.$emit("input", e), this.noRipple || (this.isChecked ? (this.ripple.start = true, this.ripple.timeout = setTimeout(() => {
        this.ripple.start = false, this.ripple.end = true, setTimeout(() => this.ripple.end = false, 100);
      }, 700)) : (this.ripple.start = false, clearTimeout(this.ripple.timeout)));
    }
  },
  watch: {
    modelValue(e) {
      this.isChecked = e;
    }
  }
};
var Qt = v(Jt, [["render", Gt]]);
var Zt = ["innerHTML"];
function ei(e, t, i, s, n, l) {
  const a = resolveComponent("w-checkbox");
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: l.checkboxItems.some((r) => r._isChecked), disabled: e.isDisabled }, {
    valid: e.valid,
    "onUpdate:valid": t[1] || (t[1] = (r) => e.valid = r),
    onReset: l.reset,
    column: !i.inline,
    wrap: i.inline,
    class: l.classes
  }), {
    default: withCtx(() => [
      (openBlock(true), createElementBlock(Fragment, null, renderList(l.checkboxItems, (r, d) => (openBlock(), createBlock(a, mergeProps({
        key: d,
        "model-value": r._isChecked,
        "onUpdate:modelValue": (h) => l.toggleCheck(r, h),
        onFocus: t[0] || (t[0] = (h) => e.$emit("focus", h)),
        name: `${e.inputName}[]`,
        ref_for: true
      }, { label: r.label, color: r.color, labelOnLeft: i.labelOnLeft, labelColor: i.labelColor, round: i.round }, {
        disabled: e.isDisabled || null,
        readonly: e.isReadonly || null,
        class: { mt1: !i.inline && d }
      }), {
        default: withCtx(() => [
          e.$slots[`item.${d + 1}`] || e.$slots.item ? renderSlot(e.$slots, e.$slots[`item.${d + 1}`] ? `item.${d + 1}` : "item", {
            key: 0,
            item: l.getOriginalItem(r),
            checked: !!r._isChecked,
            index: d + 1,
            innerHTML: r.label
          }) : r.label ? (openBlock(), createElementBlock("div", {
            key: 1,
            innerHTML: r.label
          }, null, 8, Zt)) : createCommentVNode("", true)
        ]),
        _: 2
      }, 1040, ["model-value", "onUpdate:modelValue", "name", "disabled", "readonly", "class"]))), 128))
    ]),
    _: 3
  }, 16, ["valid", "onReset", "column", "wrap", "class"]);
}
var ti = {
  name: "w-checkboxes",
  mixins: [E],
  props: {
    items: { type: Array, required: true },
    // All the possible options.
    modelValue: { type: Array },
    // v-model on selected option.
    // If true, the returnValue set on each w-checkboxes item will be returned once the checkbox is
    // checked. If false & by default, the return value of the w-checkboxes is an array of booleans.
    returnValues: { type: Boolean },
    labelOnLeft: { type: Boolean },
    itemLabelKey: { type: String, default: "label" },
    itemValueKey: { type: String, default: "value" },
    itemColorKey: { type: String, default: "color" },
    // Support a different color per item.
    inline: { type: Boolean },
    round: { type: Boolean },
    color: { type: String, default: "primary" },
    labelColor: { type: String, default: "primary" }
    // Props from mixin: name, disabled, readonly, required, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus"],
  provide() {
    return { wCheckboxes: true };
  },
  computed: {
    checkboxItems() {
      return (this.items || []).map((e, t) => {
        const i = e[this.itemValueKey] === void 0 ? e[this.itemLabelKey] || t : e[this.itemValueKey];
        return reactive({
          ...e,
          label: e[this.itemLabelKey],
          _index: t,
          value: i,
          // If no value is set then add one to prevent error.
          color: e[this.itemColorKey] || this.color,
          _isChecked: this.modelValue && this.modelValue.includes(i)
        });
      });
    },
    classes() {
      return [
        "w-checkboxes",
        `w-checkboxes--${this.inline ? "inline" : "column"}`
      ];
    }
  },
  methods: {
    reset() {
      this.checkboxItems.forEach((e) => e._isChecked = null), this.$emit("update:modelValue", []), this.$emit("input", []);
    },
    toggleCheck(e, t) {
      e._isChecked = t;
      const i = this.checkboxItems.filter((s) => s._isChecked).map((s) => this.returnValues ? s.returnValue : s.value);
      this.$emit("update:modelValue", i), this.$emit("input", i);
    },
    // Return the original item (so there is no `_index`, etc.).
    getOriginalItem(e) {
      return this.items[e._index];
    }
  }
};
var ii = v(ti, [["render", ei]]);
function li(e, t, i, s, n, l) {
  const a = resolveComponent("w-button"), r = resolveComponent("w-flex"), d = resolveComponent("w-menu"), h = resolveDirective("focus");
  return openBlock(), createBlock(d, mergeProps({
    modelValue: e.showPopup,
    "onUpdate:modelValue": t[2] || (t[2] = (y) => e.showPopup = y)
  }, l.wMenuProps), {
    activator: withCtx(({ on: y }) => [
      createVNode(a, mergeProps({ class: "w-confirm" }, { ...e.$attrs, ...l.buttonProps }, toHandlers(i.disablePrompt ? {} : { ...y })), {
        default: withCtx(() => [
          renderSlot(e.$slots, "default")
        ]),
        _: 2
      }, 1040)
    ]),
    default: withCtx(() => [
      createVNode(r, {
        column: !i.inline,
        "align-center": ""
      }, {
        default: withCtx(() => [
          createBaseVNode("div", null, [
            renderSlot(e.$slots, "question", {}, () => [
              createTextVNode(toDisplayString(i.question), 1)
            ])
          ]),
          createBaseVNode("div", {
            class: normalizeClass(["w-flex justify-end", i.inline ? "ml2" : "mt2"])
          }, [
            i.cancel !== false ? (openBlock(), createBlock(a, mergeProps({
              key: 0,
              class: "mr2"
            }, l.cancelButtonProps, {
              "bg-color": (l.cancelButton || {}).bgColor || "error",
              onKeyup: t[0] || (t[0] = withKeys((y) => !i.persistent && l.onCancel(), ["escape"])),
              onClick: l.onCancel
            }), {
              default: withCtx(() => [
                renderSlot(e.$slots, "cancel", {}, () => [
                  createTextVNode(toDisplayString(l.cancelButton.label), 1)
                ])
              ]),
              _: 3
            }, 16, ["bg-color", "onClick"])) : createCommentVNode("", true),
            withDirectives((openBlock(), createBlock(a, mergeProps(l.confirmButtonProps, {
              "bg-color": (l.confirmButton || {}).bgColor || "success",
              onKeyup: t[1] || (t[1] = withKeys((y) => !i.persistent && l.onCancel(), ["escape"])),
              onClick: l.onConfirm
            }), {
              default: withCtx(() => [
                renderSlot(e.$slots, "confirm", {}, () => [
                  createTextVNode(toDisplayString(l.confirmButton.label), 1)
                ])
              ]),
              _: 3
            }, 16, ["bg-color", "onClick"])), [
              [h]
            ])
          ], 2)
        ]),
        _: 3
      }, 8, ["column"])
    ]),
    _: 3
  }, 16, ["modelValue"]);
}
var si = {
  name: "w-confirm",
  inheritAttrs: false,
  // The attrs are only bound to the button, not the w-menu.
  props: {
    // Main button props.
    bgColor: { type: String },
    color: { type: String },
    icon: { type: String },
    disablePrompt: { type: Boolean },
    // If true, the confirm button acts like a simple w-button.
    mainButton: { type: Object },
    // Allow passing down an object of props to the w-button component.
    question: { type: String, default: "Are you sure?" },
    // Cancel & confirm buttons props.
    // Allow passing down an object of props to the w-button component.
    // If a string is given, that will be the label of the button.
    // If false, no cancel button.
    cancel: { type: [Boolean, Object, String], default: void 0 },
    // Allow passing down an object of props to the w-button component.
    // If a string is given, that will be the label of the button.
    confirm: { type: [Object, String] },
    // global menu props.
    inline: { type: Boolean },
    // The layout inside the menu.
    // W-menu props.
    // Allow passing down an object of props to the w-menu component.
    menu: { type: Object, default: () => ({}) },
    // Allow passing down an object of props to the w-tooltip component.
    // If a string is given, that will be the label of the tooltip.
    tooltip: { type: [Boolean, Object, String] },
    // All the menu props shorthands, as long as they don't conflict with the button props.
    noArrow: { type: Boolean },
    // Adds a directional triangle to the edge of the menu, like a tooltip.
    top: { type: Boolean },
    bottom: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    alignTop: { type: Boolean },
    alignBottom: { type: Boolean },
    alignLeft: { type: Boolean },
    alignRight: { type: Boolean },
    persistent: { type: Boolean },
    transition: { type: String },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: ["cancel", "confirm"],
  data: () => ({
    showPopup: false,
    props: []
  }),
  computed: {
    cancelButton() {
      let e = { label: typeof this.cancel == "string" ? this.cancel : "Cancel" };
      return typeof this.cancel == "object" && (e = Object.assign({}, e, this.cancel)), e;
    },
    // Props to pass down to the w-button component.
    cancelButtonProps() {
      const { label: e, ...t } = this.cancelButton;
      return t;
    },
    confirmButton() {
      let e = { label: typeof this.confirm == "string" ? this.confirm : "Confirm" };
      return typeof this.confirm == "object" && (e = Object.assign({}, e, this.confirm)), e;
    },
    // Props to pass down to the w-button component.
    confirmButtonProps() {
      const { label: e, ...t } = this.confirmButton;
      return t;
    },
    wMenuProps() {
      return {
        top: this.top,
        bottom: this.bottom,
        left: this.left,
        right: this.right,
        arrow: !this.noArrow,
        alignTop: this.alignTop,
        alignBottom: this.alignBottom,
        alignLeft: this.alignLeft,
        alignRight: this.alignRight,
        persistent: this.persistent,
        transition: this.transition,
        ...this.menu
      };
    },
    tooltipObject() {
      let e = { label: typeof this.tooltip == "string" ? this.tooltip : "" };
      return typeof this.tooltip == "object" && (e = Object.assign({}, e, this.tooltip)), e;
    },
    buttonProps() {
      const { label: e, ...t } = this.tooltipObject;
      return {
        bgColor: this.bgColor,
        color: this.color,
        icon: this.icon,
        dark: this.dark,
        light: this.light,
        tooltip: e,
        tooltipProps: t,
        ...this.mainButton
      };
    }
  },
  methods: {
    onCancel() {
      this.$emit("cancel"), this.showPopup = false;
    },
    onConfirm() {
      this.$emit("confirm"), this.showPopup = false;
    }
  }
};
var oi = v(si, [["render", li]]);
function ai(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-date-picker", l.classes]),
    style: normalizeStyle(l.styles)
  }, [
    renderSlot(e.$slots, "default")
  ], 6);
}
var ni = {
  name: "w-date-picker",
  props: {
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  computed: {
    classes() {
      return {
        "w-date-picker--dark": this.dark,
        "w-date-picker--light": this.light
      };
    },
    styles() {
      return false;
    }
  }
};
var ri = v(ni, [["render", ai]]);
function di(e, t, i, s, n, l) {
  const a = resolveComponent("w-card"), r = resolveComponent("w-overlay");
  return openBlock(), createBlock(r, {
    class: normalizeClass(["w-dialog", l.classes]),
    "model-value": n.showWrapper,
    persistent: i.persistent,
    "persistent-no-animation": i.persistentNoAnimation,
    onClick: l.onOutsideClick,
    onClose: l.onClose,
    "bg-color": i.overlayColor,
    opacity: i.overlayOpacity
  }, {
    default: withCtx(() => [
      createVNode(Transition, {
        name: i.transition,
        appear: "",
        onAfterLeave: l.onBeforeClose
      }, {
        default: withCtx(() => [
          withDirectives(createVNode(a, {
            class: normalizeClass(["w-dialog__content", i.dialogClass]),
            ref: "dialog",
            "no-border": "",
            color: i.color,
            "bg-color": i.bgColor,
            "title-class": i.titleClass,
            "content-class": i.contentClass,
            title: i.title || void 0,
            style: normalizeStyle(l.contentStyles)
          }, createSlots({
            default: withCtx(() => [
              renderSlot(e.$slots, "default")
            ]),
            _: 2
          }, [
            e.$slots.title ? {
              name: "title",
              fn: withCtx(() => [
                renderSlot(e.$slots, "title")
              ]),
              key: "0"
            } : void 0,
            e.$slots.actions ? {
              name: "actions",
              fn: withCtx(() => [
                renderSlot(e.$slots, "actions")
              ]),
              key: "1"
            } : void 0
          ]), 1032, ["color", "bg-color", "class", "title-class", "content-class", "title", "style"]), [
            [vShow, n.showContent]
          ])
        ]),
        _: 3
      }, 8, ["name", "onAfterLeave"])
    ]),
    _: 3
  }, 8, ["model-value", "persistent", "persistent-no-animation", "onClick", "onClose", "bg-color", "opacity", "class"]);
}
var ui = {
  name: "w-dialog",
  props: {
    modelValue: { default: true },
    width: { type: [Number, String], default: 0 },
    fullscreen: { type: Boolean },
    persistent: { type: Boolean },
    persistentNoAnimation: { type: Boolean },
    tile: { type: Boolean },
    title: { type: String },
    transition: { type: String, default: "fade" },
    // @todo: validator.
    titleClass: { type: [String, Object, Array] },
    contentClass: { type: [String, Object, Array] },
    dialogClass: { type: [String, Object, Array] },
    overlayColor: { type: String },
    color: { type: String },
    bgColor: { type: String },
    overlayOpacity: { type: [Number, String, Boolean] },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  provide() {
    return {
      // If a detachable is used inside a w-drawer without an appendTo, default to the drawer element
      // instead of the w-app.
      detachableDefaultRoot: () => this.$refs.dialog.$el || null
    };
  },
  emits: ["input", "update:modelValue", "before-close", "close"],
  data() {
    return {
      showWrapper: this.modelValue,
      showContent: this.modelValue
    };
  },
  computed: {
    titleClasses() {
      return M(this.titleClass);
    },
    contentClasses() {
      return M(this.contentClass);
    },
    dialogClasses() {
      return M(this.dialogClass);
    },
    maxWidth() {
      let e = this.width;
      return e && parseInt(e) === +e && (e += "px"), e;
    },
    classes() {
      return {
        "w-dialog--fullscreen": this.fullscreen,
        "w-dialog--dark": this.dark,
        "w-dialog--light": this.light
      };
    },
    contentStyles() {
      return {
        maxWidth: !this.fullscreen && this.maxWidth ? this.maxWidth : null
      };
    }
  },
  methods: {
    onOutsideClick() {
      this.persistent || (this.showContent = false, this.transition === "fade" && this.onBeforeClose());
    },
    onBeforeClose() {
      this.showWrapper = false, this.$emit("before-close");
    },
    onClose() {
      this.$emit("update:modelValue", false), this.$emit("input", false), this.$emit("close");
    }
  },
  watch: {
    modelValue(e) {
      this.showWrapper = e, this.showContent = e;
    }
  }
};
var hi = v(ui, [["render", di]]);
var ci = ["role", "aria-orientation"];
function pi(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-divider", l.classes]),
    role: e.$slots.default ? null : "presentation",
    "aria-orientation": i.vertical ? "vertical" : "horizontal"
  }, [
    renderSlot(e.$slots, "default")
  ], 10, ci);
}
var fi = {
  name: "w-divider",
  props: {
    vertical: { type: Boolean },
    color: { type: String },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  computed: {
    classes() {
      return {
        [`w-divider--has-color ${this.color}`]: this.color,
        [`w-divider--${this.vertical ? "vertical" : "horizontal"}`]: true,
        "w-divider--has-content": this.$slots.default,
        "w-divider--dark": this.dark,
        "w-divider--light": this.light
      };
    }
  }
};
var mi = v(fi, [["render", pi]]);
var gi = { class: "w-drawer-wrap__pushable" };
function bi(e, t, i, s, n, l) {
  const a = resolveComponent("w-overlay");
  return n.showWrapper || i.pushContent ? (openBlock(), createElementBlock("div", {
    key: 0,
    class: normalizeClass(["w-drawer-wrap", l.wrapperClasses])
  }, [
    i.pushContent ? (openBlock(), createElementBlock("div", {
      key: 0,
      class: "w-drawer-wrap__track",
      style: normalizeStyle(l.trackStyles)
    }, [
      createBaseVNode("div", gi, [
        i.noOverlay ? createCommentVNode("", true) : (openBlock(), createBlock(a, {
          key: 0,
          modelValue: n.showDrawer,
          "onUpdate:modelValue": t[0] || (t[0] = (r) => n.showDrawer = r),
          onClick: l.onOutsideClick,
          persistent: i.persistent,
          "persistent-no-animation": "",
          "bg-color": i.overlayColor || "transparent",
          opacity: i.overlayOpacity
        }, null, 8, ["modelValue", "onClick", "persistent", "bg-color", "opacity"])),
        renderSlot(e.$slots, "pushable")
      ]),
      createVNode(Transition, {
        name: "fade",
        onBeforeLeave: l.onBeforeClose,
        onAfterLeave: l.onClose
      }, {
        default: withCtx(() => [
          n.showDrawer ? (openBlock(), createBlock(resolveDynamicComponent(i.tag || "aside"), {
            key: 0,
            class: normalizeClass(["w-drawer", l.drawerClasses]),
            ref: "drawer",
            style: normalizeStyle(l.styles)
          }, {
            default: withCtx(() => [
              renderSlot(e.$slots, "default")
            ]),
            _: 3
          }, 8, ["class", "style"])) : createCommentVNode("", true)
        ]),
        _: 3
      }, 8, ["onBeforeLeave", "onAfterLeave"])
    ], 4)) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
      i.noOverlay ? createCommentVNode("", true) : (openBlock(), createBlock(a, {
        key: 0,
        modelValue: n.showDrawer,
        "onUpdate:modelValue": t[1] || (t[1] = (r) => n.showDrawer = r),
        onClick: l.onOutsideClick,
        persistent: i.persistent,
        "persistent-no-animation": "",
        "bg-color": i.overlayColor,
        opacity: i.overlayOpacity
      }, null, 8, ["modelValue", "onClick", "persistent", "bg-color", "opacity"])),
      createVNode(Transition, {
        name: l.transitionName,
        appear: "",
        onBeforeLeave: l.onBeforeClose,
        onAfterLeave: l.onClose
      }, {
        default: withCtx(() => [
          n.showDrawer ? (openBlock(), createBlock(resolveDynamicComponent(i.tag || "aside"), {
            key: 0,
            class: normalizeClass(["w-drawer", l.drawerClasses]),
            ref: "drawer",
            style: normalizeStyle(l.styles)
          }, {
            default: withCtx(() => [
              renderSlot(e.$slots, "default")
            ]),
            _: 3
          }, 8, ["class", "style"])) : createCommentVNode("", true)
        ]),
        _: 3
      }, 8, ["name", "onBeforeLeave", "onAfterLeave"])
    ], 64))
  ], 2)) : createCommentVNode("", true);
}
var yi = { left: "right", right: "left", top: "down", bottom: "up" };
var wi = {
  name: "w-drawer",
  props: {
    modelValue: { default: true },
    left: { type: Boolean },
    right: { type: Boolean },
    top: { type: Boolean },
    bottom: { type: Boolean },
    persistent: { type: Boolean },
    persistentNoAnimation: { type: Boolean },
    fitContent: { type: Boolean },
    width: { type: [Number, String, Boolean] },
    height: { type: [Number, String, Boolean] },
    zIndex: { type: [Number, String, Boolean] },
    color: { type: String },
    bgColor: { type: String },
    noOverlay: { type: Boolean },
    pushContent: { type: Boolean },
    absolute: { type: Boolean },
    overlayColor: { type: String },
    overlayOpacity: { type: [Number, String, Boolean] },
    drawerClass: { type: String },
    tag: { type: String, default: "aside" },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  provide() {
    return {
      // If a detachable is used inside a w-drawer without an appendTo, default to the drawer element
      // instead of the w-app.
      detachableDefaultRoot: () => this.$refs.drawer || null
    };
  },
  emits: ["input", "update:modelValue", "before-close", "close"],
  data() {
    return {
      showWrapper: this.modelValue,
      showDrawer: this.modelValue,
      persistentAnimate: false
    };
  },
  computed: {
    // Return the width or height value if defined, or false otherwise.
    size() {
      let e = this.width || this.height;
      return e && parseInt(e) == e && (e += "px"), e || false;
    },
    // Return `width` or `height`, `width` by default (position right by default).
    sizeProperty() {
      return ["left", "right"].includes(this.position) && "width" || "height";
    },
    position() {
      return this.left && "left" || this.right && "right" || this.top && "top" || this.bottom && "bottom" || "right";
    },
    wrapperClasses() {
      return {
        "w-drawer-wrap--fixed": !this.absolute && !this.pushContent,
        "w-drawer-wrap--absolute": this.absolute,
        "w-drawer-wrap--push-content": this.pushContent
      };
    },
    drawerClasses() {
      return {
        [this.drawerClass]: true,
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-drawer--open": !!this.showDrawer,
        [`w-drawer--${this.position}`]: true,
        "w-drawer--fit-content": this.fitContent,
        "w-drawer--persistent": this.persistent,
        "w-drawer--persistent-animate": this.persistent && this.persistentAnimate,
        "w-drawer--dark": this.dark,
        "w-drawer--light": this.light
      };
    },
    // The track is a wrapper around the pushable content and drawer.
    // It moves inside the overflow hidden outer wrap.
    trackStyles() {
      return this.pushContent && this.showDrawer && {
        transform: `translateX(${this.position === "left" ? "" : "-"}${this.size || "200px"})`
      };
    },
    styles() {
      return {
        [`max-${this.sizeProperty}`]: this.size || null,
        zIndex: this.zIndex || this.zIndex === 0 || null
      };
    },
    // In case of pushing content, the showWrapper variable doesn't reflect the behavior:
    // unmount the drawer (remove from DOM) is what it does when showWrapper is false.
    unmountDrawer() {
      return !this.showWrapper;
    },
    transitionName() {
      return `slide-${yi[this.position]}`;
    }
  },
  methods: {
    onBeforeClose() {
      this.$emit("before-close");
    },
    onClose() {
      this.showWrapper = false, this.$emit("update:modelValue", false), this.$emit("input", false), this.$emit("close");
    },
    onOutsideClick() {
      this.persistent ? this.persistentNoAnimation || (this.persistentAnimate = true, setTimeout(() => this.persistentAnimate = false, 200)) : this.showDrawer = false;
    }
  },
  watch: {
    modelValue(e) {
      e && (this.showWrapper = true), this.showDrawer = e;
    }
  }
};
var ki = v(wi, [["render", bi]]);
function vi(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(i.tag), {
    class: normalizeClass(["w-flex", l.classes])
  }, {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 8, ["class"]);
}
var Ci = {
  name: "w-flex",
  props: {
    tag: { type: String, default: "div" },
    column: { type: Boolean },
    grow: { type: Boolean },
    noGrow: { type: Boolean },
    shrink: { type: Boolean },
    noShrink: { type: Boolean },
    fillHeight: { type: Boolean },
    wrap: { type: Boolean },
    alignStart: { type: Boolean },
    alignCenter: { type: Boolean },
    alignEnd: { type: Boolean },
    justifyStart: { type: Boolean },
    justifyCenter: { type: Boolean },
    justifyEnd: { type: Boolean },
    justifySpaceBetween: { type: Boolean },
    justifySpaceAround: { type: Boolean },
    justifySpaceEvenly: { type: Boolean },
    basisZero: { type: Boolean },
    gap: { type: [Number, String], default: 0 }
  },
  computed: {
    classes() {
      return {
        column: this.column,
        grow: this.grow,
        "no-grow": this.noGrow,
        shrink: this.shrink,
        "no-shrink": this.noShrink,
        "fill-height": this.fillHeight,
        wrap: this.wrap,
        "align-start": this.alignStart,
        "align-center": this.alignCenter,
        "align-end": this.alignEnd,
        "justify-start": this.justifyStart,
        "justify-center": this.justifyCenter,
        "justify-end": this.justifyEnd,
        "justify-space-between": this.justifySpaceBetween,
        "justify-space-around": this.justifySpaceAround,
        "justify-space-evenly": this.justifySpaceEvenly,
        "basis-zero": this.basisZero,
        [`gap${this.gap}`]: ~~this.gap
      };
    }
  }
};
var Si = v(Ci, [["render", vi]]);
function Bi(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("form", {
    class: normalizeClass(["w-form", l.classes]),
    onSubmit: t[0] || (t[0] = (...a) => l.onSubmit && l.onSubmit(...a)),
    onReset: t[1] || (t[1] = (...a) => l.reset && l.reset(...a)),
    novalidate: ""
  }, [
    renderSlot(e.$slots, "default")
  ], 34);
}
var _i = async (e, t) => {
  for (const i of e)
    if (await t(i)) return true;
  return false;
};
var $i = {
  name: "w-form",
  props: {
    modelValue: {},
    allowSubmit: { type: Boolean },
    noKeyupValidation: { type: Boolean },
    noBlurValidation: { type: Boolean },
    errorPlaceholders: { type: Boolean },
    validationColor: { type: String, default: "error" },
    disabled: { type: Boolean },
    readonly: { type: Boolean }
  },
  provide() {
    return {
      formRegister: this.register,
      formUnregister: this.unregister,
      validateElement: this.validateElement,
      // Give access to the form params (like disabled) to all the form components.
      // To keep it reactive, we need an object not a list of props (by design in Vue).
      formProps: this.$props
    };
  },
  emits: [
    "submit",
    "before-validate",
    "validate",
    "success",
    "error",
    "reset",
    "input",
    "update:modelValue",
    "update:errorsCount"
  ],
  data: () => ({
    formElements: [],
    status: null,
    // null = pristine, false = error, true = success.
    errorsCount: 0
  }),
  computed: {
    classes() {
      return {
        "w-form--pristine": this.status === null,
        "w-form--error": this.status === false,
        "w-form--success": this.status === true,
        "w-form--error-placeholders": this.errorPlaceholders
      };
    }
  },
  methods: {
    register(e) {
      this.formElements.push(e);
    },
    unregister(e) {
      this.formElements = this.formElements.filter((t) => t._.uid !== e._.uid);
    },
    /**
     * On form submit or programmatic call, go through each validation rule of each form element,
     * and count the number of errors.
     * Display the element error inside the element if any error, and $emit the result.
     *
     * @param {Object} e the submit event
     * @return {Boolean} true if the form is valid
     */
    async validate(e) {
      this.$emit("before-validate", { e, errorsCount: this.errorsCount });
      let t = 0;
      return await (async () => {
        var i;
        for (const s of this.formElements)
          !((i = s.validators) != null && i.length) || s.disabled || s.readonly || (await this.checkElementValidators(s), t += ~~!s.Validation.isValid, s.$emit("update:valid", s.Validation.isValid));
      })(), this.updateErrorsCount(t), this.status = !t, this.$emit("validate", { e, errorsCount: t }), this.$emit(this.status ? "success" : "error", { e, errorsCount: t }), this.status;
    },
    async validateElement(e) {
      return await this.checkElementValidators(e), this.updateErrorsCount(), e.Validation.isValid;
    },
    // Execute the validators 1 by 1 until a failure is found. If it happens, raise the error
    // message in the form element.
    async checkElementValidators(e) {
      let t = false, i = "";
      await _i(e.validators, async (s) => {
        const n = await (typeof s == "function" && s(e.inputValue));
        return t = typeof n != "string", i = t ? "" : n, !t;
      }), e.hasJustReset = false, e.Validation.isValid = t, e.Validation.message = i;
    },
    // Reset is called from:
    // - the form `reset` event listener
    // - the modelValue watcher when set to `null`.
    reset(e) {
      this.status = null, this.formElements.forEach((t) => t.reset()), this.updateErrorsCount(0, true), this.$emit("reset", e);
    },
    updateErrorsCount(e = null, t = false) {
      this.errorsCount = e !== null ? e : this.formElements.reduce((i, s) => i + ~~(s.Validation.isValid === false), 0), this.status = t ? null : !this.errorsCount, this.$emit("update:modelValue", this.status), this.$emit("input", this.status), this.$emit("update:errorsCount", this.errorsCount);
    },
    onSubmit(e) {
      this.validate(e), this.$emit("submit", e), (!this.allowSubmit || !this.status) && e.preventDefault();
    }
  },
  created() {
    this.status = this.modelValue || null;
  },
  watch: {
    modelValue(e) {
      (this.status === false && e || e === null && this.status !== null) && this.reset(), this.status = e;
    }
  }
};
var Ii = v($i, [["render", Bi]]);
function Vi(e, t, i, s, n, l) {
  const a = resolveComponent("w-transition-expand");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(l.classes)
  }, [
    createBaseVNode("div", {
      class: normalizeClass(["w-flex grow", [i.column ? "column" : "align-center", i.wrap ? "wrap" : ""]])
    }, [
      renderSlot(e.$slots, "default")
    ], 2),
    createVNode(a, { y: "" }, {
      default: withCtx(() => [
        e.Validation.message ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass(["w-form-el__error", l.formProps.validationColor])
        }, [
          renderSlot(e.$slots, "error-message", {
            message: e.Validation.message
          }, () => [
            createTextVNode(toDisplayString(e.Validation.message), 1)
          ])
        ], 2)) : createCommentVNode("", true)
      ]),
      _: 3
    })
  ], 2);
}
var Ti = {
  name: "w-form-element",
  props: {
    valid: { required: true },
    disabled: { type: Boolean },
    readonly: { type: Boolean },
    inputValue: { required: true },
    // The form element's input value.
    validators: { type: Array },
    isFocused: { default: false },
    // Watched.
    column: { default: false },
    // Flex direction of the embedded component: column or row by default.
    wrap: { default: false }
    // Flex-wrap if needed.
  },
  inject: {
    formRegister: { default: null },
    formUnregister: { default: null },
    validateElement: { default: null },
    formProps: {
      default: () => ({
        noKeyupValidation: false,
        noBlurValidation: false,
        validationColor: "error",
        disabled: false,
        readonly: false
      })
    }
  },
  emits: ["reset", "update:valid"],
  data: () => ({
    Validation: {
      isValid: null,
      // Null is pristine (unknown), can also be true or false.
      message: ""
      // Updated on w-form validation.
    },
    hasJustReset: false
  }),
  computed: {
    classes() {
      return [
        "w-form-el",
        ["w-form-el--error error", "w-form-el--success", "w-form-el--pristine"][this.Validation.isValid === null ? 2 : ~~this.Validation.isValid]
      ];
    }
  },
  methods: {
    // Called from w-form reset.
    reset() {
      this.$emit("reset"), this.$emit("update:valid", null), this.Validation.message = "", this.Validation.isValid = null, this.hasJustReset = true;
    },
    // Allow triggering a particular field validation manually via `$refs.myField.validate()`.
    async validate() {
      this.$emit("update:valid", await this.validateElement(this));
    }
  },
  watch: {
    async inputValue() {
      if (this.hasJustReset) return this.hasJustReset = false;
      !this.formProps.noKeyupValidation && this.validators && this.$emit("update:valid", await this.validateElement(this));
    },
    async isFocused(e) {
      e ? this.hasJustReset = false : !this.formProps.noBlurValidation && this.validators && !this.readonly && this.$emit("update:valid", await this.validateElement(this));
    }
  },
  created() {
    this.formRegister && this.formRegister(this);
  },
  beforeUnmount() {
    this.formUnregister && this.formUnregister(this);
  }
};
var Li = v(Ti, [["render", Vi]]);
function Ri(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(i.tag), {
    class: normalizeClass(["w-grid", l.classes])
  }, {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 8, ["class"]);
}
var xi = {
  name: "w-grid",
  props: {
    tag: { type: String, default: "div" },
    columns: { type: [Number, Object, String] },
    gap: { type: [Number, Object, String], default: 0 }
  },
  computed: {
    breakpointsColumns() {
      let e = { xs: 0, sm: 0, md: 0, lg: 0, xl: 0 };
      switch (typeof this.columns) {
        case "object":
          e = Object.assign(e, this.columns);
          break;
        case "number":
        case "string":
          e = Object.keys(e).reduce((t, i) => t[i] = ~~this.columns, {});
          break;
      }
      return e;
    },
    breakpointsGap() {
      let e = { xs: 0, sm: 0, md: 0, lg: 0, xl: 0 };
      switch (typeof this.gap) {
        case "object":
          e = Object.assign(e, this.gap);
          break;
        case "number":
        case "string":
          e = Object.keys(e).reduce((t, i) => t[i] = ~~this.gap, {});
          break;
      }
      return e;
    },
    classes() {
      let e = null;
      typeof this.columns == "object" && (e = Object.entries(this.breakpointsColumns).reduce((i, [s, n]) => (i[`${s}-columns${n}`] = true, i), {}));
      let t = null;
      return typeof this.gap == "object" && (t = Object.entries(this.breakpointsGap).reduce((i, [s, n]) => (i[`${s}-gap${n}`] = true, i), {})), {
        ...e || { [`columns${this.columns}`]: this.columns },
        ...t || { [`gap${this.gap}`]: this.gap }
      };
    }
  }
};
var Pi = v(xi, [["render", Ri]]);
function Oi(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(i.tag || "i"), {
    class: normalizeClass(["w-icon", l.classes]),
    role: "icon",
    "aria-hidden": "true",
    style: normalizeStyle(l.readIcon() && l.styles)
  }, {
    default: withCtx(() => [
      l.hasLigature ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        createTextVNode(toDisplayString(e.icon), 1)
      ], 64)) : createCommentVNode("", true)
    ]),
    _: 1
  }, 8, ["class", "style"]);
}
var zi = {
  name: "w-icon",
  props: {
    tag: { type: String, default: "i" },
    color: { type: String },
    bgColor: { type: String },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    spin: { type: Boolean },
    spinA: { type: Boolean },
    rotate135a: { type: Boolean },
    rotate90a: { type: Boolean },
    rotate45a: { type: Boolean },
    rotate45: { type: Boolean },
    rotate90: { type: Boolean },
    rotate135: { type: Boolean },
    rotate180: { type: Boolean },
    flipX: { type: Boolean },
    flipY: { type: Boolean },
    size: { type: [Number, String] }
  },
  emits: [],
  data: () => ({
    icon: "",
    fontName: ""
  }),
  computed: {
    hasLigature() {
      return this.$waveui.config.iconsLigature === this.fontName;
    },
    forcedSize() {
      return this.size && (isNaN(this.size) ? this.size : `${this.size}px`);
    },
    presetSize() {
      return this.xs && "xs" || this.sm && "sm" || this.md && "md" || this.lg && "lg" || this.xl && "xl" || null;
    },
    classes() {
      return {
        [this.fontName]: true,
        [!this.hasLigature && this.icon]: !this.hasLigature && this.icon,
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        [`size--${this.presetSize}`]: this.presetSize && !this.forcedSize,
        "w-icon--spin": this.spin,
        "w-icon--spin-a": this.spinA,
        "w-icon--rotate45": this.rotate45,
        "w-icon--rotate90": this.rotate90,
        "w-icon--rotate135": this.rotate135,
        "w-icon--rotate180": this.rotate180,
        "w-icon--rotate-45": this.rotate45a,
        "w-icon--rotate-90": this.rotate90a,
        "w-icon--rotate-135": this.rotate135a,
        "w-icon--flip-x": this.flipX,
        "w-icon--flip-y": this.flipY
      };
    },
    styles() {
      return this.forcedSize && `font-size: ${this.forcedSize}`;
    }
  },
  methods: {
    readIcon() {
      const { default: e } = this.$slots, [t = "", i = ""] = typeof e == "function" && e()[0].children.trim().split(" ") || [];
      return this.fontName = t, this.icon = i, true;
    }
  }
};
var Mi = v(zi, [["render", Oi]]);
var Ei = {
  key: 0,
  class: "w-image__loader"
};
var Ai = ["innerHTML"];
function Wi(e, t, i, s, n, l) {
  const a = resolveComponent("w-progress");
  return openBlock(), createBlock(resolveDynamicComponent(l.wrapperTag), {
    class: normalizeClass(["w-image", l.wrapperClasses]),
    style: normalizeStyle(l.wrapperStyles)
  }, {
    default: withCtx(() => [
      createVNode(Transition, {
        name: i.transition,
        appear: ""
      }, {
        default: withCtx(() => [
          n.loaded ? (openBlock(), createBlock(resolveDynamicComponent(l.normalized.tag), {
            key: 0,
            class: normalizeClass(["w-image__image", l.imageClasses]),
            style: normalizeStyle(l.imageStyles),
            src: l.normalized.tag === "img" ? n.computedImg.src : null
          }, null, 8, ["class", "style", "src"])) : createCommentVNode("", true)
        ]),
        _: 1
      }, 8, ["name"]),
      !i.noSpinner && n.loading ? (openBlock(), createElementBlock("span", Ei, [
        e.$slots.loading ? renderSlot(e.$slots, "loading", { key: 0 }) : (openBlock(), createBlock(a, mergeProps({
          key: 1,
          circle: "",
          indeterminate: ""
        }, i.spinnerColor ? { color: i.spinnerColor } : {}), null, 16))
      ])) : createCommentVNode("", true),
      e.$slots.default ? (openBlock(), createBlock(resolveDynamicComponent(l.wrapperTag), {
        key: 1,
        class: normalizeClass(["w-image__content", i.contentClass])
      }, {
        default: withCtx(() => [
          renderSlot(e.$slots, "default")
        ]),
        _: 3
      }, 8, ["class"])) : createCommentVNode("", true),
      e.$slots.caption ? (openBlock(), createElementBlock("figcaption", {
        key: 2,
        class: normalizeClass(["w-image__caption caption", i.captionClass])
      }, [
        renderSlot(e.$slots, "caption")
      ], 2)) : i.caption ? (openBlock(), createElementBlock("figcaption", {
        key: 3,
        class: normalizeClass(["w-image__caption caption", i.captionClass]),
        innerHTML: i.caption
      }, null, 10, Ai)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 8, ["class", "style"]);
}
var Ki = {
  name: "w-image",
  props: {
    tag: { type: String, default: "span" },
    // span, div, figure, img.
    src: { type: String },
    width: { type: [Number, String] },
    height: { type: [Number, String] },
    maxWidth: { type: [Number, String] },
    maxHeight: { type: [Number, String] },
    ratio: { type: [Number, String] },
    lazy: { type: Boolean },
    absolute: { type: Boolean },
    fixed: { type: Boolean },
    contain: { type: Boolean },
    noSpinner: { type: Boolean },
    spinnerColor: { type: String },
    fallback: { type: String },
    transition: { type: String, default: "fade" },
    contentClass: { type: [String, Array, Object] },
    caption: { type: String },
    captionClass: { type: String }
  },
  emits: ["loading", "loaded", "error"],
  data() {
    return {
      loading: false,
      loaded: false,
      // The computed image source, and real image dimensions.
      computedImg: {
        src: "",
        width: 0,
        height: 0,
        ratio: 0
      }
    };
  },
  computed: {
    // Normalized props.
    normalized() {
      return {
        width: (isNaN(this.width) ? this.width : `${this.width}px`) || null,
        height: (isNaN(this.height) ? this.height : `${this.height}px`) || null,
        maxWidth: (isNaN(this.maxWidth) ? this.maxWidth : `${this.maxWidth}px`) || null,
        maxHeight: (isNaN(this.maxHeight) ? this.maxHeight : `${this.maxHeight}px`) || null,
        ratio: parseFloat(this.ratio) || void 0,
        tag: this.tag === "img" || this.tag === "figure" || this.caption ? "img" : "span"
      };
    },
    wrapperTag() {
      return this.caption || this.tag === "figure" ? "figure" : ["span", "div"].includes(this.tag) ? this.tag : "span";
    },
    wrapperClasses() {
      return {
        "w-image--absolute": this.absolute,
        "w-image--fixed": this.fixed,
        "w-image--has-ratio": this.normalized.ratio
      };
    },
    wrapperStyles() {
      let e = this.normalized.width;
      const t = this.normalized.height;
      let i = this.normalized.maxWidth, s = this.normalized.ratio;
      return s && !e && !t ? e = "100%" : !e && !t ? (e = "100%", i = this.normalized.maxWidth || `${this.computedImg.width}px`, s = s || this.computedImg.width / this.computedImg.height) : (e && !t || t && !e) && (s = s || this.computedImg.width / this.computedImg.height), {
        width: e,
        height: t,
        maxWidth: i,
        maxHeight: this.normalized.maxHeight,
        aspectRatio: s
      };
    },
    imageClasses() {
      return {
        "w-image__image--loading": this.loading,
        "w-image__image--loaded": this.loaded,
        "w-image__image--contain": this.contain
      };
    },
    imageStyles() {
      return {
        "background-image": this.normalized.tag !== "img" && this.loaded ? `url('${this.computedImg.src}')` : null
      };
    }
  },
  methods: {
    loadImage(e = false) {
      if (!this.loading)
        return this.loading = true, this.loaded = false, this.$emit("loading", e ? this.fallback : this.src), new Promise((t) => {
          const i = new Image();
          i.onload = (s) => (this.computedImg.width = s.target.width, this.computedImg.height = s.target.height, this.computedImg.ratio = s.target.height / s.target.width, this.loading = false, this.loaded = true, this.computedImg.src = e ? this.fallback : this.src, this.$emit("loaded", this.computedImg.src), t(i)), i.onerror = (s) => {
            this.$emit("error", s), this.fallback && !e ? (this.loading = false, this.loadImage(true)) : (this.loading = false, this.loaded = false);
          }, i.src = e ? this.fallback : this.src;
        });
    }
  },
  mounted() {
    if (!this.src) return j("The w-image component was used without src.");
    if (this.lazy) {
      const e = new IntersectionObserver((t) => {
        t[0] && t[0].isIntersecting && (this.loadImage(), e.disconnect());
      }, this.intersectionConfig);
      e.observe(this.$el);
    } else this.loadImage();
  },
  watch: {
    src() {
      this.loadImage();
    }
  }
};
var Ni = v(Ki, [["render", Wi]]);
var Di = ["name"];
var Hi = ["for"];
var Fi = ["id", "type", "name", "placeholder", "step", "min", "max", "minlength", "maxlength", "readonly", "aria-readonly", "disabled", "required", "tabindex"];
var ji = ["id", "name", "multiple", "disabled", "data-progress"];
var Ui = {
  class: "w-input__no-file",
  key: "no-file"
};
var qi = ["for"];
var Xi = ["src"];
var Yi = ["for"];
function Gi(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon"), r = resolveComponent("w-progress");
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: n.inputValue, disabled: e.isDisabled, readonly: e.isReadonly, isFocused: n.isFocused }, {
    valid: e.valid,
    "onUpdate:valid": t[10] || (t[10] = (d) => e.valid = d),
    onReset: t[11] || (t[11] = (d) => {
      e.$emit("update:modelValue", n.inputValue = ""), e.$emit("input", "");
    }),
    wrap: l.hasLabel && i.labelPosition !== "inside",
    class: l.classes,
    style: e.$attrs.style
  }), {
    default: withCtx(() => [
      i.type === "hidden" ? withDirectives((openBlock(), createElementBlock("input", {
        key: 0,
        type: "hidden",
        name: e.name || null,
        "onUpdate:modelValue": t[0] || (t[0] = (d) => n.inputValue = d)
      }, null, 8, Di)), [
        [vModelText, n.inputValue]
      ]) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        i.labelPosition === "left" ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
          e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
            key: 0,
            class: normalizeClass(["w-input__label w-input__label--left w-form-el-shakable", e.labelClasses]),
            for: `w-input--${e._.uid}`
          }, [
            renderSlot(e.$slots, "default", {}, () => [
              createTextVNode(toDisplayString(i.label), 1)
            ])
          ], 10, Hi)) : createCommentVNode("", true)
        ], 64)) : createCommentVNode("", true),
        createBaseVNode("div", {
          class: normalizeClass(["w-input__input-wrap", l.inputWrapClasses])
        }, [
          renderSlot(e.$slots, "icon-left", {
            inputId: `w-input--${e._.uid}`
          }, () => [
            i.innerIconLeft ? (openBlock(), createBlock(a, {
              key: 0,
              class: "w-input__icon w-input__icon--inner-left",
              tag: "label",
              for: `w-input--${e._.uid}`,
              onClick: t[1] || (t[1] = (d) => e.$emit("click:inner-icon-left", d))
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(i.innerIconLeft), 1)
              ]),
              _: 1
            }, 8, ["for"])) : createCommentVNode("", true)
          ]),
          i.type !== "file" ? withDirectives((openBlock(), createElementBlock("input", mergeProps({
            key: 0,
            class: "w-input__input",
            ref: "input",
            "onUpdate:modelValue": t[2] || (t[2] = (d) => n.inputValue = d),
            onInput: t[3] || (t[3] = (...d) => l.onInput && l.onInput(...d)),
            onFocus: t[4] || (t[4] = (...d) => l.onFocus && l.onFocus(...d)),
            onBlur: t[5] || (t[5] = (...d) => l.onBlur && l.onBlur(...d)),
            id: `w-input--${e._.uid}`,
            type: i.type,
            name: e.inputName,
            placeholder: i.placeholder || null,
            step: i.step || null,
            min: i.min || null,
            max: i.max || null,
            minlength: i.minlength || null,
            maxlength: i.maxlength || null,
            readonly: e.isReadonly || null,
            "aria-readonly": e.isReadonly ? "true" : "false",
            disabled: e.isDisabled || null,
            required: e.required || null,
            tabindex: e.tabindex || null
          }, l.attrs), null, 16, Fi)), [
            [vModelDynamic, n.inputValue]
          ]) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
            createBaseVNode("input", mergeProps({
              ref: "input",
              id: `w-input--${e._.uid}`,
              type: "file",
              name: e.name || null,
              onFocus: t[6] || (t[6] = (...d) => l.onFocus && l.onFocus(...d)),
              onBlur: t[7] || (t[7] = (...d) => l.onBlur && l.onBlur(...d)),
              onChange: t[8] || (t[8] = (...d) => l.onFileChange && l.onFileChange(...d)),
              multiple: i.multiple || null
            }, l.attrs, {
              disabled: e.isDisabled || null,
              "data-progress": l.overallFilesProgress
              /* Needed to emit the overallProgress. */
            }), null, 16, ji),
            createVNode(TransitionGroup, {
              class: "w-input__input w-input__input--file",
              tag: "label",
              name: "fade",
              for: `w-input--${e._.uid}`
            }, {
              default: withCtx(() => [
                !n.inputFiles.length && n.isFocused ? (openBlock(), createElementBlock("span", Ui, [
                  renderSlot(e.$slots, "no-file", {}, () => [
                    e.$slots["no-file"] === void 0 ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
                      createTextVNode("No file")
                    ], 64)) : createCommentVNode("", true)
                  ])
                ])) : createCommentVNode("", true),
                (openBlock(true), createElementBlock(Fragment, null, renderList(n.inputFiles, (d, h) => (openBlock(), createElementBlock("span", {
                  key: d.lastModified
                }, [
                  createTextVNode(toDisplayString(h ? ", " : ""), 1),
                  (openBlock(), createElementBlock("span", {
                    class: "filename",
                    key: `${h}b`
                  }, toDisplayString(d.base), 1)),
                  createTextVNode(toDisplayString(d.extension ? `.${d.extension}` : ""), 1)
                ]))), 128))
              ]),
              _: 3
            }, 8, ["for"])
          ], 64)),
          i.labelPosition === "inside" && l.showLabelInside ? (openBlock(), createElementBlock(Fragment, { key: 2 }, [
            e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
              key: 0,
              class: normalizeClass(["w-input__label w-input__label--inside w-form-el-shakable", e.labelClasses])
            }, [
              renderSlot(e.$slots, "default", {}, () => [
                createTextVNode(toDisplayString(i.label), 1)
              ])
            ], 2)) : createCommentVNode("", true)
          ], 64)) : createCommentVNode("", true),
          renderSlot(e.$slots, "icon-right", {
            inputId: `w-input--${e._.uid}`
          }, () => [
            i.innerIconRight ? (openBlock(), createBlock(a, {
              key: 0,
              class: "w-input__icon w-input__icon--inner-right",
              tag: "label",
              for: `w-input--${e._.uid}`,
              onClick: t[9] || (t[9] = (d) => e.$emit("click:inner-icon-right", d))
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(i.innerIconRight), 1)
              ]),
              _: 1
            }, 8, ["for"])) : createCommentVNode("", true)
          ]),
          l.hasLoading || i.showProgress && (l.uploadInProgress || l.uploadComplete) ? (openBlock(), createBlock(r, {
            key: 3,
            class: "fill-width",
            size: "2",
            color: i.progressColor || i.color,
            "model-value": i.showProgress ? (l.uploadInProgress || l.uploadComplete) && l.overallFilesProgress : l.loadingValue
          }, null, 8, ["color", "model-value"])) : createCommentVNode("", true)
        ], 2),
        i.type === "file" && i.preview && n.inputFiles.length ? (openBlock(), createElementBlock("label", {
          key: 1,
          class: "d-flex",
          for: `w-input--${e._.uid}`
        }, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(n.inputFiles, (d, h) => (openBlock(), createElementBlock(Fragment, null, [
            d.progress < 100 ? (openBlock(), createElementBlock("i", {
              class: "w-icon wi-spinner w-icon--spin size--sm w-input__file-preview primary",
              key: `${h}a`
            })) : d.preview ? (openBlock(), createElementBlock("img", {
              class: "w-input__file-preview",
              key: `${h}b`,
              src: d.preview,
              alt: ""
            }, null, 8, Xi)) : (openBlock(), createElementBlock("i", {
              class: normalizeClass(["w-icon w-input__file-preview primary size--md", i.preview && typeof i.preview == "string" ? i.preview : "wi-file"]),
              key: `${h}c`
            }, null, 2))
          ], 64))), 256))
        ], 8, qi)) : createCommentVNode("", true),
        i.labelPosition === "right" ? (openBlock(), createElementBlock(Fragment, { key: 2 }, [
          e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
            key: 0,
            class: normalizeClass(["w-input__label w-input__label--right w-form-el-shakable", e.labelClasses]),
            for: `w-input--${e._.uid}`
          }, [
            renderSlot(e.$slots, "default", {}, () => [
              createTextVNode(toDisplayString(i.label), 1)
            ])
          ], 10, Yi)) : createCommentVNode("", true)
        ], 64)) : createCommentVNode("", true)
      ], 64))
    ]),
    _: 3
  }, 16, ["valid", "wrap", "class", "style"]);
}
var Ji = {
  name: "w-input",
  mixins: [E],
  inheritAttrs: false,
  // The attrs should only be added to the input not the wrapper.
  props: {
    modelValue: { default: "" },
    type: { type: String, default: "text" },
    label: { type: String },
    labelPosition: { type: String, default: "inside" },
    innerIconLeft: { type: String },
    innerIconRight: { type: String },
    staticLabel: { type: Boolean },
    // When label is inside, fix the label above.
    placeholder: { type: String },
    color: { type: String, default: "primary" },
    bgColor: { type: String },
    labelColor: { type: String, default: "primary" },
    progressColor: { type: String },
    minlength: { type: [Number, String] },
    maxlength: { type: [Number, String] },
    step: { type: [Number, String] },
    min: { type: [Number, String] },
    max: { type: [Number, String] },
    outline: { type: Boolean },
    round: { type: Boolean },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    multiple: { type: Boolean },
    // Only for file uploads.
    preview: { type: [Boolean, String], default: true },
    // Only for file uploads.
    loading: { type: [Boolean, Number], default: false },
    // If a number is given, it will be the value of the progress.
    showProgress: { type: [Boolean] },
    // Only for file uploads.
    // Allow syncing the files 1 way: prefilling a file is not possible.
    // https://stackoverflow.com/questions/16365668/pre-populate-html-form-file-input
    files: { type: Array },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, tabindex, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus", "blur", "click:inner-icon-left", "click:inner-icon-right", "update:overallProgress"],
  data() {
    return {
      inputValue: this.modelValue,
      // In case of incorrect input type="number", the inputValue gets emptied,
      // and the label would come back on top of the input text.
      inputNumberError: false,
      isFocused: false,
      inputFiles: [],
      // For input type file.
      fileReader: null,
      // For input type file.
      isAutofilled: false
    };
  },
  computed: {
    attrs() {
      const { class: e, style: t, ...i } = this.$attrs;
      return this.type === "file" && !this.inputFiles.length && (i.value = null), i;
    },
    hasValue() {
      switch (this.type) {
        case "file":
          return !!this.inputFiles.length;
        case "number":
          return this.inputValue || this.inputValue === 0 || this.inputNumberError;
        case "date":
        case "time":
          return true;
        default:
          return this.inputValue || this.inputValue === 0;
      }
    },
    hasLabel() {
      return this.label || this.$slots.default;
    },
    hasLoading() {
      return ![void 0, false].includes(this.loading);
    },
    loadingValue() {
      let e;
      return typeof this.loading == "number" ? e = this.loading : this.loading && (e = this.type === "file" && this.overallFilesProgress ? this.overallFilesProgress : void 0), e;
    },
    showLabelInside() {
      return !this.staticLabel || !this.hasValue && !this.placeholder;
    },
    overallFilesProgress() {
      const t = +this.inputFiles.reduce((i, s) => i + s.progress, 0) / this.inputFiles.length;
      return this.$emit("update:overallProgress", this.inputFiles.length ? t : 0), t;
    },
    uploadInProgress() {
      return this.overallFilesProgress > 0 && this.overallFilesProgress < 100;
    },
    uploadComplete() {
      return this.overallFilesProgress === 100;
    },
    classes() {
      return {
        "w-input": true,
        "w-input--file": this.type === "file",
        "w-input--disabled": this.isDisabled,
        "w-input--readonly": this.isReadonly,
        [`w-input--${this.hasValue || this.isAutofilled ? "filled" : "empty"}`]: true,
        "w-input--focused": this.isFocused && !this.isReadonly,
        "w-input--dark": this.dark,
        "w-input--light": this.light,
        "w-input--floating-label": this.hasLabel && this.labelPosition === "inside" && !this.staticLabel,
        "w-input--no-padding": !this.outline && !this.bgColor && !this.shadow && !this.round,
        "w-input--has-placeholder": this.placeholder,
        "w-input--inner-icon-left": this.innerIconLeft,
        "w-input--inner-icon-right": this.innerIconRight,
        // With the inheritAttrs set to false any class on the component would be lost, so add it back.
        [this.$attrs.class]: !!this.$attrs.class
      };
    },
    inputWrapClasses() {
      return {
        [this.valid === false ? this.validationColor : this.color]: this.color || this.valid === false,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-input__input-wrap--file": this.type === "file",
        "w-input__input-wrap--round": this.round,
        "w-input__input-wrap--tile": this.tile,
        // Box adds a padding on input. If there is a bgColor or shadow, a padding is needed.
        "w-input__input-wrap--box": this.outline || this.bgColor || this.shadow,
        "w-input__input-wrap--underline": !this.outline,
        "w-input__input-wrap--shadow": this.shadow,
        "w-input__input-wrap--no-padding": !this.outline && !this.bgColor && !this.shadow && !this.round,
        "w-input__input-wrap--loading": this.loading || this.showProgress && this.uploadInProgress,
        "w-input__input-wrap--upload-complete": this.uploadComplete
      };
    }
  },
  methods: {
    onInput(e) {
      this.inputNumberError = e.target.validity.badInput, this.$emit("update:modelValue", this.inputValue), this.$emit("input", this.inputValue);
    },
    onFocus(e) {
      this.isFocused = true, this.$emit("focus", e);
    },
    onBlur(e) {
      this.isFocused = false, this.$emit("blur", e);
    },
    // For file input.
    onFileChange(e) {
      this.inputFiles = [...e.target.files].map((i) => {
        const [, s = "", n = "", l = ""] = i.name.match(/^(.*?)\.([^.]*)$|(.*)/), a = reactive({
          name: i.name,
          base: s || l,
          extension: n,
          type: i.type,
          size: i.size,
          lastModified: i.lastModified,
          preview: null,
          progress: 0,
          file: i
        });
        return this.readFile(i, a), a;
      });
      const t = this.multiple ? this.inputFiles : this.inputFiles[0];
      this.$emit("update:modelValue", t), this.$emit("input", t);
    },
    // For file input.
    readFile(e, t) {
      const i = new FileReader(), s = typeof this.preview == "string", n = e.type && e.type.startsWith("image/");
      this.preview && !s && n ? i.addEventListener("load", (l) => {
        t.preview = l.target.result;
      }) : delete t.preview, i.addEventListener("progress", (l) => {
        l.loaded && l.total && (t.progress = l.loaded * 100 / l.total);
      }), i.readAsDataURL(e);
    }
  },
  mounted() {
    setTimeout(() => {
      this.$refs.input && this.$refs.input.matches(":-webkit-autofill") && (this.isAutofilled = true);
    }, 400);
  },
  watch: {
    modelValue(e) {
      this.inputValue = e, !e && e !== 0 && (this.isAutofilled = false, this.inputFiles = []);
    }
  }
};
var Qi = v(Ji, [["render", Gi]]);
function Zi(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon"), r = resolveComponent("w-list", true);
  return openBlock(), createElementBlock("ul", {
    class: normalizeClass(["w-list", l.classes])
  }, [
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.listItems, (d, h) => (openBlock(), createElementBlock("li", {
      class: normalizeClass(["w-list__item", { "w-list__item--parent": (d.children || []).length }]),
      key: h
    }, [
      i.icon ? (openBlock(), createBlock(a, {
        key: 0,
        class: "w-list__item-bullet"
      }, {
        default: withCtx(() => [
          createTextVNode(toDisplayString(i.icon), 1)
        ]),
        _: 1
      })) : createCommentVNode("", true),
      e.$slots[`item.${h + 1}`] || e.$slots.item || e.$slots.default ? (openBlock(), createBlock(resolveDynamicComponent(i.checklist ? "w-checkbox" : i.nav && !d.disabled && d.route ? l.hasRouter ? "router-link" : "a" : "div"), mergeProps({
        key: 1,
        class: "w-list__item-label",
        ref_for: true
      }, l.liLabelProps(d, h, d._selected)), {
        default: withCtx(() => [
          e.$slots[`item.${h + 1}`] ? renderSlot(e.$slots, `item.${h + 1}`, {
            key: 0,
            item: l.cleanLi(d),
            index: h + 1,
            selected: d._selected
          }) : e.$slots.item ? renderSlot(e.$slots, "item", {
            key: 1,
            item: l.cleanLi(d),
            index: h + 1,
            selected: d._selected
          }) : renderSlot(e.$slots, "default", {
            key: 2,
            item: l.cleanLi(d),
            index: h + 1,
            selected: d._selected
          }, () => [
            createTextVNode(toDisplayString(d._label), 1)
          ])
        ]),
        _: 2
      }, 1040)) : (openBlock(), createBlock(resolveDynamicComponent(i.checklist ? "w-checkbox" : i.nav && !d.disabled && d.route ? l.hasRouter ? "router-link" : "a" : "div"), mergeProps({
        key: 2,
        class: "w-list__item-label",
        ref_for: true
      }, l.liLabelProps(d, h, d._selected)), null, 16)),
      (d.children || []).length ? (openBlock(), createBlock(r, mergeProps({
        key: 3,
        ref_for: true
      }, e.$props, {
        items: d.children,
        depth: i.depth + 1,
        "onUpdate:modelValue": t[0] || (t[0] = (y) => e.$emit("update:modelValue", y)),
        onInput: t[1] || (t[1] = (y) => e.$emit("input", y)),
        onItemClick: t[2] || (t[2] = (y) => e.$emit("item-click", y)),
        onItemSelect: t[3] || (t[3] = (y) => e.$emit("item-select", y))
      }), createSlots({ _: 2 }, [
        e.$slots.item ? {
          name: "item",
          fn: withCtx(({ item: y, index: S, selected: g }) => [
            renderSlot(e.$slots, "item", {
              item: l.cleanLi(y),
              index: S,
              selected: g
            })
          ]),
          key: "0"
        } : {
          name: "default",
          fn: withCtx(({ item: y, index: S, selected: g }) => [
            renderSlot(e.$slots, "default", {
              item: l.cleanLi(y),
              index: S,
              selected: g
            }, () => [
              createTextVNode(toDisplayString(y[i.itemLabelKey]), 1)
            ])
          ]),
          key: "1"
        }
      ]), 1040, ["items", "depth"])) : createCommentVNode("", true)
    ], 2))), 128))
  ], 2);
}
var el = {
  name: "w-list",
  props: {
    items: { type: [Array, Number], required: true },
    // All the possible options.
    modelValue: {},
    // v-model on selected item if any.
    checklist: { type: Boolean },
    roundCheckboxes: { type: Boolean },
    // Checklist option.
    // If selectable (if value !== false), this allows multiple selections.
    multiple: { type: Boolean },
    // When true, will add an id on the list and on all the list items.
    // Useful for a11y aria fields (e.g. use with w-select).
    addIds: { type: [Boolean, String] },
    hover: { type: Boolean },
    color: { type: String },
    // Applies to all the items.
    selectionColor: { type: String },
    // Applies to the selected items only.
    bgColor: { type: String },
    // Applies to all the items.
    // Navigation type adds a router-link on items with `route`.
    nav: { type: Boolean },
    icon: { type: String, default: "" },
    itemLabelKey: { type: String, default: "label" },
    // Name of the label field.
    itemValueKey: { type: String, default: "value" },
    // Name of the value field.
    itemClassKey: { type: String, default: "class" },
    // Name of the class field.
    itemColorKey: { type: String, default: "color" },
    // Support a different color per item.
    itemRouteKey: { type: String, default: "route" },
    // Name of the route field for `nav` lists.
    itemClass: { type: String },
    depth: { type: Number, default: 0 },
    // For recursive call.
    returnObject: { type: Boolean },
    // By default you can unselect a list item by re-selecting it.
    // Allow preventing that on single selection lists only.
    noUnselect: { type: Boolean },
    arrowsNavigation: { type: Boolean }
  },
  emits: ["input", "update:modelValue", "item-click", "item-select", "keydown:escape", "keydown:enter"],
  data: () => ({
    listItems: []
  }),
  computed: {
    hasRouter() {
      return "$router" in this;
    },
    listId() {
      return this.addIds ? typeof this.addIds == "string" ? this.addIds : `w-list--${this._.uid}` : null;
    },
    selectedItems() {
      return this.listItems.filter((e) => e._selected);
    },
    // Faster cached enabled items lookup.
    enabledItemsIndexes() {
      return this.listItems.filter((e) => !e.disabled).map((e) => e.index);
    },
    isMultipleSelect() {
      return this.multiple || this.checklist;
    },
    isSelectable() {
      return this.modelValue !== void 0 || this.checklist || this.nav;
    },
    SelectionColor() {
      const e = this.selectionColor === void 0 ? !this.color && "primary" : this.selectionColor;
      return this.isSelectable && e;
    },
    classes() {
      return {
        [this.color]: this.color || null,
        [`${this.bgColor}--bg`]: this.bgColor || null,
        "w-list--checklist": this.checklist,
        "w-list--navigation": this.nav,
        "w-list--icon": this.icon,
        [`w-list--child w-list--depth-${this.depth}`]: this.depth
      };
    }
  },
  methods: {
    // If object, get the item value, if none, get the item label, if none get the id.
    // If simple value, return as is.
    getItemValue(e) {
      return e && typeof e == "object" ? e[this.itemValueKey] !== void 0 ? e[this.itemValueKey] : e[this.itemLabelKey] !== void 0 ? e[this.itemLabelKey] : e.index : e;
    },
    // Action of selecting 1 item.
    selectItem(e, t) {
      e._selected && !this.multiple && this.noUnselect || (e._selected = t !== void 0 ? t : !e._selected, e._selected && !this.isMultipleSelect && this.listItems.forEach((i) => i._index !== e._index && (i._selected = false)), this.emitSelection());
    },
    liLabelClasses(e) {
      return {
        "w-list__item-label--disabled": e.disabled || this.nav && !e[this.itemRouteKey] && !e.children,
        "w-list__item-label--active": this.isSelectable && e._selected || null,
        "w-list__item-label--focused": e._focused,
        "w-list__item-label--hoverable": this.hover,
        "w-list__item-label--selectable": this.isSelectable,
        [e[this.itemColorKey]]: !!e[this.itemColorKey],
        [this.SelectionColor]: e._selected && !e[this.itemColorKey] && this.SelectionColor,
        [e[this.itemClassKey] || this.itemClass]: e[this.itemClassKey] || this.itemClass
      };
    },
    liLabelProps(e, t, i) {
      const s = this.$slots[`item.${t + 1}`] || this.$slots.item, n = () => {
        if (!e.disabled) {
          const d = this.cleanLi(e);
          this.$emit("item-click", d), this.$emit("item-select", d);
        }
      }, l = this.isSelectable && ((d) => {
        d.stopPropagation(), !e.disabled && this.selectItem(e);
      }), a = this.isSelectable && ((d) => {
        !e.disabled && d.keyCode === 13 ? (this.selectItem(e), this.$emit("keydown:enter"), this.$emit("item-select", this.cleanLi(e))) : d.keyCode === 27 ? this.$emit("keydown:escape") : this.arrowsNavigation && [38, 40].includes(d.keyCode) && (d.preventDefault(), d.keyCode === 38 && this.focusPrevNextItem(e._index, false), d.keyCode === 40 && this.focusPrevNextItem(e._index, true));
      }), r = {
        class: this.liLabelClasses(e),
        tabindex: e.disabled || this.checklist ? null : "0",
        "aria-selected": i ? "true" : "false",
        id: this.listId ? `${this.listId}_item-${t + 1}` : null,
        role: "option"
      };
      return this.checklist ? (r.modelValue = e._selected, r.color = e[this.itemColorKey] || this.color, r.round = this.roundCheckboxes, r.disabled = e.disabled, s || (r.label = e._label || null), r.onFocus = () => e._focused = true, r.onBlur = () => e._focused = false, r.onInput = (d) => this.selectItem(e, d), r.onClick = (d) => {
        const h = d.target.querySelector('input[type="checkbox"]');
        h && (h.focus(), h.click()), n();
      }) : this.nav ? (!e.disabled && e[this.itemRouteKey] && (r.onKeydown = a, r.onMousedown = l, this.$router ? (r.to = e[this.itemRouteKey], r.onClick = (d) => {
        d.preventDefault(), this.$router.push(e[this.itemRouteKey]), n();
      }) : (r.href = e[this.itemRouteKey], r.onClick = n)), s || (r.innerHTML = e._label)) : (this.isSelectable && (e.disabled || (r.tabindex = 0), r.onClick = n, r.onKeydown = a, r.onMousedown = l), s || (r.innerHTML = e._label)), r;
    },
    // Convert the received items selection to array if it is a unique value.
    // Also accept objects if returnObject is true and convert to the object's value.
    // In any case, always end up with an array of values.
    // The values given can be (in this order) a value, a label or the index of the item.
    checkSelection(e) {
      return e = Array.isArray(e) ? e : e ? [e] : [], this.returnObject && (e = e.map(this.getItemValue)), e;
    },
    // Emit the cleaned-up selection to the v-model.
    emitSelection() {
      const e = this.selectedItems.map((i) => {
        if (!this.returnObject) return i._value;
        const { _value: s, _selected: n, ...l } = i;
        return l;
      }), t = this.isMultipleSelect ? e : e[0] !== void 0 ? e[0] : null;
      this.$emit("update:modelValue", t), this.$emit("input", t);
    },
    focusPrevNextItem(e, t = true) {
      e = this.enabledItemsIndexes[this.enabledItemsIndexes.indexOf(e) + (t ? 1 : -1)];
      const i = t ? 0 : this.enabledItemsIndexes.length - 1;
      e === void 0 && (e = this.enabledItemsIndexes[i]), this.$el.querySelector(`#${this.listId}_item-${e + 1}`).focus();
    },
    cleanLi(e) {
      const { _index: t, _value: i, _label: s, _selected: n, _focused: l, ...a } = e;
      return a;
    },
    refreshListItems() {
      const e = typeof this.items == "number" ? Array(this.items).fill({}) : this.items || [];
      this.listItems = e.map((t, i) => ({
        ...t,
        _index: i,
        // If no value is set on the item, add one from its label, or from its index.
        // The result is store in a _value attribute.
        _value: t[this.itemValueKey] === void 0 ? t[this.itemLabelKey] || i : t[this.itemValueKey],
        _selected: t._selected || false,
        _label: t[this.itemLabelKey] || "",
        _focused: false
      }));
    },
    applySelectionOnItems(e) {
      this.isMultipleSelect || this.listItems.forEach((i) => i._selected = false);
      const t = this.checkSelection(e);
      this.listItems.forEach((i) => {
        i._selected = t.find((s) => i._value === s) !== void 0;
      });
    }
  },
  created() {
    this.refreshListItems(), this.applySelectionOnItems(this.modelValue);
  },
  watch: {
    items() {
      this.refreshListItems(), this.applySelectionOnItems(this.modelValue);
    },
    modelValue(e) {
      this.applySelectionOnItems(e);
    },
    multiple(e) {
      if (!e) {
        let t = null;
        this.listItems.forEach((i) => {
          i._selected && !t ? t = i : i._selected && (i._selected = false);
        }), this.emitSelection();
      }
    }
  }
};
var tl = v(el, [["render", Zi]]);
var Be = {
  props: {
    // Position.
    appendTo: { type: [String, Boolean, Object] },
    fixed: { type: Boolean },
    top: { type: Boolean },
    bottom: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    alignTop: { type: Boolean },
    alignBottom: { type: Boolean },
    alignLeft: { type: Boolean },
    alignRight: { type: Boolean },
    noPosition: { type: Boolean },
    zIndex: { type: [Number, String, Boolean] },
    // Optionally designate an external activator.
    // The activator can be a DOM string selector, a ref or a DOM node.
    activator: { type: [String, Object] }
  },
  inject: {
    detachableDefaultRoot: { default: null }
  },
  data: () => ({
    // The event listeners handlers have to be removed the exact same way they have been attached.
    // Since the handler functions have variables that change after hot-reload, keep them exactly
    // as is in an array so we can delete them on destroy.
    // This only applies to the activatorEventHandlers, the other events listeners can be removed
    // normally.
    docEventListenersHandlers: [],
    // The user may open and close the detachable so fast (like when toggling on hover) that it
    // should not show up at all. Keep the ability to cancel the opening timer (if there is a set
    // delay prop).
    openTimeout: null
  }),
  computed: {
    // DOM element to attach tooltip/menu to.
    // ! \ This computed uses the DOM - NO SSR (only trigger from beforeMount and later).
    appendToTarget() {
      let e = ".w-app";
      typeof this.detachableDefaultRoot == "function" && (e = this.detachableDefaultRoot() || e);
      let t = this.appendTo || e;
      return t === true ? t = e : this.appendTo === "activator" ? t = this.$el.previousElementSibling : t && !["object", "string"].includes(typeof t) ? t = e : typeof t == "object" && !t.nodeType && (t = e, j(`Invalid node provided in ${this.$options.name} \`append-to\`. Falling back to .w-app.`)), typeof t == "string" && (t = document.querySelector(t)), t || (j(`Unable to locate ${this.appendTo ? `target ${this.appendTo}` : e}`), t = document.querySelector(e)), t;
    },
    // DOM element that will receive the tooltip/menu.
    // ! \ This computed uses the DOM - NO SSR (only trigger from beforeMount and later).
    detachableParentEl() {
      return this.appendToTarget;
    },
    hasSeparateActivator() {
      var i;
      if (this.$slots.activator) return false;
      const e = typeof this.activator == "string", t = (((i = this.activator) == null ? void 0 : i.$el) || this.activator) instanceof HTMLElement;
      return e || t;
    },
    activatorEl: {
      get() {
        var e;
        if (this.hasSeparateActivator) {
          const t = ((e = this.activator) == null ? void 0 : e.$el) || this.activator;
          return t instanceof HTMLElement ? t : document.querySelector(this.activator);
        }
        return this.$el.nextElementSibling;
      },
      set() {
      }
    },
    position() {
      return this.top && "top" || this.bottom && "bottom" || this.left && "left" || this.right && "right" || "bottom";
    },
    alignment() {
      return ["top", "bottom"].includes(this.position) && this.alignLeft && "left" || ["top", "bottom"].includes(this.position) && this.alignRight && "right" || ["left", "right"].includes(this.position) && this.alignTop && "top" || ["left", "right"].includes(this.position) && this.alignBottom && "bottom" || "";
    },
    shouldShowOnClick() {
      return this.$options.props.showOnHover && !this.showOnHover || this.$options.props.showOnClick && this.showOnClick;
    }
  },
  methods: {
    // ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
    async open(e) {
      this.delay && await new Promise((t) => this.openTimeout = setTimeout(t, this.delay)), !(this.delay && !this.openTimeout) && (this.detachableVisible = true, this.activator && (this.activatorEl = e.target), await this.insertInDOM(), this.minWidth === "activator" && (this.activatorWidth = this.activatorEl.offsetWidth), this.noPosition || this.computeDetachableCoords(), this.timeoutId = setTimeout(() => {
        this.$emit("update:modelValue", true), this.$emit("input", true), this.$emit("open");
      }, 0), this.persistent || document.addEventListener("mousedown", this.onOutsideMousedown), this.noPosition || window.addEventListener("resize", this.onResize));
    },
    // ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
    getActivatorCoordinates() {
      const { top: e, left: t, width: i, height: s } = this.activatorEl.getBoundingClientRect();
      let n = { top: e, left: t, width: i, height: s };
      if (!this.fixed) {
        const { top: l, left: a } = this.detachableParentEl.getBoundingClientRect(), r = window.getComputedStyle(this.detachableParentEl, null);
        n = {
          ...n,
          top: e - l + this.detachableParentEl.scrollTop - parseInt(r.getPropertyValue("border-top-width")),
          left: t - a + this.detachableParentEl.scrollLeft - parseInt(r.getPropertyValue("border-left-width"))
        };
      }
      return n;
    },
    // ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
    computeDetachableCoords() {
      let { top: e, left: t, width: i, height: s } = this.getActivatorCoordinates();
      if (!this.detachableEl) return;
      this.detachableEl.style.visibility = "hidden", this.detachableEl.style.display = "flex";
      const n = window.getComputedStyle(this.detachableEl, null);
      switch (this.position) {
        case "top": {
          e -= this.detachableEl.offsetHeight, this.alignRight ? t += i - this.detachableEl.offsetWidth + parseInt(n.getPropertyValue("border-right-width")) : this.alignLeft || (t += (i - this.detachableEl.offsetWidth) / 2);
          break;
        }
        case "bottom": {
          e += s, this.alignRight ? t += i - this.detachableEl.offsetWidth + parseInt(n.getPropertyValue("border-right-width")) : this.alignLeft || (t += (i - this.detachableEl.offsetWidth) / 2);
          break;
        }
        case "left": {
          t -= this.detachableEl.offsetWidth, this.alignBottom ? e += s - this.detachableEl.offsetHeight : this.alignTop || (e += (s - this.detachableEl.offsetHeight) / 2);
          break;
        }
        case "right": {
          t += i, this.alignBottom ? e += s - this.detachableEl.offsetHeight + parseInt(n.getPropertyValue("margin-top")) : this.alignTop || (e += (s - this.detachableEl.offsetHeight) / 2 + // top: 50% of activator - half menu height.
          parseInt(n.getPropertyValue("margin-top")));
          break;
        }
      }
      this.detachableEl.style.visibility = null, this.detachableVisible || (this.detachableEl.style.display = "none"), this.detachableCoords = { top: e, left: t };
    },
    onResize() {
      this.minWidth === "activator" && (this.activatorWidth = this.activatorEl.offsetWidth), this.computeDetachableCoords();
    },
    // ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
    onOutsideMousedown(e) {
      !this.detachableEl.contains(e.target) && !this.activatorEl.contains(e.target) && (this.$emit("update:modelValue", this.detachableVisible = false), this.$emit("input", false), this.$emit("close"), document.removeEventListener("mousedown", this.onOutsideMousedown), window.removeEventListener("resize", this.onResize));
    },
    insertInDOM() {
      return new Promise((e) => {
        this.$nextTick(() => {
          var t;
          this.detachableEl = ((t = this.$refs.detachable) == null ? void 0 : t.$el) || this.$refs.detachable, this.detachableEl && this.appendToTarget.appendChild(this.detachableEl), e();
        });
      });
    },
    removeFromDOM() {
      document.removeEventListener("mousedown", this.onOutsideMousedown), window.removeEventListener("resize", this.onResize), this.detachableEl && this.detachableEl.parentNode && (this.detachableVisible = false, this.detachableEl.remove(), this.detachableEl = null);
    },
    // If the activator is external, add event listeners to the document and check the target is
    // the activator when toggling.
    // This way, the activator can be a future DOM element, that is not yet in the DOM.
    bindActivatorEvents() {
      const e = typeof this.activator == "string";
      Object.entries(this.activatorEventHandlers).forEach(([t, i]) => {
        t = t.replace("mouseenter", "mouseover").replace("mouseleave", "mouseout");
        const s = (n) => {
          var l;
          (e && ((l = n.target) != null && l.matches) && n.target.matches(this.activator) || n.target === this.activatorEl || this.activatorEl.contains(n.target)) && i(n);
        };
        document.addEventListener(t, s), this.docEventListenersHandlers.push({ eventName: t, handler: s });
      });
    }
  },
  mounted() {
    var e;
    this.activator ? this.bindActivatorEvents() : this.$nextTick(() => {
      this.activator && this.bindActivatorEvents(), this.modelValue && this.open({ target: this.activatorEl });
    }), this.overlay && (this.overlayEl = (e = this.$refs.overlay) == null ? void 0 : e.$el), this.modelValue && this.activator ? this.toggle({ type: this.shouldShowOnClick ? "click" : "mouseenter", target: this.activatorEl }) : this.modelValue && this.open({ target: this.activatorEl });
  },
  unmounted() {
    this.close(), this.removeFromDOM(), this.docEventListenersHandlers.length && this.docEventListenersHandlers.forEach(({ eventName: e, handler: t }) => {
      document.removeEventListener(e, t);
    });
  },
  watch: {
    modelValue(e) {
      !!e !== this.detachableVisible && (e ? this.open({ target: this.activatorEl }) : this.close());
    },
    appendTo() {
      this.removeFromDOM(), this.insertInDOM();
    }
  }
};
function il(e, t, i, s, n, l) {
  const a = resolveComponent("w-card"), r = resolveComponent("w-overlay");
  return openBlock(), createElementBlock(Fragment, null, [
    renderSlot(e.$slots, "activator", { on: l.activatorEventHandlers }),
    createVNode(Transition, {
      name: l.transitionName,
      appear: ""
    }, {
      default: withCtx(() => [
        i.custom && e.detachableVisible ? (openBlock(), createElementBlock("div", mergeProps({
          key: 0,
          class: "w-menu",
          ref: "detachable"
        }, e.$attrs, {
          onClick: t[0] || (t[0] = (d) => i.hideOnMenuClick && l.close(true)),
          onMouseenter: t[1] || (t[1] = (d) => i.showOnHover && (e.hoveringMenu = true)),
          onMouseleave: t[2] || (t[2] = (d) => i.showOnHover && (e.hoveringMenu = false, l.close())),
          class: l.classes,
          style: l.styles
        }), [
          renderSlot(e.$slots, "default")
        ], 16)) : e.detachableVisible ? (openBlock(), createBlock(a, mergeProps({
          key: 1,
          class: "w-menu",
          ref: "detachable"
        }, e.$attrs, {
          onClick: t[3] || (t[3] = (d) => i.hideOnMenuClick && l.close(true)),
          onMouseenter: t[4] || (t[4] = (d) => i.showOnHover && (e.hoveringMenu = true)),
          onMouseleave: t[5] || (t[5] = (d) => i.showOnHover && (e.hoveringMenu = false, l.close())),
          tile: i.tile,
          "title-class": l.titleClasses,
          "content-class": l.contentClasses,
          shadow: i.shadow,
          "no-border": i.noBorder,
          class: l.classes,
          style: l.styles
        }), createSlots({
          default: withCtx(() => [
            renderSlot(e.$slots, "default")
          ]),
          _: 2
        }, [
          e.$slots.title ? {
            name: "title",
            fn: withCtx(() => [
              renderSlot(e.$slots, "title")
            ]),
            key: "0"
          } : void 0,
          e.$slots.actions ? {
            name: "actions",
            fn: withCtx(() => [
              renderSlot(e.$slots, "actions")
            ]),
            key: "1"
          } : void 0
        ]), 1040, ["tile", "title-class", "content-class", "shadow", "no-border", "class", "style"])) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["name"]),
    i.overlay ? (openBlock(), createBlock(r, mergeProps({
      key: 0,
      ref: "overlay",
      "model-value": e.detachableVisible,
      persistent: i.persistent,
      class: l.overlayClasses
    }, i.overlayProps, {
      "z-index": (e.zIndex || 200) - 1,
      "onUpdate:modelValue": t[6] || (t[6] = (d) => e.detachableVisible = false)
    }), null, 16, ["model-value", "persistent", "class", "z-index"])) : createCommentVNode("", true)
  ], 64);
}
var ll = {
  name: "w-menu",
  mixins: [Be],
  inheritAttrs: false,
  // The attrs are only bound to the button-partial, not the root.
  props: {
    modelValue: {},
    // Show or hide.
    showOnHover: { type: Boolean },
    hideOnMenuClick: { type: Boolean },
    color: { type: String },
    bgColor: { type: String },
    shadow: { type: Boolean },
    custom: { type: Boolean },
    // Include a w-card or not. It does by default.
    tile: { type: Boolean },
    round: { type: Boolean },
    noBorder: { type: Boolean },
    transition: { type: String },
    menuClass: { type: [String, Object, Array] },
    titleClass: { type: [String, Object, Array] },
    contentClass: { type: [String, Object, Array] },
    arrow: { type: Boolean },
    // The small triangle pointing toward the activator.
    minWidth: { type: [Number, String] },
    // can be like: `40`, `5em`, `activator`.
    maxWidth: { type: [Number, String] },
    // can be like: `40`, `5em`, `activator`.
    overlay: { type: Boolean },
    overlayClass: { type: [String, Object, Array] },
    overlayProps: { type: Object },
    // Allow passing down an object of props to the w-overlay component.
    persistent: { type: Boolean },
    delay: { type: Number },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Other props in the detachable mixin:
    // detachTo, appendTo, fixed, top, bottom, left, right, alignTop, alignBottom, alignLeft,
    // alignRight, noPosition, zIndex, activator.
  },
  provide() {
    return {
      // If a detachable is used inside a w-menu without an appendTo, default to the menu element
      // instead of the w-app.
      detachableDefaultRoot: () => {
        var e;
        return ((e = this.$refs.detachable) == null ? void 0 : e.$el) || this.$refs.detachable || null;
      }
    };
  },
  emits: ["input", "update:modelValue", "open", "close"],
  data: () => ({
    detachableVisible: false,
    hoveringActivator: false,
    hoveringMenu: false,
    // The menu computed top & left coordinates.
    detachableCoords: {
      top: 0,
      left: 0
    },
    activatorWidth: 0,
    detachableEl: null,
    timeoutId: null
  }),
  computed: {
    /**
     * Other computed in the detachable mixin:
     * - `appendToTarget`
     * - `detachableParentEl`
     * - `activatorEl`
     * - `position`
     * - `alignment`
     **/
    transitionName() {
      return this.transition || "scale-fade";
    },
    menuMinWidth() {
      return this.minWidth === "activator" ? this.activatorWidth ? `${this.activatorWidth}px` : 0 : isNaN(this.minWidth) ? this.minWidth : this.minWidth ? `${this.minWidth}px` : 0;
    },
    menuMaxWidth() {
      return this.maxWidth === "activator" ? this.activatorWidth ? `${this.activatorWidth}px` : 0 : isNaN(this.maxWidth) ? this.maxWidth : this.maxWidth ? `${this.maxWidth}px` : 0;
    },
    menuClasses() {
      return M(this.menuClass);
    },
    titleClasses() {
      return M(this.titleClass);
    },
    contentClasses() {
      return M(this.contentClass);
    },
    overlayClasses() {
      return {
        ...M(this.overlayClass),
        "w-overlay--no-pointer-event": this.showOnHover
      };
    },
    classes() {
      return {
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        ...this.menuClasses,
        [`w-menu--${this.position}`]: !this.noPosition,
        [`w-menu--align-${this.alignment}`]: !this.noPosition && this.alignment,
        "w-menu--tile": this.tile,
        "w-menu--card": !this.custom,
        "w-menu--round": this.round,
        "w-menu--arrow": this.arrow,
        "w-menu--shadow": this.shadow,
        "w-menu--fixed": this.fixed,
        "w-menu--dark": this.dark,
        "w-menu--light": this.light
      };
    },
    // The floating menu styles.
    styles() {
      return {
        zIndex: this.zIndex || this.zIndex === 0 || this.overlay && !this.zIndex && 200 || null,
        top: this.detachableCoords.top && `${~~this.detachableCoords.top}px` || null,
        left: this.detachableCoords.left && `${~~this.detachableCoords.left}px` || null,
        minWidth: this.minWidth && this.menuMinWidth || null,
        maxWidth: this.maxWidth && this.menuMaxWidth || null,
        "--w-menu-bg-color": this.arrow && (this.$waveui.colors[this.bgColor] || "rgb(var(--w-base-bg-color-rgb))")
      };
    },
    activatorEventHandlers() {
      let e = {};
      return this.showOnHover ? (e = {
        focus: this.toggle,
        blur: this.toggle,
        mouseenter: (t) => {
          this.hoveringActivator = true, this.open(t);
        },
        mouseleave: (t) => {
          this.hoveringActivator = false, setTimeout(() => {
            this.hoveringMenu || this.close();
          }, 10);
        }
      }, typeof window < "u" && "ontouchstart" in window && (e.click = this.toggle)) : e = { click: this.toggle }, e;
    }
  },
  methods: {
    /**
     * Other methods in the `detachable` mixin:
     * - `open`
     * - `getActivatorCoordinates`
     * - `computeDetachableCoords`
     * - `onResize`
     * - `onOutsideMousedown`
     * - `insertInDOM`
     * - `removeFromDOM`
     **/
    // ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
    toggle(e) {
      let t = this.detachableVisible;
      typeof window < "u" && "ontouchstart" in window && this.showOnHover && e.type === "click" || e.type === "click" && !this.showOnHover ? t = !t : e.type === "mouseenter" && this.showOnHover ? (this.hoveringActivator = true, t = true) : e.type === "mouseleave" && this.showOnHover && (this.hoveringActivator = false, t = false), this.timeoutId = clearTimeout(this.timeoutId), t ? this.open(e) : this.close();
    },
    /**
     * Closes the menu. Can happen on:
     * - click of activator
     * - hover outside if showOnHover
     * - click inside menu if hideOnMenuClick.
     * / ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
     *
     * @param {Boolean} force when showOnHover is set to true, hovering menu should keep it open.
     *                        But if hideOnMenuClick is also set to true, this should force close
     *                        even while hovering the menu.
     */
    async close(e = false) {
      this.openTimeout = clearTimeout(this.openTimeout), this.detachableVisible && (this.showOnHover && !e && (await new Promise((t) => setTimeout(t, 10)), this.showOnHover && (this.hoveringMenu || this.hoveringActivator)) || (this.$emit("update:modelValue", this.detachableVisible = false), this.$emit("input", false), this.$emit("close"), document.removeEventListener("mousedown", this.onOutsideMousedown), window.removeEventListener("resize", this.onResize)));
    }
  }
  // watch, mounted & beforeDestroy hooks are set in the detachable.js mixin.
};
var sl = v(ll, [["render", il]]);
function ol(e, t, i, s, n, l) {
  const a = resolveComponent("w-alert");
  return openBlock(), createBlock(Transition, {
    name: l.transitionName,
    appear: ""
  }, {
    default: withCtx(() => [
      n.show ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["w-notification", l.classes]),
        style: normalizeStyle(l.styles)
      }, [
        createVNode(a, mergeProps(l.alertProps, {
          class: l.alertClasses,
          "onUpdate:modelValue": t[0] || (t[0] = (r) => {
            e.$emit("update:modelValue", false), e.$emit("input", false);
          })
        }), {
          default: withCtx(() => [
            renderSlot(e.$slots, "default")
          ]),
          _: 3
        }, 16, ["class"])
      ], 6)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 8, ["name"]);
}
var al = {
  name: "w-notification",
  props: {
    // Notification props.
    modelValue: { default: true },
    // Show or hide.
    transition: { type: [String, Boolean], default: "" },
    timeout: { type: [Number, String], default: 0 },
    absolute: { type: Boolean },
    top: { type: Boolean },
    bottom: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    zIndex: { type: [Number, String, Boolean] },
    // Alert props.
    success: { type: Boolean },
    info: { type: Boolean },
    warning: { type: Boolean },
    error: { type: Boolean },
    color: { type: String },
    bgColor: { type: String },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    round: { type: Boolean },
    plain: { type: Boolean },
    noBorder: { type: Boolean },
    borderLeft: { type: Boolean },
    borderRight: { type: Boolean },
    borderTop: { type: Boolean },
    borderBottom: { type: Boolean },
    outline: { type: Boolean },
    dismiss: { type: Boolean },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: ["input", "update:modelValue", "close"],
  data() {
    return {
      show: this.modelValue,
      timeoutId: null
    };
  },
  computed: {
    transitionName() {
      return this.transition === false ? "" : this.transition ? this.transition : `slide-${{ top: "down", bottom: "up", left: "right", right: "left" }[this.position[this.position[1] === "center" ? 0 : 1]]}`;
    },
    position() {
      let e = [];
      return !this.top && !this.bottom && !this.left && !this.right ? e = ["top", "right"] : e = [
        this.top && "top" || this.bottom && "bottom" || "top",
        this.left && "left" || this.right && "right" || "center"
      ], e;
    },
    hasType() {
      return !!(this.success || this.info || this.warning || this.error);
    },
    alertProps() {
      return {
        modelValue: this.show,
        success: this.success,
        info: this.info,
        warning: this.warning,
        error: this.error,
        color: this.color,
        bgColor: this.bgColor || !this.hasType && "white" || "",
        shadow: this.shadow,
        tile: this.tile,
        round: this.round,
        plain: this.plain,
        noBorder: this.noBorder,
        borderLeft: this.borderLeft,
        borderRight: this.borderRight,
        borderTop: this.borderTop,
        borderBottom: this.borderBottom,
        outline: this.outline,
        dismiss: this.dismiss,
        xs: this.xs,
        sm: this.sm,
        md: this.md,
        lg: this.lg,
        xl: this.xl
      };
    },
    classes() {
      return {
        "w-notification--dark": this.dark,
        "w-notification--light": this.light,
        "w-notification--absolute": this.absolute,
        [`w-notification--${this.position.join(" w-notification--")}`]: true
      };
    },
    alertClasses() {
      return this.bgColor || (this.success || this.info || this.warning || this.error) && this.plain ? null : "white--bg";
    },
    styles() {
      return {
        zIndex: this.zIndex || this.zIndex === 0 || null
      };
    },
    timeoutVal() {
      return parseInt(this.timeout);
    }
  },
  methods: {
    countdown() {
      this.timeoutId = setTimeout(() => {
        this.$emit("update:modelValue", this.show = false), this.$emit("input", false), this.$emit("close");
      }, this.timeoutVal);
    }
  },
  created() {
    this.modelValue && this.timeoutVal && this.countdown();
  },
  watch: {
    modelValue(e) {
      clearTimeout(this.timeoutId), this.show = e, e && this.timeoutVal && this.countdown();
    }
  }
};
var nl = v(al, [["render", ol]]);
function rl(e, t, i, s, n, l) {
  const a = resolveDirective("focus");
  return openBlock(), createBlock(Transition, {
    name: "fade",
    appear: "",
    onAfterLeave: l.onClose
  }, {
    default: withCtx(() => [
      i.modelValue ? withDirectives((openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["w-overlay", l.classes]),
        ref: "overlay",
        style: normalizeStyle(i.modelValue && l.styles || null),
        onKeydown: t[0] || (t[0] = withKeys(withModifiers((...r) => l.onClick && l.onClick(...r), ["stop"]), ["escape"])),
        onClick: t[1] || (t[1] = (...r) => l.onClick && l.onClick(...r)),
        tabindex: "0"
      }, [
        renderSlot(e.$slots, "default")
      ], 38)), [
        [vShow, e.showOverlay],
        [a]
      ]) : createCommentVNode("", true)
    ]),
    _: 3
  }, 8, ["onAfterLeave"]);
}
var dl = {
  name: "w-overlay",
  props: {
    modelValue: {},
    opacity: { type: [Number, String, Boolean] },
    bgColor: { type: String },
    absolute: { type: Boolean },
    zIndex: { type: [Number, String, Boolean] },
    persistent: { type: Boolean },
    persistentNoAnimation: { type: Boolean }
  },
  provide() {
    return {
      // If a detachable is used inside a w-overlay without an appendTo, default to the overlay element
      // instead of the w-app.
      detachableDefaultRoot: () => this.$refs.overlay || null
    };
  },
  emits: ["input", "update:modelValue", "click", "before-close", "close"],
  data: () => ({
    persistentAnimate: false,
    showOverlay: false
  }),
  computed: {
    backgroundColor() {
      return this.bgColor || this.opacity && `rgba(0, 0, 0, ${this.opacity})` || false;
    },
    classes() {
      return {
        "w-overlay--persistent-animate": this.persistentAnimate,
        "w-overlay--absolute": this.absolute
      };
    },
    styles() {
      return {
        backgroundColor: this.backgroundColor,
        zIndex: this.zIndex || this.zIndex === 0 ? this.zIndex : false
      };
    }
  },
  methods: {
    onClick(e) {
      e.target.classList.contains("w-overlay") && (this.persistent && !this.persistentNoAnimation ? (this.persistentAnimate = true, setTimeout(() => this.persistentAnimate = false, 150)) : this.persistent || (this.showOverlay = false, this.$emit("before-close")), this.$emit("click", e));
    },
    // Wait until the end of the closing transition (v-show) to completely unmount (v-if).
    // The onClose method is called twice from the transition: once for the v-show, and once for the v-if.
    onClose() {
      this.$emit("update:modelValue", false), this.$emit("input", false), this.modelValue || this.$emit("close");
    }
  },
  created() {
    this.showOverlay = this.modelValue;
  },
  watch: {
    modelValue(e) {
      e && (this.showOverlay = true);
    }
  }
};
var ul = v(dl, [["render", rl]]);
var hl = { class: "w-parallax" };
function cl(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", hl);
}
var pl = {
  name: "w-parallax",
  props: {},
  emits: [],
  data: () => ({})
};
var fl = v(pl, [["render", cl]]);
var ml = ["viewBox"];
var gl = ["cx", "cy", "r", "stroke-dasharray", "stroke-width"];
var bl = ["cx", "cy", "r", "stroke-width", "stroke-linecap", "stroke-dasharray"];
function yl(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-progress", l.classes]),
    style: normalizeStyle(l.styles)
  }, [
    i.circle ? (openBlock(), createElementBlock("svg", {
      key: 1,
      viewBox: `${l.circleCenter / 2} ${l.circleCenter / 2} ${l.circleCenter} ${l.circleCenter}`
    }, [
      i.bgColor || this.progressValue > -1 ? (openBlock(), createElementBlock("circle", {
        key: 0,
        class: normalizeClass(["bg", i.bgColor || null]),
        cx: l.circleCenter,
        cy: l.circleCenter,
        r: e.circleRadius,
        fill: "transparent",
        "stroke-dasharray": e.circleCircumference,
        "stroke-width": i.stroke
      }, null, 10, gl)) : createCommentVNode("", true),
      createBaseVNode("circle", {
        class: "w-progress__progress",
        cx: l.circleCenter,
        cy: l.circleCenter,
        r: e.circleRadius,
        fill: "transparent",
        "stroke-width": i.stroke,
        "stroke-linecap": i.roundCap && "round",
        "stroke-dasharray": e.circleCircumference,
        style: normalizeStyle(`stroke-dashoffset: ${(1 - l.progressValue / 100) * e.circleCircumference}`)
      }, null, 12, bl)
    ], 8, ml)) : (openBlock(), createElementBlock("div", {
      key: 0,
      class: normalizeClass(["w-progress__progress", { full: l.progressValue === 100 }]),
      style: normalizeStyle(`width: ${l.progressValue}%`)
    }, null, 6)),
    i.label || e.$slots.default ? (openBlock(), createElementBlock("div", {
      key: 2,
      class: normalizeClass(["w-progress__label", i.labelColor || false])
    }, [
      renderSlot(e.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(Math.round(l.progressValue)) + toDisplayString(i.circle ? "" : "%"), 1)
      ])
    ], 2)) : createCommentVNode("", true)
  ], 6);
}
var Q = 40;
var wl = Q / 2;
var kl = Math.round(Q * 3.14 * 100) / 100;
var vl = {
  name: "w-progress",
  props: {
    modelValue: { type: [Number, String, Boolean], default: -1 },
    label: { type: Boolean },
    roundCap: { type: Boolean },
    color: { type: String, default: "primary" },
    bgColor: { type: String },
    labelColor: { type: String },
    size: { type: [Number, String] },
    // Circular progress thickness.
    circle: { type: Boolean },
    stroke: { type: [Number, String], default: 4 },
    // For linear progress.
    shadow: { type: Boolean },
    tile: { type: Boolean },
    round: { type: Boolean },
    outline: { type: Boolean },
    stripes: { type: Boolean },
    absolute: { type: Boolean },
    fixed: { type: Boolean },
    top: { type: Boolean },
    bottom: { type: Boolean },
    zIndex: { type: [Number, String, Boolean] }
  },
  emits: [],
  data: () => ({
    circleSize: Q,
    circleRadius: wl,
    circleCircumference: kl
  }),
  computed: {
    progressValue() {
      return parseFloat(this.modelValue);
    },
    circleCenter() {
      return Q + this.stroke;
    },
    forcedSize() {
      return this.size && (isNaN(this.size) ? this.size : `${this.size}px`);
    },
    // If linear, with position fixed or absolute.
    position() {
      return this.top && "top" || this.bottom && "bottom" || "top";
    },
    classes() {
      return {
        [`w-progress--${this.circle ? "circular" : "linear"}`]: true,
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor && !this.circle,
        [`w-progress--${this.position}`]: !this.circle && (this.absolute || this.fixed),
        "w-progress--default-bg": !this.bgColor,
        "w-progress--indeterminate": this.modelValue === -1,
        "w-progress--outline": !this.circle && this.outline,
        "w-progress--tile": !this.circle && this.tile,
        "w-progress--stripes": !this.circle && this.stripes,
        "w-progress--round": !this.circle && this.round,
        "w-progress--shadow": this.shadow,
        "w-progress--absolute": !this.circle && this.absolute,
        "w-progress--fixed": !this.circle && !this.absolute && this.fixed,
        [`w-progress--${this.roundCap ? "round" : "flat"}-cap`]: true
      };
    },
    styles() {
      return {
        [this.circle ? "width" : "height"]: this.forcedSize || null
      };
    }
  }
};
var Cl = v(vl, [["render", yl]]);
var Sl = ["id", "name", "checked", "disabled", "required", "tabindex", "aria-checked"];
var Bl = ["for"];
var _l = ["for", "innerHTML"];
var $l = ["for"];
var Il = ["for", "innerHTML"];
function Vl(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister && !l.wRadios ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: e.inputValue, disabled: e.isDisabled, readonly: e.isReadonly }, {
    valid: e.valid,
    "onUpdate:valid": t[3] || (t[3] = (a) => e.valid = a),
    onReset: t[4] || (t[4] = (a) => {
      e.$emit("update:modelValue", e.inputValue = null), e.$emit("input", null);
    }),
    class: l.classes
  }), {
    default: withCtx(() => [
      createBaseVNode("input", {
        ref: "input",
        id: `w-radio--${e._.uid}`,
        type: "radio",
        name: e.inputName,
        checked: e.inputValue || null,
        disabled: e.isDisabled || e.isReadonly || null,
        required: e.required || null,
        tabindex: e.tabindex || null,
        onFocus: t[0] || (t[0] = (a) => e.$emit("focus", a)),
        onChange: t[1] || (t[1] = (a) => l.onInput(a)),
        "aria-checked": e.inputValue || "false",
        role: "radio"
      }, null, 40, Sl),
      l.hasLabel && i.labelOnLeft ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        e.$slots.default ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-radio__label w-form-el-shakable pr2", e.labelClasses]),
          for: `w-radio--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, Bl)) : i.label ? (openBlock(), createElementBlock("label", {
          key: 1,
          class: normalizeClass(["w-radio__label w-form-el-shakable pr2", e.labelClasses]),
          for: `w-radio--${e._.uid}`,
          innerHTML: i.label
        }, null, 10, _l)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true),
      createBaseVNode("div", {
        class: normalizeClass(["w-radio__input", this.color]),
        onClick: t[2] || (t[2] = (a) => {
          e.$refs.input.focus(), e.$refs.input.click();
        })
      }, null, 2),
      l.hasLabel && !i.labelOnLeft ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        e.$slots.default ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-radio__label w-form-el-shakable pl2", e.labelClasses]),
          for: `w-radio--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, $l)) : i.label ? (openBlock(), createElementBlock("label", {
          key: 1,
          class: normalizeClass(["w-radio__label w-form-el-shakable pl2", e.labelClasses]),
          for: `w-radio--${e._.uid}`,
          innerHTML: i.label
        }, null, 10, Il)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 16, ["valid", "class"]);
}
var Tl = {
  name: "w-radio",
  mixins: [E],
  inject: { wRadios: { default: null } },
  props: {
    modelValue: { default: false },
    // v-model to check or uncheck.
    // When `value` is taken by a v-model and multiple w-radio are plugged on
    // the same v-model, this allows returning a custom value to the v-model.
    returnValue: {},
    label: { type: String },
    labelOnLeft: { type: Boolean },
    color: { type: String, default: "primary" },
    labelColor: { type: String, default: "primary" },
    noRipple: { type: Boolean },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, tabindex, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus"],
  data: () => ({
    inputValue: false,
    ripple: {
      start: false,
      end: false,
      timeout: null
    }
  }),
  computed: {
    hasLabel() {
      return this.label || this.$slots.default;
    },
    classes() {
      return {
        [`w-radio w-radio--${this.inputValue ? "checked" : "unchecked"}`]: true,
        "w-radio--disabled": this.isDisabled,
        "w-radio--readonly": this.isReadonly,
        "w-radio--ripple": this.ripple.start,
        "w-radio--rippled": this.ripple.end,
        "w-radio--dark": this.ripple.dark,
        "w-radio--light": this.ripple.light
      };
    }
  },
  methods: {
    toggleFromOutside() {
      this.inputValue = this.returnValue !== void 0 ? this.returnValue === this.modelValue : this.modelValue;
    },
    onInput(e) {
      this.inputValue = e.target.checked;
      const t = this.inputValue && this.returnValue !== void 0 ? this.returnValue : this.inputValue;
      this.$emit("update:modelValue", t), this.$emit("input", t), this.noRipple || (this.inputValue ? (this.ripple.start = true, this.ripple.timeout = setTimeout(() => {
        this.ripple.start = false, this.ripple.end = true, setTimeout(() => this.ripple.end = false, 100);
      }, 700)) : (this.ripple.start = false, clearTimeout(this.ripple.timeout)));
    }
  },
  created() {
    this.modelValue !== void 0 && this.toggleFromOutside();
  },
  watch: {
    modelValue() {
      this.toggleFromOutside();
    }
  }
};
var Ll = v(Tl, [["render", Vl]]);
var Rl = ["innerHTML"];
function xl(e, t, i, s, n, l) {
  const a = resolveComponent("w-radio");
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: e.inputValue, disabled: e.isDisabled }, {
    valid: e.valid,
    "onUpdate:valid": t[1] || (t[1] = (r) => e.valid = r),
    onReset: t[2] || (t[2] = (r) => {
      e.$emit("update:modelValue", e.inputValue = null), e.$emit("input", null);
    }),
    column: !i.inline,
    wrap: i.inline,
    class: l.classes
  }), {
    default: withCtx(() => [
      (openBlock(true), createElementBlock(Fragment, null, renderList(l.radioItems, (r, d) => (openBlock(), createBlock(a, mergeProps({
        key: d,
        "model-value": r.value === i.modelValue,
        "onUpdate:modelValue": (h) => l.onInput(r),
        onFocus: t[0] || (t[0] = (h) => e.$emit("focus", h)),
        name: e.inputName,
        ref_for: true
      }, { label: r.label, color: r.color, labelOnLeft: i.labelOnLeft, labelColor: i.labelColor }, {
        disabled: e.isDisabled || null,
        readonly: e.isReadonly || null,
        class: { mt1: !i.inline && d }
      }), {
        default: withCtx(() => [
          e.$slots[`item.${d + 1}`] || e.$slots.item ? renderSlot(e.$slots, e.$slots[`item.${d + 1}`] ? `item.${d + 1}` : "item", {
            key: 0,
            item: l.getOriginalItem(r),
            index: d + 1,
            checked: r.value === i.modelValue,
            innerHTML: r.label
          }) : r.label ? (openBlock(), createElementBlock("div", {
            key: 1,
            innerHTML: r.label
          }, null, 8, Rl)) : createCommentVNode("", true)
        ]),
        _: 2
      }, 1040, ["model-value", "onUpdate:modelValue", "name", "disabled", "readonly", "class"]))), 128))
    ]),
    _: 3
  }, 16, ["valid", "column", "wrap", "class"]);
}
var Pl = {
  name: "w-radios",
  mixins: [E],
  props: {
    items: { type: Array, required: true },
    // All the possible options.
    modelValue: { type: [String, Number, Boolean] },
    // v-model on selected option.
    labelOnLeft: { type: Boolean },
    itemLabelKey: { type: String, default: "label" },
    itemValueKey: { type: String, default: "value" },
    itemColorKey: { type: String, default: "color" },
    // Support a different color per item.
    inline: { type: Boolean },
    color: { type: String, default: "primary" },
    labelColor: { type: String, default: "primary" }
    // Props from mixin: name, disabled, readonly, required, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus"],
  provide() {
    return { wRadios: true };
  },
  data: () => ({
    inputValue: null
  }),
  computed: {
    radioItems() {
      return (this.items || []).map((e, t) => ({
        ...e,
        _index: t,
        label: e[this.itemLabelKey],
        // If no value is set then add one to prevent error.
        value: e[this.itemValueKey] === void 0 ? e[this.itemLabelKey] || t : e[this.itemValueKey],
        color: e[this.itemColorKey] || this.color
      }));
    },
    classes() {
      return [
        "w-radios",
        `w-radios--${this.inline ? "inline" : "column"}`
      ];
    }
  },
  methods: {
    onInput(e) {
      this.inputValue = true, this.$emit("update:modelValue", e.value), this.$emit("input", e.value);
    },
    // Return the original item (so there is no `_index`).
    getOriginalItem(e) {
      return this.items[e._index];
    }
  }
};
var Ol = v(Pl, [["render", xl]]);
var zl = ["id", "name", "value"];
var Ml = ["disabled", "onMouseenter", "onClick", "tabindex"];
function El(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: n.rating, disabled: e.isDisabled, readonly: e.isReadonly }, {
    valid: e.valid,
    "onUpdate:valid": t[4] || (t[4] = (a) => e.valid = a),
    onReset: t[5] || (t[5] = (a) => {
      e.$emit("update:modelValue", n.rating = null), e.$emit("input", null);
    }),
    class: l.classes
  }), {
    default: withCtx(() => [
      createBaseVNode("input", {
        id: e.inputName,
        name: e.inputName,
        type: "hidden",
        value: n.rating
      }, null, 8, zl),
      (openBlock(true), createElementBlock(Fragment, null, renderList(i.max, (a) => (openBlock(), createElementBlock(Fragment, { key: a }, [
        e.$slots.item ? renderSlot(e.$slots, "item", {
          key: 0,
          index: a + 1
        }) : createCommentVNode("", true),
        createBaseVNode("button", {
          class: normalizeClass(["w-rating__button", l.buttonClasses(a)]),
          disabled: e.isDisabled || e.isReadonly,
          onMouseenter: (r) => n.hover = a,
          onMouseleave: t[0] || (t[0] = (r) => n.hover = 0),
          onClick: (r) => l.onButtonClick(a),
          onFocus: t[1] || (t[1] = (...r) => l.onFocus && l.onFocus(...r)),
          onBlur: t[2] || (t[2] = (...r) => l.onBlur && l.onBlur(...r)),
          onKeydown: t[3] || (t[3] = (...r) => l.onKeydown && l.onKeydown(...r)),
          type: "button",
          tabindex: a === 1 ? 0 : -1
        }, [
          a - 1 === ~~n.rating && n.rating - ~~n.rating ? (openBlock(), createElementBlock("i", {
            key: 0,
            class: normalizeClass(["w-icon", `${i.icon} ${i.color}`]),
            role: "icon",
            "aria-hidden": "true",
            style: normalizeStyle(l.halfStarStyle)
          }, null, 6)) : createCommentVNode("", true)
        ], 42, Ml)
      ], 64))), 128))
    ]),
    _: 3
  }, 16, ["valid", "class"]);
}
var Al = {
  name: "w-rating",
  mixins: [E],
  props: {
    modelValue: {},
    max: { type: [Number, String], default: 5 },
    color: { type: String, default: "primary" },
    bgColor: { type: String },
    icon: { type: String, default: "wi-star" },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    noRipple: { type: Boolean },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus", "blur"],
  data() {
    return {
      rating: parseFloat(this.modelValue || 0),
      hover: 0,
      // The index (starts at 1) of the currently hovered button.
      hasFocus: 0,
      // The index (starts at 1) of the currently focused button.
      ripple: {
        start: false,
        end: false,
        timeout: null
      }
    };
  },
  computed: {
    size() {
      return this.xs && "xs" || this.sm && "sm" || this.lg && "lg" || this.xl && "xl" || "md";
    },
    classes() {
      return {
        "w-rating": true,
        "w-rating--dark": this.dark,
        "w-rating--light": this.light,
        "w-rating--focus": this.hasFocus,
        "w-rating--hover": this.hover,
        "w-rating--disabled": this.isDisabled,
        "w-rating--readonly": this.isReadonly,
        "w-rating--ripple": this.ripple.start,
        "w-rating--rippled": this.ripple.end
      };
    },
    halfStarStyle() {
      return {
        width: this.hover <= ~~this.rating && `${(this.rating - ~~this.rating) * 100}%`
      };
    }
  },
  methods: {
    onButtonClick(e) {
      this.rating = e, this.$emit("update:modelValue", this.rating), this.$emit("input", this.rating), this.noRipple || (this.ripple.start = true, this.ripple.timeout = setTimeout(() => {
        this.ripple.start = false, this.ripple.end = true, setTimeout(() => this.ripple.end = false, 100);
      }, 700));
    },
    onFocus(e) {
      this.hasFocus = true, this.$emit("focus", e);
    },
    onBlur(e) {
      this.hasFocus = false, this.$emit("blur", e);
    },
    onKeydown(e) {
      if ([37, 38, 39, 40].includes(e.keyCode)) {
        [39, 40].includes(e.keyCode) ? this.rating <= this.max - 1 && this.rating++ : this.rating > 1 && this.rating--;
        const t = this.$el.querySelectorAll("button")[this.rating - 1];
        t && (t.focus(), t.click()), e.preventDefault();
      }
    },
    buttonClasses(e) {
      const t = e - 1 === ~~this.rating && this.rating - ~~this.rating, i = this.hover >= e || !t && this.hover === 0 && this.rating >= e;
      return {
        "w-rating__button--on": i,
        "w-rating__button--half": t,
        [this.icon]: true,
        [`size--${this.size}`]: true,
        [this.color]: i,
        [this.bgColor]: this.bgColor && !i
      };
    }
  },
  watch: {
    value(e) {
      this.rating = parseFloat(e);
    }
  }
};
var Wl = v(Al, [["render", El]]);
function Kl(e, t, i, s, n, l) {
  return openBlock(), createElementBlock(Fragment, null, [
    createBaseVNode("div", mergeProps({
      class: ["w-scrollable", l.scrollableClasses],
      ref: "scrollable",
      onMouseenter: t[0] || (t[0] = (...a) => l.onMouseEnter && l.onMouseEnter(...a)),
      onMouseleave: t[1] || (t[1] = (...a) => l.onMouseLeave && l.onMouseLeave(...a)),
      onMousewheel: t[2] || (t[2] = (...a) => l.onMouseWheel && l.onMouseWheel(...a))
    }, e.$attrs, { style: l.scrollableStyles }), [
      renderSlot(e.$slots, "default")
    ], 16),
    createBaseVNode("div", {
      class: normalizeClass(["w-scrollbar", l.scrollbarClasses]),
      ref: "track",
      onMousedown: t[3] || (t[3] = (...a) => l.onTrackMouseDown && l.onTrackMouseDown(...a))
    }, [
      createBaseVNode("div", {
        class: "w-scrollbar__thumb",
        ref: "thumb",
        style: normalizeStyle(l.thumbStyles)
      }, null, 4)
    ], 34)
  ], 64);
}
var Nl = {
  h: {
    direction: "horizontal",
    topOrLeft: "left",
    size: "width",
    offsetSize: "offsetWidth",
    maxSize: "max-width",
    scrollSize: "scrollWidth",
    clientXorY: "clientX",
    deltaXorY: "deltaX",
    scrollTopOrLeft: "scrollLeft"
  },
  v: {
    direction: "vertical",
    topOrLeft: "top",
    size: "height",
    offsetSize: "offsetHeight",
    maxSize: "max-height",
    scrollSize: "scrollHeight",
    clientXorY: "clientY",
    deltaXorY: "deltaY",
    scrollTopOrLeft: "scrollTop"
  }
};
var Dl = {
  name: "w-scrollable",
  props: {
    color: { type: String, default: "primary" },
    bgColor: { type: String },
    width: { type: [Number, String] },
    height: { type: [Number, String] }
  },
  emits: [],
  data: () => ({
    mounted: false,
    scrollable: {
      top: null,
      left: null,
      hovered: false
    },
    scrollValuePercent: 0
  }),
  computed: {
    isHorizontal() {
      var e, t, i, s;
      return this.mounted ? (console.log("💂‍♂️", (e = this.$refs.scrollable) == null ? void 0 : e.scrollWidth, (t = this.$refs.scrollable) == null ? void 0 : t.offsetWidth), this.width && !this.height || ((i = this.$refs.scrollable) == null ? void 0 : i.scrollWidth) > ((s = this.$refs.scrollable) == null ? void 0 : s.offsetWidth)) : false;
    },
    m() {
      return Nl[this.isHorizontal ? "h" : "v"];
    },
    scrollableClasses() {
      return {
        [`w-scrollable--${this.m.direction}`]: true
      };
    },
    scrollbarClasses() {
      return {
        [`w-scrollbar--${this.m.direction}`]: true
      };
    },
    thumbSizePercent() {
      var t;
      return this.mounted && (this[this.m.size] ?? this.$refs.scrollable[[this.m.offsetSize]]) * 100 / ((t = this.$refs.scrollable) == null ? void 0 : t[this.m.scrollSize]) || 0;
    },
    scrollableStyles() {
      return {
        [this.m.maxSize]: `${this[this.m.size]}px`
      };
    },
    thumbStyles() {
      let e = this.scrollValuePercent;
      return e = Math.max(0, Math.min(e, 100 - this.thumbSizePercent)), {
        [this.m.size]: `${this.thumbSizePercent}%`,
        [this.m.topOrLeft]: `${e}%`
      };
    }
  },
  methods: {
    onTrackMouseDown(e) {
      if (this.isDisabled || this.isReadonly || "ontouchstart" in window && e.type === "mousedown") return;
      const { top: t, left: i, width: s, height: n } = this.$refs.track.getBoundingClientRect();
      this.isHorizontal ? (this.$refs.track.width = s, this.$refs.track.left = i) : (this.$refs.track.height = n, this.$refs.track.top = t), this.dragging = true, this.computeScroll(e.type === "touchstart" ? e.touches[0][this.m.clientXorY] : e[this.m.clientXorY]), this.scroll(), document.addEventListener(e.type === "touchstart" ? "touchmove" : "mousemove", this.onDrag), document.addEventListener(e.type === "touchstart" ? "touchend" : "mouseup", this.onMouseUp, { once: true });
    },
    onDrag(e) {
      this.computeScroll(e.type === "touchmove" ? e.touches[0][this.m.clientXorY] : e[this.m.clientXorY]), this.scroll();
    },
    onMouseUp(e) {
      this.dragging = false, document.removeEventListener(e.type === "touchend" ? "touchmove" : "mousemove", this.onDrag), this.$refs.thumb && this.$refs.thumb.focus();
    },
    onMouseEnter() {
      this.scrollable.hovered = true;
    },
    onMouseLeave() {
      this.scrollable.hovered = false;
    },
    onResize(e) {
    },
    onMouseWheel(e) {
      this.scrollable.hovered && (this.scrollValuePercent <= 0 && e[this.m.deltaXorY] < 0 || this.scrollValuePercent >= 100 - this.thumbSizePercent && e[this.m.deltaXorY] > 0 || (e.preventDefault(), this.scrollValuePercent += e[this.m.deltaXorY] * 0.05, this.scrollValuePercent = Math.max(0, Math.min(this.scrollValuePercent, 100)), this.scroll()));
    },
    computeScroll(e) {
      const { top: t, left: i, width: s, height: n } = this.$refs.scrollable.getBoundingClientRect(), l = this.isHorizontal ? i : t, a = this.isHorizontal ? s : n;
      this.scrollValuePercent = Math.max(0, Math.min((e - l) / a * 100, 100));
    },
    scroll() {
      var e;
      this.$refs.scrollable[this.m.scrollTopOrLeft] = this.scrollValuePercent * ((e = this.$refs.scrollable) == null ? void 0 : e[this.m.scrollSize]) / 100, this.updateThumbPosition();
    },
    updateThumbPosition() {
      this.$refs.thumb.style[this.m.topOrLeft] = this.scrollValuePercent;
    }
  },
  mounted() {
    this.mounted = true;
    const { top: e, left: t } = this.$refs.scrollable.getBoundingClientRect();
    this.scrollable.top = e, this.scrollable.left = t, this.$el.parentNode.style.position = "relative", this.$el.parentNode.style.padding = 0, window.addEventListener("resize", this.onResize);
  }
};
var Hl = v(Dl, [["render", Kl]]);
var Fl = ["aria-expanded", "aria-owns", "aria-activedescendant"];
var jl = {
  key: 0,
  class: "w-select__selection-slot"
};
var Ul = ["innerHTML"];
var ql = ["value", "name"];
function Xl(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon"), r = resolveComponent("w-list"), d = resolveComponent("w-menu");
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: l.selectionString, disabled: e.isDisabled, readonly: e.isReadonly, isFocused: e.isFocused }, {
    valid: e.valid,
    "onUpdate:valid": t[12] || (t[12] = (h) => e.valid = h),
    onReset: l.onReset,
    wrap: l.hasLabel && i.labelPosition !== "inside",
    class: l.classes
  }), {
    default: withCtx(() => [
      i.labelPosition === "left" ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-select__label w-select__label--left w-form-el-shakable", e.labelClasses]),
          onClick: t[0] || (t[0] = (h) => e.$refs["selection-input"].click())
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 2)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true),
      createVNode(d, mergeProps({
        modelValue: e.showMenu,
        "onUpdate:modelValue": t[10] || (t[10] = (h) => e.showMenu = h),
        onClose: l.closeMenu,
        "menu-class": `w-select__menu ${i.menuClass || ""}`,
        transition: "slide-fade-down",
        "append-to": (i.menuProps || {}).appendTo !== void 0 ? (i.menuProps || {}).appendTo : void 0,
        "align-left": "",
        custom: "",
        "min-width": "activator"
      }, i.menuProps || {}), {
        activator: withCtx(() => [
          createBaseVNode("div", {
            class: normalizeClass(["w-select__selection-wrap", l.inputWrapClasses]),
            onClick: t[6] || (t[6] = (h) => !e.isDisabled && !e.isReadonly && l.onInputFieldClick()),
            role: "button",
            "aria-haspopup": "listbox",
            "aria-expanded": e.showMenu ? "true" : "false",
            "aria-owns": `w-select-menu--${e._.uid}`,
            "aria-activedescendant": `w-select-menu--${e._.uid}_item-1`
          }, [
            renderSlot(e.$slots, "icon-left", {}, () => [
              i.innerIconLeft ? (openBlock(), createBlock(a, {
                key: 0,
                class: "w-select__icon w-select__icon--inner-left",
                tag: "label",
                onClick: t[1] || (t[1] = (h) => e.$emit("click:inner-icon-left", h))
              }, {
                default: withCtx(() => [
                  createTextVNode(toDisplayString(i.innerIconLeft), 1)
                ]),
                _: 1
              })) : createCommentVNode("", true)
            ]),
            e.$slots.selection ? (openBlock(), createElementBlock("div", jl, [
              renderSlot(e.$slots, "selection", {
                item: i.multiple ? e.inputValue : e.inputValue[0]
              })
            ])) : createCommentVNode("", true),
            createBaseVNode("div", mergeProps({
              class: "w-select__selection",
              ref: "selection-input",
              onFocus: t[2] || (t[2] = (h) => !e.isDisabled && !e.isReadonly && l.onFocus(h)),
              onBlur: t[3] || (t[3] = (...h) => l.onBlur && l.onBlur(...h)),
              onKeydown: t[4] || (t[4] = (h) => !e.isDisabled && !e.isReadonly && l.onKeydown(h))
            }, l.selectionAttributes, { innerHTML: l.selectionHtml }), null, 16, Ul),
            (openBlock(true), createElementBlock(Fragment, null, renderList(e.inputValue.length ? e.inputValue : [{}], (h, y) => (openBlock(), createElementBlock("input", {
              key: y,
              type: "hidden",
              value: h.value === void 0 ? "" : h.value.toString(),
              name: e.inputName + (i.multiple ? "[]" : "")
            }, null, 8, ql))), 128)),
            i.labelPosition === "inside" && l.showLabelInside ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
              e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
                key: 0,
                class: normalizeClass(["w-select__label w-select__label--inside w-form-el-shakable", e.labelClasses])
              }, [
                renderSlot(e.$slots, "default", {}, () => [
                  createTextVNode(toDisplayString(i.label), 1)
                ])
              ], 2)) : createCommentVNode("", true)
            ], 64)) : createCommentVNode("", true),
            renderSlot(e.$slots, "icon-right", {}, () => [
              i.innerIconRight ? (openBlock(), createBlock(a, {
                key: 0,
                class: "w-select__icon w-select__icon--inner-right",
                tag: "label",
                onClick: t[5] || (t[5] = (h) => e.$emit("click:inner-icon-right", h))
              }, {
                default: withCtx(() => [
                  createTextVNode(toDisplayString(i.innerIconRight), 1)
                ]),
                _: 1
              })) : createCommentVNode("", true)
            ])
          ], 10, Fl)
        ]),
        default: withCtx(() => [
          createVNode(r, {
            ref: "w-list",
            "model-value": e.inputValue,
            "onUpdate:modelValue": l.onInput,
            onItemClick: t[7] || (t[7] = (h) => e.$emit("item-click", h)),
            onItemSelect: l.onListItemSelect,
            onKeydown: l.onWListKeydown,
            "onKeydown:enter": t[8] || (t[8] = (h) => i.noUnselect && !i.multiple && l.closeMenu()),
            "onKeydown:escape": t[9] || (t[9] = (h) => e.showMenu && (e.showMenu = false)),
            items: l.selectItems,
            multiple: i.multiple,
            "arrows-navigation": "",
            "return-object": "",
            "add-ids": `w-select-menu--${e._.uid}`,
            "no-unselect": i.noUnselect,
            "selection-color": i.selectionColor,
            "item-color-key": i.itemColorKey,
            role: "listbox",
            tabindex: "-1"
          }, createSlots({ _: 2 }, [
            renderList(i.items.length, (h) => ({
              name: `item.${h}`,
              fn: withCtx(({ item: y, selected: S, index: g }) => [
                e.$slots[`item.${h}`] && e.$slots[`item.${h}`](y, S, g) ? renderSlot(e.$slots, `item.${h}`, {
                  key: 0,
                  item: y,
                  selected: S,
                  index: g
                }, () => [
                  createTextVNode(toDisplayString(y[i.itemLabelKey]), 1)
                ]) : renderSlot(e.$slots, "item", {
                  key: 1,
                  item: y,
                  selected: S,
                  index: g
                }, () => [
                  createTextVNode(toDisplayString(y[i.itemLabelKey]), 1)
                ])
              ])
            }))
          ]), 1032, ["model-value", "onUpdate:modelValue", "onItemSelect", "onKeydown", "items", "multiple", "add-ids", "no-unselect", "selection-color", "item-color-key"])
        ]),
        _: 3
      }, 16, ["modelValue", "onClose", "menu-class", "append-to"]),
      i.labelPosition === "right" ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-select__label w-select__label--right w-form-el-shakable", e.labelClasses]),
          onClick: t[11] || (t[11] = (h) => e.$refs["selection-input"].click())
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 2)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 16, ["valid", "onReset", "wrap", "class"]);
}
var Yl = {
  name: "w-select",
  mixins: [E],
  props: {
    items: { type: Array, required: true },
    modelValue: {},
    // v-model on selected item if any.
    multiple: { type: Boolean },
    placeholder: { type: String },
    label: { type: String },
    labelPosition: { type: String, default: "inside" },
    innerIconLeft: { type: String },
    innerIconRight: { type: String, default: "wi-triangle-down" },
    // When label is inside, allows to move the label above on focus or when filled.
    staticLabel: { type: Boolean },
    itemLabelKey: { type: String, default: "label" },
    // Name of the label field.
    itemColorKey: { type: String, default: "color" },
    // Name of the color field.
    itemValueKey: { type: String, default: "value" },
    // Name of the value field.
    itemClass: { type: String },
    menuClass: { type: String },
    color: { type: String, default: "primary" },
    // Applies to all the items.
    bgColor: { type: String },
    // Applies to all the items.
    labelColor: { type: String, default: "primary" },
    selectionColor: { type: String, default: "primary" },
    // Applies to the selected items only.
    outline: { type: Boolean },
    round: { type: Boolean },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    returnObject: { type: Boolean },
    // By default you can unselect a list item by re-selecting it.
    // Allow preventing that on single selection lists only.
    noUnselect: { type: Boolean },
    menuProps: { type: Object },
    // Allow passing down an object of props to the w-menu component.
    dark: { type: Boolean },
    light: { type: Boolean },
    fitToContent: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, tabindex, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus", "blur", "item-click", "item-select", "click:inner-icon-left", "click:inner-icon-right"],
  data: () => ({
    // Selection is always an array (internally), but emits a single value if not multiple.
    // inputValue is always an array of objects that have a `value`.
    inputValue: [],
    showMenu: false,
    menuMinWidth: 0,
    isFocused: false,
    selectionWrapRef: void 0,
    // Lookup a select list item from typing the first characters.
    // If typing is too slow (> 1s), the lookup string is cleared.
    quickLookup: { string: "", timeout: null }
  }),
  computed: {
    // Check all the items and add a `value` if missing, containing either: value, label or index
    // in this order.
    selectItems() {
      return this.items.map((e, t) => {
        const i = { ...e };
        return i.value = i[this.itemValueKey] === void 0 ? i[this.itemLabelKey] || t : i[this.itemValueKey], i.index = t, i;
      });
    },
    hasLabel() {
      return this.label || this.$slots.default;
    },
    showLabelInside() {
      return !this.staticLabel || !this.inputValue.length && !this.placeholder;
    },
    selectionAttributes() {
      return {
        class: { "w-select__selection--placeholder": !this.$slots.selection && !this.selectionString && this.placeholder },
        disabled: this.isDisabled || null,
        readonly: true,
        ariareadonly: "true",
        tabindex: this.tabindex ?? null,
        contenteditable: this.isDisabled || this.isReadonly ? "false" : "true"
      };
    },
    selectionString() {
      return this.inputValue.map(
        (e) => e[this.itemValueKey] !== void 0 ? e[this.itemLabelKey] : e[this.itemLabelKey] ?? e
      ).join(", ");
    },
    selectionHtml() {
      return this.inputValue.length ? this.$slots.selection ? "" : this.selectionString : this.placeholder || "";
    },
    classes() {
      return {
        "w-select": true,
        "w-select--dark": this.dark,
        "w-select--light": this.light,
        "w-select--disabled": this.isDisabled,
        "w-select--fit-to-content": this.fitToContent,
        "w-select--readonly": this.isReadonly,
        [`w-select--${this.inputValue.length ? "filled" : "empty"}`]: true,
        "w-select--focused": (this.isFocused || this.showMenu) && !this.isReadonly,
        "w-select--floating-label": this.hasLabel && this.labelPosition === "inside" && !this.staticLabel,
        "w-select--no-padding": !this.outline && !this.bgColor && !this.shadow && !this.round,
        "w-select--has-placeholder": this.placeholder,
        "w-select--inner-icon-left": this.innerIconLeft,
        "w-select--inner-icon-right": this.innerIconRight,
        "w-select--open": this.showMenu
      };
    },
    inputWrapClasses() {
      return {
        [this.valid === false ? "error" : this.color]: this.color || this.valid === false,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-select__selection-wrap--round": this.round,
        "w-select__selection-wrap--tile": this.tile,
        // Box adds a padding on input. If there is a bgColor or shadow, a padding is needed.
        "w-select__selection-wrap--box": this.outline || this.bgColor || this.shadow,
        "w-select__selection-wrap--underline": !this.outline,
        "w-select__selection-wrap--shadow": this.shadow,
        "w-select__selection-wrap--no-padding": !this.outline && !this.bgColor && !this.shadow && !this.round
      };
    }
  },
  methods: {
    onFocus(e) {
      if (!this.isFocused)
        return this.isFocused = true, this.$emit("focus", e), false;
    },
    onBlur(e) {
      this.showMenu || (this.isFocused = false, this.$emit("blur", e));
    },
    onKeydown(e) {
      if (!e.metaKey && !e.ctrlKey && e.keyCode !== 9 && e.preventDefault(), e.keyCode === 27 && this.showMenu) this.closeMenu();
      else if ([13, 32].includes(e.keyCode)) this.openMenu();
      else if ([38, 40].includes(e.keyCode))
        if (this.multiple) this.openMenu();
        else {
          let { index: t } = this.inputValue[0] || {};
          const i = this.selectItems;
          if (t === void 0) t = e.keyCode === 38 ? i.length - 1 : 0;
          else {
            const n = e.keyCode === 38 ? -1 : 1;
            t = (t + i.length + n) % i.length;
          }
          let s = false;
          if (i[t].disabled) {
            const n = e.keyCode === 38 ? -1 : 1;
            let l = (t + n + i.length) % i.length;
            const a = i.length;
            let r = 0;
            for (; r < a && i[l].disabled; )
              l = (l + i.length + n) % i.length, r++;
            r >= a && (s = true), t = l;
          }
          s || this.onInput(i[t]);
        }
      else e.key.length === 1 && this.focusItemOnQuickLookup(e);
    },
    onWListKeydown(e) {
      e.key.length === 1 && this.focusItemOnQuickLookup(e);
    },
    focusItemOnQuickLookup(e) {
      var s, n, l;
      this.quickLookup.timeout && clearTimeout(this.quickLookup.timeout), this.quickLookup.timeout = setTimeout(() => this.quickLookup.string = "", 1e3), this.quickLookup.string += e.key;
      const t = new RegExp(`^${this.quickLookup.string}`, "i"), i = this.selectItems.findIndex(
        (a) => !a.disabled && a[this.itemLabelKey].match(t)
      ) + 1;
      if (i) {
        const a = `.w-list__item:nth-child(${i}) .w-list__item-label`;
        (l = (n = (s = this.$refs["w-list"]) == null ? void 0 : s.$el) == null ? void 0 : n.querySelector(a)) == null || l.focus();
      }
    },
    // The items are given by the w-list component.
    onInput(e) {
      this.inputValue = e === null ? [] : this.multiple ? e : [e], e = this.inputValue.map((i) => this.returnObject ? this.items[i.index] : i.value);
      const t = this.multiple ? e : e[0];
      this.$emit("update:modelValue", t), this.$emit("input", t);
    },
    onInputFieldClick() {
      this.showMenu ? this.showMenu = false : this.openMenu();
    },
    // Called on item selection: on click & `enter` keydown.
    onListItemSelect(e) {
      this.$emit("item-select", e), this.multiple || (this.showMenu = false);
    },
    onReset() {
      this.inputValue = [];
      const e = this.multiple ? [] : null;
      this.$emit("update:modelValue", e), this.$emit("input", e);
    },
    // Convert the received items selection to array if it is a unique value.
    // Also accept objects if returnObject is true.
    // In any case, always end up with an array.
    checkSelection(e) {
      e = Array.isArray(e) ? e : e !== void 0 ? [e] : [];
      const t = this.selectItems.map((i) => i.value);
      return e.map((i) => {
        let s = i;
        return i && typeof i == "object" && (s = i[this.itemValueKey] ?? i[this.itemLabelKey] ?? i), this.selectItems[t.indexOf(s)];
      }).filter((i) => i !== void 0);
    },
    // Open the dropdown selection list.
    openMenu() {
      this.showMenu = true, setTimeout(() => {
        var t;
        const e = this.inputValue.length ? this.inputValue[0].index : 0;
        (t = this.$refs["w-list"].$el.querySelector(`#w-select-menu--${this._.uid}_item-${e + 1}`)) == null || t.focus();
      }, 100);
    },
    // Close the dropdown selection list.
    closeMenu() {
      var e;
      ((e = this.menuProps) == null ? void 0 : e.hideOnMenuClick) !== false && (this.showMenu = false, setTimeout(() => {
        var t;
        return (t = this.$refs["selection-input"]) == null ? void 0 : t.focus();
      }, 50));
    }
  },
  created() {
    this.inputValue = this.checkSelection(this.modelValue);
  },
  watch: {
    modelValue(e) {
      e !== this.inputValue && (this.inputValue = this.checkSelection(e));
    },
    items() {
      this.inputValue = this.checkSelection(this.modelValue);
    }
  }
};
var Gl = v(Yl, [["render", Xl]]);
var Jl = ["for"];
var Ql = ["for", "innerHTML"];
var Zl = { class: "w-slider__track-wrap" };
var es = ["aria-valuemin", "aria-valuemax", "aria-valuenow", "aria-readonly"];
var ts = ["id", "name", "model-value", "disabled", "readonly", "aria-readonly", "tabindex"];
var is = ["for"];
var ls = { key: 0 };
var ss = {
  key: 0,
  class: "w-slider__step-labels"
};
var os = ["onClick"];
var as = ["for"];
var ns = ["for", "innerHTML"];
function rs(e, t, i, s, n, l) {
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: e.rangeValueScaled, disabled: e.isDisabled, readonly: e.isReadonly }, {
    valid: e.valid,
    "onUpdate:valid": t[8] || (t[8] = (a) => e.valid = a),
    onReset: t[9] || (t[9] = (a) => {
      e.rangeValuePercent = 0, l.updateRangeValueScaled();
    }),
    wrap: e.formRegister || null,
    class: l.wrapperClasses
  }), {
    default: withCtx(() => [
      e.$slots["label-left"] ? (openBlock(), createElementBlock("label", {
        key: 0,
        class: normalizeClass(["w-slider__label w-slider__label--left w-form-el-shakable", e.labelClasses]),
        for: `button--${e._.uid}`
      }, [
        renderSlot(e.$slots, "label-left")
      ], 10, Jl)) : i.labelLeft ? (openBlock(), createElementBlock("label", {
        key: 1,
        class: normalizeClass(["w-slider__label w-slider__label--left w-form-el-shakable", e.labelClasses]),
        for: `button--${e._.uid}`,
        innerHTML: i.labelLeft
      }, null, 10, Ql)) : createCommentVNode("", true),
      createBaseVNode("div", Zl, [
        createBaseVNode("div", {
          class: normalizeClass(["w-slider__track", l.trackClasses]),
          ref: "track",
          onMousedown: t[4] || (t[4] = (...a) => l.onTrackMouseDown && l.onTrackMouseDown(...a)),
          onTouchstart: t[5] || (t[5] = (...a) => l.onTrackMouseDown && l.onTrackMouseDown(...a)),
          role: "slider",
          "aria-label": "Slider",
          "aria-valuemin": l.minVal,
          "aria-valuemax": l.maxVal,
          "aria-valuenow": e.rangeValueScaled,
          "aria-readonly": e.isReadonly ? "true" : "false",
          "aria-orientation": "horizontal"
        }, [
          createBaseVNode("div", {
            class: normalizeClass(["w-slider__range", l.rangeClasses]),
            style: normalizeStyle(l.rangeStyles)
          }, null, 6),
          createBaseVNode("div", {
            class: "w-slider__thumb",
            style: normalizeStyle(l.thumbStyles)
          }, [
            createBaseVNode("button", {
              class: normalizeClass(["w-slider__thumb-button", [i.color]]),
              ref: "thumb",
              id: `button--${e._.uid}`,
              name: e.inputName,
              "model-value": e.rangeValueScaled,
              disabled: e.isDisabled || null,
              readonly: e.isReadonly || null,
              "aria-readonly": e.isReadonly ? "true" : "false",
              tabindex: e.isDisabled || e.isReadonly ? -1 : null,
              onKeydown: [
                t[0] || (t[0] = withKeys((a) => l.onKeyDown(a, -1), ["left"])),
                t[1] || (t[1] = withKeys((a) => l.onKeyDown(a, 1), ["right"]))
              ],
              onFocus: t[2] || (t[2] = (a) => e.$emit("focus", a)),
              onClick: t[3] || (t[3] = withModifiers(() => {
              }, ["prevent"]))
            }, null, 42, ts),
            i.thumbLabel ? (openBlock(), createElementBlock("label", {
              key: 0,
              class: normalizeClass(["w-slider__thumb-label", l.thumbClasses]),
              for: `button--${e._.uid}`
            }, [
              i.thumbLabel === "droplet" ? (openBlock(), createElementBlock("div", ls, [
                renderSlot(e.$slots, "label", { value: e.rangeValueScaled }, () => [
                  createTextVNode(toDisplayString(~~e.rangeValueScaled), 1)
                ])
              ])) : renderSlot(e.$slots, "label", {
                key: 1,
                value: e.rangeValueScaled
              }, () => [
                createTextVNode(toDisplayString(~~e.rangeValueScaled), 1)
              ])
            ], 10, is)) : createCommentVNode("", true)
          ], 4)
        ], 42, es),
        i.stepLabels && i.step ? (openBlock(), createElementBlock("div", ss, [
          createBaseVNode("div", {
            class: "w-slider__step-label",
            onClick: t[6] || (t[6] = (a) => l.onStepLabelClick(0))
          }, toDisplayString(this.minVal), 1),
          (openBlock(true), createElementBlock(Fragment, null, renderList(~~l.numberOfSteps, (a) => (openBlock(), createElementBlock("div", {
            class: "w-slider__step-label",
            key: a,
            onClick: (r) => l.onStepLabelClick(a * (100 / l.numberOfSteps)),
            style: normalizeStyle(`left: ${a * (100 / l.numberOfSteps)}%`)
          }, toDisplayString(l.percentToScaled(a * (100 / l.numberOfSteps))), 13, os))), 128)),
          ~~l.numberOfSteps !== l.numberOfSteps ? (openBlock(), createElementBlock("div", {
            key: 0,
            class: "w-slider__step-label",
            onClick: t[7] || (t[7] = (a) => l.onStepLabelClick(100)),
            style: { left: "100%" }
          }, toDisplayString(this.maxVal), 1)) : createCommentVNode("", true)
        ])) : createCommentVNode("", true)
      ]),
      e.$slots["label-right"] ? (openBlock(), createElementBlock("label", {
        key: 2,
        class: normalizeClass(["w-slider__label w-slider__label--right w-form-el-shakable", e.labelClasses]),
        for: `button--${e._.uid}`
      }, [
        renderSlot(e.$slots, "label-right")
      ], 10, as)) : i.labelRight ? (openBlock(), createElementBlock("label", {
        key: 3,
        class: normalizeClass(["w-slider__label w-slider__label--right w-form-el-shakable", e.labelClasses]),
        for: `button--${e._.uid}`,
        innerHTML: i.labelRight
      }, null, 10, ns)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 16, ["valid", "wrap", "class"]);
}
var ds = {
  name: "w-slider",
  mixins: [E],
  props: {
    modelValue: { type: Number, default: 0 },
    color: { type: String, default: "primary" },
    bgColor: { type: String },
    labelColor: { type: String, default: "primary" },
    stepLabels: { type: [Boolean, Array] },
    thumbLabel: { type: [Boolean, String] },
    // One of true, false, 'droplet'.
    thumbLabelClass: { type: String },
    trackClass: { type: String },
    rangeClass: { type: String },
    min: { type: [Number, String], default: 0 },
    max: { type: [Number, String], default: 100 },
    step: { type: [Number, String] },
    labelLeft: { type: String },
    labelRight: { type: String },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus"],
  data: () => ({
    track: {
      el: null,
      left: 0,
      width: 0
    },
    dragging: false,
    rangeValuePercent: 0,
    rangeValueScaled: 0
  }),
  computed: {
    minVal() {
      return parseFloat(this.min);
    },
    maxVal() {
      return parseFloat(this.max);
    },
    stepValPercent() {
      return Math.min(parseFloat(this.step), this.scaledRange) / this.scaledRange * 100;
    },
    // Lighten the maths while dragging by caching some of the maths - it's already that!
    scaledRange() {
      return this.maxVal - this.minVal;
    },
    numberOfSteps() {
      return 100 / this.stepValPercent;
    },
    rangeStyles() {
      return {
        width: `${this.rangeValuePercent}%`
      };
    },
    thumbStyles() {
      return {
        left: `${this.rangeValuePercent}%`
      };
    },
    rangeClasses() {
      return {
        [`${this.color}--bg`]: this.color,
        [this.rangeClass]: this.rangeClass || null
      };
    },
    trackClasses() {
      return {
        [`${this.bgColor}--bg`]: this.bgColor,
        [this.trackClass]: this.trackClass || null
      };
    },
    thumbClasses() {
      return {
        [this.thumbLabelClass]: this.thumbLabelClass || null,
        "w-slider__thumb-label--droplet": this.thumbLabel === "droplet"
      };
    },
    wrapperClasses() {
      return {
        "w-slider": true,
        "w-slider--dark": this.dark,
        "w-slider--light": this.light,
        "w-slider--dragging": this.dragging,
        "w-slider--disabled": this.isDisabled,
        "w-slider--readonly": this.isReadonly,
        "w-slider--has-step-labels": this.step && this.stepLabels
      };
    }
  },
  methods: {
    scaledToPercent(e) {
      return Math.max(0, Math.min((e - this.minVal) / this.scaledRange * 100, 100));
    },
    percentToScaled(e) {
      return Math.round((e / 100 * this.scaledRange + this.minVal) * 100) / 100;
    },
    onTrackMouseDown(e) {
      if (this.isDisabled || this.isReadonly || "ontouchstart" in window && e.type === "mousedown") return;
      const { left: t, width: i } = this.track.el.getBoundingClientRect();
      this.track.width = i, this.track.left = t, this.dragging = true, this.updateRange(e.type === "touchstart" ? e.touches[0].clientX : e.clientX), document.addEventListener(e.type === "touchstart" ? "touchmove" : "mousemove", this.onDrag), document.addEventListener(e.type === "touchstart" ? "touchend" : "mouseup", this.onMouseUp, { once: true });
    },
    onDrag(e) {
      this.updateRange(e.type === "touchmove" ? e.touches[0].clientX : e.clientX);
    },
    onMouseUp(e) {
      this.dragging = false, document.removeEventListener(e.type === "touchend" ? "touchmove" : "mousemove", this.onDrag), this.$refs.thumb && this.$refs.thumb.focus();
    },
    onStepLabelClick(e) {
      this.rangeValuePercent = e, this.updateRangeValueScaled();
    },
    onKeyDown(e, t) {
      this.isDisabled || this.isReadonly || (this.rangeValuePercent += t * (e.shiftKey ? 5 : 1) * (this.stepValPercent || 1), this.rangeValuePercent = Math.max(0, Math.min(this.rangeValuePercent, 100)), this.updateRangeValueScaled());
    },
    updateRange(e) {
      if (this.rangeValuePercent = Math.max(0, Math.min((e - this.track.left) / this.track.width * 100, 100)), this.step) {
        const t = this.rangeValuePercent + this.stepValPercent / 2;
        this.rangeValuePercent = t - t % this.stepValPercent;
      }
      this.updateRangeValueScaled();
    },
    updateRangeValueScaled() {
      this.rangeValueScaled = this.percentToScaled(this.rangeValuePercent), this.$emit("update:modelValue", this.rangeValueScaled), this.$emit("input", this.rangeValueScaled);
    }
  },
  beforeMount() {
    this.$nextTick(() => {
      this.track.el = this.$refs.track, this.rangeValueScaled = this.modelValue, this.rangeValuePercent = this.scaledToPercent(this.modelValue);
    });
  },
  watch: {
    modelValue(e) {
      this.rangeValueScaled !== e && (this.rangeValueScaled = e, this.rangeValuePercent = this.scaledToPercent(e));
    }
  }
};
var us = v(ds, [["render", rs]]);
var hs = { key: 0 };
function cs(e, t, i, s, n, l) {
  return i.modelValue || i.modelValue === void 0 ? (openBlock(), createElementBlock("div", {
    key: 0,
    class: normalizeClass(["w-spinner", l.classes]),
    style: normalizeStyle(l.styles)
  }, [
    l.isThreeDots ? (openBlock(), createElementBlock("span", hs)) : createCommentVNode("", true)
  ], 6)) : createCommentVNode("", true);
}
var ps = {
  name: "w-spinner",
  props: {
    modelValue: {},
    color: { type: String, default: "primary" },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    size: { type: [Number, String] },
    bounce: { type: Boolean },
    fade: { type: Boolean }
  },
  emits: [],
  computed: {
    isThreeDots() {
      return !this.bounce && !this.fade;
    },
    forcedSize() {
      return this.size && (isNaN(this.size) ? this.size : `${this.size}px`);
    },
    presetSize() {
      return this.xs && "xs" || this.sm && "sm" || this.md && "md" || this.lg && "lg" || this.xl && "xl" || null;
    },
    styles() {
      return this.forcedSize && `font-size: ${this.forcedSize}` || null;
    },
    classes() {
      return {
        [this.color]: this.color,
        [`size--${this.presetSize}`]: this.presetSize && !this.forcedSize,
        "w-spinner--bounce": this.bounce,
        "w-spinner--fade": this.fade,
        "w-spinner--three-dots": this.isThreeDots
      };
    }
  }
};
var fs = v(ps, [["render", cs]]);
function ms(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-steps", l.classes])
  }, null, 2);
}
var gs = {
  name: "w-steps",
  props: {
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  data: () => ({}),
  computed: {
    classes() {
      return {
        "w-steps--dark": this.dark,
        "w-steps--light": this.light
      };
    }
  }
};
var bs = v(gs, [["render", ms]]);
var ys = ["id", "name", "checked", "disabled", "readonly", "aria-readonly", "required", "tabindex", "aria-checked"];
var ws = ["for"];
var ks = {
  key: 0,
  class: "w-switch__track"
};
var vs = {
  key: 1,
  class: "w-switch__thumb"
};
var Cs = ["for"];
function Ss(e, t, i, s, n, l) {
  const a = resolveComponent("w-progress");
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: n.isOn, disabled: e.isDisabled, readonly: e.isReadonly }, {
    valid: e.valid,
    "onUpdate:valid": t[3] || (t[3] = (r) => e.valid = r),
    onReset: t[4] || (t[4] = (r) => {
      e.$emit("update:modelValue", n.isOn = null), e.$emit("input", null);
    }),
    class: l.classes,
    style: e.$attrs.style
  }), {
    default: withCtx(() => [
      createBaseVNode("input", mergeProps({
        ref: "input",
        id: `w-switch--${e._.uid}`,
        type: "checkbox",
        name: e.inputName,
        checked: n.isOn,
        disabled: e.isDisabled || e.isReadonly || null,
        readonly: e.isReadonly || null,
        "aria-readonly": e.isReadonly ? "true" : "false",
        required: e.required || null,
        tabindex: e.tabindex || null
      }, l.attrs, {
        onChange: t[0] || (t[0] = (r) => l.onInput()),
        onFocus: t[1] || (t[1] = (r) => e.$emit("focus", r)),
        "aria-checked": n.isOn || "false",
        role: "switch"
      }), null, 16, ys),
      l.hasLabel && i.labelOnLeft ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-switch__label w-switch__label--left w-form-el-shakable", e.labelClasses]),
          for: `w-switch--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, ws)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true),
      createBaseVNode("div", {
        class: normalizeClass(["w-switch__input", l.inputClasses]),
        onClick: t[2] || (t[2] = (r) => {
          e.$refs.input.focus(), e.$refs.input.click();
        })
      }, [
        e.$slots.track ? (openBlock(), createElementBlock("div", ks, [
          renderSlot(e.$slots, "track")
        ])) : createCommentVNode("", true),
        e.$slots.thumb || i.loading ? (openBlock(), createElementBlock("div", vs, [
          i.loading ? (openBlock(), createBlock(a, mergeProps({
            key: 0,
            circle: "",
            color: "inherit"
          }, typeof i.loading == "number" && { "model-value": i.loading }), null, 16)) : renderSlot(e.$slots, "thumb", { key: 1 })
        ])) : createCommentVNode("", true)
      ], 2),
      l.hasLabel && !i.labelOnLeft ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-switch__label w-switch__label--right w-form-el-shakable", e.labelClasses]),
          for: `w-switch--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, Cs)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 16, ["valid", "class", "style"]);
}
var Bs = {
  name: "w-switch",
  mixins: [E],
  inheritAttrs: false,
  // The attrs should only be added to the input not the wrapper.
  props: {
    modelValue: { default: false },
    // v-model.
    label: { type: String, default: "" },
    labelOnLeft: { type: Boolean },
    color: { type: String, default: "primary" },
    labelColor: { type: String, default: "primary" },
    thin: { type: Boolean },
    noRipple: { type: Boolean },
    loading: { type: [Boolean, Number], default: false },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, tabindex, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus"],
  data() {
    return {
      isOn: this.modelValue,
      ripple: {
        start: false,
        end: false,
        timeout: null
      }
    };
  },
  computed: {
    attrs() {
      const { class: e, style: t, ...i } = this.$attrs;
      return i;
    },
    hasLabel() {
      return this.label || this.$slots.default;
    },
    classes() {
      return {
        [`w-switch w-switch--${this.isOn ? "on" : "off"}`]: true,
        "w-switch--thin": this.thin,
        "w-switch--disabled": this.isDisabled,
        "w-switch--readonly": this.isReadonly,
        "w-switch--ripple": this.ripple.start,
        "w-switch--custom-thumb": this.$slots.thumb,
        "w-switch--custom-track": this.$slots.track,
        "w-switch--loading": this.loading,
        "w-switch--rippled": this.ripple.end,
        "w-switch--dark": this.dark,
        "w-switch--light": this.light,
        // With the inheritAttrs set to false any class on the component would be lost, so add it back.
        [this.$attrs.class]: !!this.$attrs.class
      };
    },
    inputClasses() {
      const e = this.hasLabel && this.labelOnLeft ? "l" : "r";
      return [
        this.color,
        this.hasLabel ? this.thin ? `m${e}3` : `m${e}2` : ""
      ];
    }
  },
  methods: {
    onInput() {
      this.isOn = !this.isOn, this.$emit("update:modelValue", this.isOn), this.$emit("input", this.isOn), this.noRipple || (this.isOn ? (this.ripple.start = true, this.ripple.timeout = setTimeout(() => {
        this.ripple.start = false, this.ripple.end = true, setTimeout(() => this.ripple.end = false, 100);
      }, 700)) : (this.ripple.start = false, clearTimeout(this.ripple.timeout)));
    }
  },
  watch: {
    modelValue(e) {
      this.isOn = e;
    }
  }
};
var _s = v(Bs, [["render", Ss]]);
var $s = { class: "w-tabs__content" };
function Is(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", $s, [
    renderSlot(e.$slots, "default", { item: i.item })
  ]);
}
var Vs = {
  name: "tab-content",
  // Keep-alive include/exclude mechanism is component-name-based.
  props: { item: Object }
};
var Ts = v(Vs, [["render", Is]]);
var Ls = ["onClick", "onFocus", "tabindex", "onKeypress", "aria-selected"];
var Rs = ["innerHTML"];
var xs = {
  key: 0,
  class: "w-tabs__bar-extra"
};
var Ps = {
  key: 0,
  class: "w-tabs__content-wrap"
};
var Os = ["innerHTML"];
var zs = ["innerHTML"];
function Ms(e, t, i, s, n, l) {
  const a = resolveComponent("tab-content");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-tabs", l.tabsClasses])
  }, [
    createBaseVNode("div", {
      class: normalizeClass(["w-tabs__bar", l.tabsBarClasses]),
      ref: "tabs-bar"
    }, [
      (openBlock(true), createElementBlock(Fragment, null, renderList(e.tabs, (r, d) => (openBlock(), createElementBlock("div", {
        class: normalizeClass(["w-tabs__bar-item", l.barItemClasses(r)]),
        key: d,
        onClick: (h) => !r._disabled && r._uid !== e.activeTabUid && l.openTab(r._uid),
        onFocus: (h) => e.$emit("focus", l.getOriginalItem(r)),
        tabindex: !r._disabled && 0,
        onKeypress: withKeys((h) => !r._disabled && l.openTab(r._uid), ["enter"]),
        "aria-selected": r._uid === e.activeTabUid ? "true" : "false",
        role: "tab"
      }, [
        e.$slots[`item-title.${r.id || d + 1}`] ? renderSlot(e.$slots, `item-title.${r.id || d + 1}`, {
          key: 0,
          item: l.getOriginalItem(r),
          index: d + 1,
          active: r._uid === e.activeTabUid
        }) : renderSlot(e.$slots, "item-title", {
          key: 1,
          item: l.getOriginalItem(r),
          index: d + 1,
          active: r._uid === e.activeTabUid
        }, () => [
          createBaseVNode("div", {
            innerHTML: r[i.itemTitleKey]
          }, null, 8, Rs)
        ])
      ], 42, Ls))), 128)),
      e.$slots["tabs-bar-extra"] ? (openBlock(), createElementBlock("div", xs, [
        renderSlot(e.$slots, "tabs-bar-extra")
      ])) : createCommentVNode("", true),
      !i.noSlider && !i.card ? (openBlock(), createElementBlock("div", {
        key: 1,
        class: normalizeClass(["w-tabs__slider", i.sliderColor]),
        style: normalizeStyle(l.sliderStyles)
      }, null, 6)) : createCommentVNode("", true)
    ], 2),
    e.tabs.length ? (openBlock(), createElementBlock("div", Ps, [
      i.keepInDom ? (openBlock(), createBlock(TransitionGroup, {
        key: 0,
        name: l.transitionName
      }, {
        default: withCtx(() => [
          (openBlock(true), createElementBlock(Fragment, null, renderList(e.tabs, (r, d) => withDirectives((openBlock(), createBlock(a, {
            key: r._uid,
            item: r,
            class: normalizeClass(i.contentClass)
          }, {
            default: withCtx(() => [
              e.$slots[`item-content.${r._index + 1}`] ? renderSlot(e.$slots, `item-content.${r._index + 1}`, {
                key: 0,
                item: l.getOriginalItem(r),
                index: r._index + 1,
                active: r._index === l.activeTab._index
              }) : renderSlot(e.$slots, "item-content", {
                key: 1,
                item: l.getOriginalItem(r),
                index: r._index + 1,
                active: r._index === l.activeTab._index
              }, () => [
                r[i.itemContentKey] ? (openBlock(), createElementBlock("div", {
                  key: 0,
                  innerHTML: r[i.itemContentKey]
                }, null, 8, Os)) : createCommentVNode("", true)
              ])
            ]),
            _: 2
          }, 1032, ["item", "class"])), [
            [vShow, r._uid === l.activeTab._uid]
          ])), 128))
        ]),
        _: 3
      }, 8, ["name"])) : (openBlock(), createBlock(Transition, {
        key: 1,
        name: l.transitionName,
        mode: l.transitionMode
      }, {
        default: withCtx(() => [
          (openBlock(), createBlock(KeepAlive, {
            exclude: i.keepAlive ? "" : "tab-content"
          }, [
            (openBlock(), createBlock(a, {
              key: e.activeTabUid,
              item: l.activeTab,
              class: normalizeClass(i.contentClass)
            }, {
              default: withCtx(({ item: r }) => [
                r ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
                  e.$slots[`item-content.${r._index + 1}`] ? renderSlot(e.$slots, `item-content.${r._index + 1}`, {
                    key: 0,
                    item: l.getOriginalItem(r),
                    index: r._index + 1,
                    active: r._uid === e.activeTabUid
                  }) : renderSlot(e.$slots, "item-content", {
                    key: 1,
                    item: l.getOriginalItem(r),
                    index: r._index + 1,
                    active: r._uid === e.activeTabUid
                  }, () => [
                    r[i.itemContentKey] ? (openBlock(), createElementBlock("div", {
                      key: 0,
                      innerHTML: r[i.itemContentKey]
                    }, null, 8, zs)) : createCommentVNode("", true)
                  ])
                ], 64)) : createCommentVNode("", true)
              ]),
              _: 3
            }, 8, ["item", "class"]))
          ], 1032, ["exclude"]))
        ]),
        _: 3
      }, 8, ["name", "mode"]))
    ])) : createCommentVNode("", true)
  ], 2);
}
var ye = 0;
var Es = {
  name: "w-tabs",
  props: {
    modelValue: { type: [Number, String] },
    color: { type: String },
    bgColor: { type: String },
    items: { type: [Array, Number] },
    itemIdKey: { type: String, default: "id" },
    itemTitleKey: { type: String, default: "title" },
    itemContentKey: { type: String, default: "content" },
    titleClass: { type: String },
    activeClass: { type: String, default: "primary" },
    noSlider: { type: Boolean },
    pillSlider: { type: Boolean },
    sliderColor: { type: String, default: "primary" },
    contentClass: { type: String },
    transition: { type: [String, Boolean], default: "" },
    fillBar: { type: Boolean },
    center: { type: Boolean },
    right: { type: Boolean },
    card: { type: Boolean },
    dark: { type: Boolean },
    light: { type: Boolean },
    keepAlive: { type: Boolean, default: true },
    keepInDom: { type: Boolean, default: false }
  },
  components: { TabContent: Ts },
  emits: ["input", "update:modelValue", "focus"],
  data: () => ({
    tabs: [],
    activeTabEl: null,
    activeTabUid: null,
    activeTabIndex: 0,
    prevTabIndex: -1,
    // To detect transition direction.
    slider: {
      left: 0,
      width: 0
    },
    init: true
  }),
  computed: {
    transitionName() {
      return this.transition === false ? "" : this.transition || `w-tabs-slide-${this.direction}`;
    },
    transitionMode() {
      return ["w-tabs-slide-left", "w-tabs-slide-right"].includes(this.transitionName) ? "" : "out-in";
    },
    direction() {
      return this.activeTab._index < this.prevTabIndex ? "right" : "left";
    },
    activeTab() {
      return this.tabsByUid[this.activeTabUid] || this.tabs[0] || {};
    },
    // An object indexing the tabs by their uid.
    tabsByUid() {
      return this.tabs.reduce((e, t) => (e[t._uid] = t) && e, {});
    },
    tabsClasses() {
      return {
        "w-tabs--card": this.card,
        "w-tabs--no-slider": this.noSlider,
        "w-tabs--pill-slider": this.pillSlider,
        "w-tabs--fill-bar": this.fillBar,
        "w-tabs--init": this.init,
        "w-tabs--dark": this.dark,
        "w-tabs--light": this.light
      };
    },
    tabsBarClasses() {
      return {
        "w-tabs__bar--right": this.right,
        "w-tabs__bar--center": this.center
      };
    },
    sliderStyles() {
      return {
        left: this.slider.left,
        width: this.slider.width
      };
    }
  },
  methods: {
    // Adding a tab in the list.
    addTab(e) {
      (e[this.itemIdKey] ?? e._uid ?? false) || (e._uid = +`${this._.uid}${++ye}`), this.tabs.push({
        _uid: e[this.itemIdKey] ?? e._uid,
        _index: this.tabs.length,
        ...e,
        _disabled: !!e.disabled
      });
    },
    refreshTabs() {
      let e = this.items;
      typeof e == "number" && (e = Array(e).fill().map((t, i) => this.tabs[i] || {})), this.tabs = e.map((t, i) => ((t[this.itemIdKey] ?? t._uid ?? false) || (t._uid = +`${this._.uid}${++ye}`), {
        ...t,
        _uid: t[this.itemIdKey] ?? t._uid,
        _index: i,
        _disabled: !!t.disabled
      }));
    },
    reopenTheActiveTab() {
      var t, i, s;
      if (this.tabs.length === 1) return this.openTab(this.tabs[0]._uid);
      let e = (t = this.tabsByUid[this.activeTabUid]) == null ? void 0 : t._uid;
      e || (e = (i = this.tabs[this.activeTabIndex]) == null ? void 0 : i._uid), e || (e = (s = this.tabs[Math.max(this.activeTabIndex - 1, this.tabs.length - 1)]) == null ? void 0 : s._uid), e && this.openTab(e);
    },
    onResize() {
      this.updateSlider(false);
    },
    barItemClasses(e) {
      const t = e._index === this.activeTabIndex;
      return {
        [`${this.bgColor}--bg`]: this.bgColor,
        [this.color]: this.color && !e._disabled && !(this.activeClass && t),
        [`w-tabs__bar-item--active ${this.activeClass}`]: t,
        "w-tabs__bar-item--disabled": e._disabled,
        [this.titleClass]: this.titleClass
      };
    },
    // Switching tabs.
    openTab(e) {
      this.prevTabIndex = this.activeTabIndex;
      const t = this.tabsByUid[e];
      this.activeTabIndex = t._index, this.activeTabUid = t._uid, this.$emit("update:modelValue", t._index), this.$emit("input", t._index), this.noSlider || this.$nextTick(this.updateSlider);
    },
    // Updates the slider position.
    updateSlider(e = true) {
      if (e) {
        const t = this.$refs["tabs-bar"];
        this.activeTabEl = t && t.querySelector(".w-tabs__bar-item--active");
      }
      if (!this.fillBar && this.activeTabEl) {
        const { left: t, width: i } = this.activeTabEl.getBoundingClientRect(), s = this.activeTabEl.parentNode, { left: n } = s.getBoundingClientRect(), { borderLeftWidth: l } = getComputedStyle(s);
        this.slider.left = `${t - n - parseInt(l) + s.scrollLeft}px`, this.slider.width = `${i}px`;
      } else
        this.slider.left = `${this.activeTab._index * 100 / this.tabs.length}%`, this.slider.width = `${100 / this.tabs.length}%`;
    },
    updateActiveTab(e) {
      var t, i;
      typeof e == "string" ? e = ~~e : (isNaN(e) || e < 0) && (e = 0), (t = this.tabs[e]) != null && t._uid && (this.openTab((i = this.tabs[e]) == null ? void 0 : i._uid), this.$nextTick(() => {
        const s = this.$refs["tabs-bar"];
        this.activeTabEl = s && s.querySelector(`.w-tabs__bar-item:nth-child(${e + 1})`), this.activeTabEl && this.activeTabEl.scrollIntoView({ behavior: "smooth", block: "nearest", inline: "nearest" });
      }));
    },
    // Return the original item (so there is no `_index`, etc.).
    getOriginalItem(e) {
      return this.items[e._index] || {};
    }
  },
  beforeMount() {
    this.tabs = [], (typeof this.items == "number" ? Array(this.items).fill().map(Object) : this.items).forEach(this.addTab), (this.modelValue ?? false) && this.updateActiveTab(this.modelValue), this.$nextTick(() => {
      this.updateSlider(), setTimeout(() => this.init = false, 0);
    }), this.noSlider || window.addEventListener("resize", this.onResize);
  },
  beforeUnmount() {
    window.removeEventListener("resize", this.onResize);
  },
  watch: {
    modelValue(e) {
      e !== this.activeTabIndex && this.updateActiveTab(e);
    },
    items: {
      handler() {
        this.refreshTabs(), this.tabs.length && this.reopenTheActiveTab(), this.noSlider || this.$nextTick(this.updateSlider);
      },
      deep: true
    },
    fillBar() {
      this.noSlider || this.$nextTick(this.updateSlider);
    },
    noSlider(e) {
      e ? window.removeEventListener("resize", this.onResize) : (this.updateSlider(), window.addEventListener("resize", this.onResize));
    }
  }
};
var As = v(Es, [["render", Ms]]);
var Ws = { class: "w-table__scroll-wrap" };
var Ks = { ref: "colgroup" };
var Ns = ["width"];
var Ds = { key: 0 };
var Hs = ["onClick"];
var Fs = ["innerHTML"];
var js = {
  key: 0,
  class: "w-table__progress-bar"
};
var Us = ["colspan"];
var qs = {
  key: 0,
  class: "w-table__progress-bar"
};
var Xs = ["colspan"];
var Ys = { class: "w-table__loading-text" };
var Gs = {
  key: 1,
  class: "no-data"
};
var Js = ["colspan"];
var Qs = ["onClick"];
var Zs = ["data-label"];
var eo = ["data-label"];
var to = ["innerHTML"];
var io = {
  key: 2,
  class: "w-table__row w-table__row--expansion"
};
var lo = ["colspan"];
var so = { key: 0 };
var oo = {
  key: 3,
  class: "w-table__extra-row"
};
var ao = {
  key: 1,
  class: "w-table__footer"
};
var no = {
  key: 1,
  class: "w-table__row"
};
var ro = ["colspan"];
var uo = {
  key: 0,
  class: "w-table__pagination w-pagination"
};
var ho = { class: "pages-wrap" };
var co = { class: "w-pagination__results" };
function po(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon"), r = resolveComponent("w-progress"), d = resolveComponent("w-transition-fade"), h = resolveComponent("w-transition-expand"), y = resolveComponent("w-select"), S = resolveComponent("w-button");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-table w-table--wrap", l.classes])
  }, [
    createBaseVNode("div", Ws, [
      createBaseVNode("table", {
        class: "w-table__table",
        onMousedown: t[1] || (t[1] = (...g) => l.onMouseDown && l.onMouseDown(...g)),
        onMouseover: t[2] || (t[2] = (...g) => l.onMouseOver && l.onMouseOver(...g)),
        onMouseout: t[3] || (t[3] = (...g) => l.onMouseOut && l.onMouseOut(...g))
      }, [
        createBaseVNode("colgroup", Ks, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(i.headers, (g, V) => (openBlock(), createElementBlock("col", {
            class: normalizeClass(["w-table__col", l.colClasses[V]]),
            key: V,
            width: g.width || null
          }, null, 10, Ns))), 128))
        ], 512),
        i.noHeaders ? createCommentVNode("", true) : (openBlock(), createElementBlock("thead", Ds, [
          createBaseVNode("tr", null, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(i.headers, (g, V) => (openBlock(), createElementBlock("th", {
              class: normalizeClass(["w-table__header", l.headerClasses(g)]),
              key: V,
              onClick: (I) => !e.colResizing.dragging && g.sortable !== false && l.sortTable(g)
            }, [
              g.sortable !== false && g.align === "right" ? (openBlock(), createBlock(a, {
                key: 0,
                class: normalizeClass(["w-table__header-sort", l.headerSortClasses(g)])
              }, {
                default: withCtx(() => t[8] || (t[8] = [
                  createTextVNode("wi-arrow-down")
                ])),
                _: 2
              }, 1032, ["class"])) : createCommentVNode("", true),
              g.label ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
                e.$slots["header-label"] ? renderSlot(e.$slots, "header-label", {
                  key: 0,
                  header: g,
                  label: g.label,
                  index: V + 1
                }, () => [
                  createTextVNode(toDisplayString(g.label || ""), 1)
                ]) : (openBlock(), createElementBlock("span", {
                  key: 1,
                  innerHTML: g.label || ""
                }, null, 8, Fs))
              ], 64)) : createCommentVNode("", true),
              g.sortable !== false && g.align !== "right" ? (openBlock(), createBlock(a, {
                key: 2,
                class: normalizeClass(["w-table__header-sort", l.headerSortClasses(g)])
              }, {
                default: withCtx(() => t[9] || (t[9] = [
                  createTextVNode("wi-arrow-down")
                ])),
                _: 2
              }, 1032, ["class"])) : createCommentVNode("", true),
              V < i.headers.length - 1 && i.resizableColumns ? (openBlock(), createElementBlock("span", {
                key: 3,
                class: normalizeClass(["w-table__col-resizer", { "w-table__col-resizer--hover": e.colResizing.hover === V, "w-table__col-resizer--active": e.colResizing.columnIndex === V }]),
                onClick: t[0] || (t[0] = withModifiers(() => {
                }, ["stop"]))
              }, null, 2)) : createCommentVNode("", true)
            ], 10, Hs))), 128))
          ]),
          createVNode(d, null, {
            default: withCtx(() => [
              i.loading === "header" ? (openBlock(), createElementBlock("tr", js, [
                createBaseVNode("td", {
                  colspan: i.headers.length
                }, [
                  createVNode(r, { tile: "" })
                ], 8, Us)
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          })
        ])),
        createBaseVNode("tbody", null, [
          i.loading === true ? (openBlock(), createElementBlock("tr", qs, [
            createBaseVNode("td", {
              colspan: i.headers.length
            }, [
              createVNode(r, { tile: "" }),
              createBaseVNode("div", Ys, [
                renderSlot(e.$slots, "loading", {}, () => [
                  t[10] || (t[10] = createTextVNode("Loading..."))
                ])
              ])
            ], 8, Xs)
          ])) : l.tableItems.length ? createCommentVNode("", true) : (openBlock(), createElementBlock("tr", Gs, [
            createBaseVNode("td", {
              class: "w-table__cell text-center",
              colspan: i.headers.length
            }, [
              renderSlot(e.$slots, "no-data", {}, () => [
                t[11] || (t[11] = createTextVNode("No data to show."))
              ])
            ], 8, Js)
          ])),
          l.tableItems.length && i.loading !== true ? (openBlock(true), createElementBlock(Fragment, { key: 2 }, renderList(l.paginatedItems, (g, V) => (openBlock(), createElementBlock(Fragment, { key: V }, [
            e.$slots.item ? renderSlot(e.$slots, "item", {
              key: 0,
              item: g,
              index: V + 1,
              select: () => l.doSelectRow(g, V),
              classes: { "w-table__row": true, "w-table__row--selected": l.selectedRowsByUid[g._uid] !== void 0, "w-table__row--expanded": l.expandedRowsByUid[g._uid] !== void 0 }
            }) : (openBlock(), createElementBlock("tr", {
              key: 1,
              class: normalizeClass(["w-table__row", { "w-table__row--selected": l.selectedRowsByUid[g._uid] !== void 0, "w-table__row--expanded": l.expandedRowsByUid[g._uid] !== void 0 }]),
              onClick: (I) => l.doSelectRow(g, V)
            }, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(i.headers, (I, O) => (openBlock(), createElementBlock(Fragment, null, [
                e.$slots[`item-cell.${I.key}`] || e.$slots[`item-cell.${O + 1}`] || e.$slots["item-cell"] ? (openBlock(), createElementBlock("td", {
                  class: normalizeClass(["w-table__cell", { [`text-${I.align || "left"}`]: true, "w-table__cell--sticky": I.sticky }]),
                  key: `${O}-a`,
                  "data-label": I.label
                }, [
                  e.$slots[`item-cell.${I.key}`] ? renderSlot(e.$slots, `item-cell.${I.key}`, {
                    key: 0,
                    header: I,
                    item: g,
                    label: g[I.key] || "",
                    index: V + 1
                  }) : e.$slots[`item-cell.${O + 1}`] ? renderSlot(e.$slots, `item-cell.${O + 1}`, {
                    key: 1,
                    header: I,
                    item: g,
                    label: g[I.key] || "",
                    index: V + 1
                  }) : e.$slots["item-cell"] ? renderSlot(e.$slots, "item-cell", {
                    key: 2,
                    header: I,
                    item: g,
                    label: g[I.key] || "",
                    index: V + 1
                  }) : createCommentVNode("", true),
                  O < i.headers.length - 1 && i.resizableColumns ? (openBlock(), createElementBlock("span", {
                    key: 3,
                    class: normalizeClass(["w-table__col-resizer", { "w-table__col-resizer--hover": e.colResizing.hover === O, "w-table__col-resizer--active": e.colResizing.columnIndex === O }])
                  }, null, 2)) : createCommentVNode("", true)
                ], 10, Zs)) : (openBlock(), createElementBlock("td", {
                  class: normalizeClass(["w-table__cell", { [`text-${I.align || "left"}`]: true, "w-table__cell--sticky": I.sticky }]),
                  key: `${O}-b`,
                  "data-label": I.label
                }, [
                  createBaseVNode("div", {
                    innerHTML: g[I.key] || ""
                  }, null, 8, to),
                  O < i.headers.length - 1 && i.resizableColumns ? (openBlock(), createElementBlock("span", {
                    key: 0,
                    class: normalizeClass(["w-table__col-resizer", { "w-table__col-resizer--hover": e.colResizing.hover === O, "w-table__col-resizer--active": e.colResizing.columnIndex === O }])
                  }, null, 2)) : createCommentVNode("", true)
                ], 10, eo))
              ], 64))), 256))
            ], 10, Qs)),
            l.expandedRowsByUid[g._uid] ? (openBlock(), createElementBlock("tr", io, [
              createBaseVNode("td", {
                class: "w-table__cell",
                colspan: i.headers.length
              }, [
                createVNode(h, { y: "" }, {
                  default: withCtx(() => [
                    l.expandedRowsByUid[g._uid] ? (openBlock(), createElementBlock("div", so, [
                      renderSlot(e.$slots, "row-expansion", {
                        item: g,
                        index: V + 1
                      })
                    ])) : createCommentVNode("", true),
                    V < i.headers.length - 1 && i.resizableColumns ? (openBlock(), createElementBlock("span", {
                      key: 1,
                      class: normalizeClass(["w-table__col-resizer", { "w-table__col-resizer--hover": e.colResizing.hover === V, "w-table__col-resizer--active": e.colResizing.columnIndex === e.j }])
                    }, null, 2)) : createCommentVNode("", true)
                  ]),
                  _: 2
                }, 1024)
              ], 8, lo)
            ])) : createCommentVNode("", true)
          ], 64))), 128)) : createCommentVNode("", true),
          e.$slots["extra-row"] ? (openBlock(), createElementBlock("div", oo, [
            renderSlot(e.$slots, "extra-row")
          ])) : createCommentVNode("", true)
        ]),
        e.$slots.footer || e.$slots["footer-row"] ? (openBlock(), createElementBlock("tfoot", ao, [
          e.$slots["footer-row"] ? renderSlot(e.$slots, "footer-row", { key: 0 }) : e.$slots.footer ? (openBlock(), createElementBlock("tr", no, [
            createBaseVNode("td", {
              class: "w-table__cell",
              colspan: i.headers.length
            }, [
              renderSlot(e.$slots, "footer")
            ], 8, ro)
          ])) : createCommentVNode("", true)
        ])) : createCommentVNode("", true)
      ], 32)
    ]),
    i.pagination && e.paginationConfig ? (openBlock(), createElementBlock("div", uo, [
      renderSlot(e.$slots, "pagination", {
        range: `${e.paginationConfig.start}-${e.paginationConfig.end}`,
        total: e.paginationConfig.total,
        pagesCount: e.paginationConfig.pagesCount,
        page: e.paginationConfig.page,
        goToPage: l.goToPage
      }, () => [
        e.paginationConfig.itemsPerPageOptions ? (openBlock(), createBlock(y, {
          key: 0,
          class: "w-pagination__items-per-page",
          modelValue: e.paginationConfig.itemsPerPage,
          "onUpdate:modelValue": t[4] || (t[4] = (g) => e.paginationConfig.itemsPerPage = g),
          onInput: t[5] || (t[5] = (g) => l.updatePaginationConfig({ itemsPerPage: e.paginationConfig.itemsPerPage })),
          items: e.paginationConfig.itemsPerPageOptions,
          "label-position": "left",
          label: "Items per page",
          "label-color": "inherit"
        }, null, 8, ["modelValue", "items"])) : createCommentVNode("", true),
        createBaseVNode("div", ho, [
          createVNode(S, {
            class: "w-pagination__arrow w-pagination__arrow--prev",
            onClick: t[6] || (t[6] = (g) => l.goToPage("-1")),
            disabled: e.paginationConfig.page <= 1,
            icon: "wi-chevron-left",
            text: "",
            lg: ""
          }, null, 8, ["disabled"]),
          e.paginationConfig.pagesCount > 7 ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(e.paginationConfig.pagesCount, (g) => (openBlock(), createElementBlock(Fragment, { key: g }, [
            [1, e.paginationConfig.pagesCount, e.paginationConfig.page - 1, e.paginationConfig.page, e.paginationConfig.page + 1].includes(g) ? (openBlock(), createBlock(S, {
              key: 0,
              class: normalizeClass(["w-pagination__page", { "w-pagination__page--active": g === e.paginationConfig.page }]),
              onClick: (V) => g !== e.paginationConfig.page && l.goToPage(g),
              round: "",
              lg: ""
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(g), 1)
              ]),
              _: 2
            }, 1032, ["onClick", "class"])) : [1, e.paginationConfig.pagesCount, e.paginationConfig.page - 1, e.paginationConfig.page, e.paginationConfig.page + 1].includes(g - 1) ? (openBlock(), createBlock(S, {
              key: 1,
              class: normalizeClass(["w-pagination__page", { "w-pagination__page--active": g === e.paginationConfig.page }]),
              onClick: (V) => g !== e.paginationConfig.page && l.goToPage(g),
              round: "",
              lg: ""
            }, {
              default: withCtx(() => t[12] || (t[12] = [
                createTextVNode("...")
              ])),
              _: 2
            }, 1032, ["onClick", "class"])) : createCommentVNode("", true)
          ], 64))), 128)) : (openBlock(true), createElementBlock(Fragment, { key: 1 }, renderList(e.paginationConfig.pagesCount, (g) => (openBlock(), createBlock(S, {
            class: normalizeClass(["w-pagination__page", { "w-pagination__page--active": g === e.paginationConfig.page }]),
            key: g,
            onClick: (V) => g !== e.paginationConfig.page && l.goToPage(g),
            round: "",
            lg: ""
          }, {
            default: withCtx(() => [
              createTextVNode(toDisplayString(g), 1)
            ]),
            _: 2
          }, 1032, ["onClick", "class"]))), 128)),
          createVNode(S, {
            class: "w-pagination__arrow w-pagination__arrow--next",
            onClick: t[7] || (t[7] = (g) => l.goToPage("+1")),
            disabled: e.paginationConfig.page >= e.paginationConfig.pagesCount,
            icon: "wi-chevron-right",
            text: "",
            lg: ""
          }, null, 8, ["disabled"])
        ]),
        createBaseVNode("span", co, toDisplayString(e.paginationConfig.start) + "-" + toDisplayString(e.paginationConfig.end || e.paginationConfig.total) + " of " + toDisplayString(e.paginationConfig.total), 1)
      ])
    ])) : createCommentVNode("", true)
  ], 2);
}
var we = 15;
var fo = {
  name: "w-table",
  props: {
    items: { type: Array, required: true },
    headers: { type: Array, required: true },
    noHeaders: { type: Boolean },
    fixedLayout: { type: Boolean },
    fixedHeaders: { type: Boolean },
    fixedFooter: { type: Boolean },
    loading: { type: [Boolean, String] },
    // Bool or 'header' to only display the bar in the header.
    // Allow single sort: `+id`, or multiple in an array like: ['+id', '-firstName'].
    sort: { type: [String, Array] },
    sortFunction: { type: Function },
    filter: { type: Function },
    fetch: { type: Function },
    expandableRows: {
      validator: (e) => ([void 0, true, false, 1, "1", ""].includes(e) || F(
        `Wrong value for the w-table's \`expandableRows\` prop. Given: "${e}", expected one of: [undefined, true, false, 1, '1', ''].`
      ), true)
    },
    // Allow providing the expanded rows and keeping it in sync via .sync in Vue 2 or v-model:expandedRows in Vue 3.
    expandedRows: { type: Array },
    selectableRows: {
      validator: (e) => ([void 0, true, false, 1, "1", ""].includes(e) || F(
        `Wrong value for the w-table's \`selectableRows\` prop. Given: "${e}", expected one of: [undefined, true, false, 1, '1', ''].`
      ), true)
    },
    // Allow providing the selected rows and keeping it in sync via .sync in Vue 2 or v-model:selectedRows in Vue 3.
    selectedRows: { type: Array },
    forceSelection: { type: Boolean },
    // Useful to select or expand a row, and even after a filter, the same row will stay selected or expanded.
    uidKey: { type: String, default: "id" },
    mobileBreakpoint: { type: Number, default: 0 },
    resizableColumns: { type: Boolean },
    // An object containing:
    // - itemsPerPage
    // - itemsPerPageOptions
    // - start
    // - end
    // - page
    // - total
    pagination: {
      type: [Boolean, Object, String],
      validator: (e) => {
        if (e) {
          if (typeof e == "object" && (!e.itemsPerPage || e.page && isNaN(e.page)))
            return F(
              "Wrong pagination config received in the w-table's `pagination` prop (received: `" + JSON.stringify(e) + "`). \nExpected object: { itemsPerPage: Integer, page: Integer } or { itemsPerPage: Integer, start: Integer }."
            ), false;
        } else return true;
        return true;
      }
    },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [
    "row-select",
    "row-expand",
    "row-click",
    "update:sort",
    "update:selected-rows",
    "update:expanded-rows",
    "column-resize"
  ],
  data: () => ({
    activeSorting: [],
    selectedRowsInternal: [],
    // Array of uids.
    expandedRowsInternal: [],
    // Array of uids.
    // Column resizing feature.
    colResizing: {
      dragging: false,
      hover: false,
      // False or a column number starting from 0.
      columnIndex: null,
      // Column number starting from 0.
      startCursorX: null,
      colWidth: null,
      nextColWidth: null,
      columnEl: null,
      nextColumnEl: null
    },
    paginationConfig: {
      itemsPerPage: 0,
      itemsPerPageOptions: {},
      start: void 0,
      end: void 0,
      page: 1,
      total: 0
    }
  }),
  computed: {
    tableItems() {
      return this.items.map((e, t) => (e._uid = e[this.uidKey] ?? t, e));
    },
    filteredItems() {
      return typeof this.filter == "function" ? this.tableItems.filter(this.filter) : this.tableItems;
    },
    sortedItems() {
      if (!this.activeSorting.length || this.sortFunction || this.fetch) return this.filteredItems;
      const e = this.activeSorting[0].replace(/^[+-]/, ""), t = this.activeSorting[0][0] === "-";
      return [...this.filteredItems].sort((i, s) => (i = i[e], s = s[e], !isNaN(i) && !isNaN(s) && (i = parseFloat(i), s = parseFloat(s)), (i > s ? 1 : -1) * (t ? -1 : 1)));
    },
    paginatedItems() {
      return typeof this.fetch == "function" ? this.sortedItems : this.sortedItems.slice(this.paginationConfig.start - 1, this.paginationConfig.end);
    },
    // Returns an object containing { key1: '+', key2: '-' }. With + or - for ASC/DESC.
    activeSortingKeys() {
      return this.activeSorting.reduce((e, t) => (e[t.replace(/^[+-]/, "")] = t[0], e), {});
    },
    classes() {
      return {
        "w-table--loading": this.loading,
        "w-table--loading-in-header": this.loading === "header",
        "w-table--fixed-layout": this.fixedLayout || this.resizableColumns || this.hasStickyColumn,
        "w-table--mobile": this.isMobile || null,
        "w-table--resizable-cols": this.resizableColumns || null,
        "w-table--resizing": this.colResizing.dragging,
        "w-table--fixed-header": this.fixedHeaders,
        "w-table--fixed-footer": this.fixedFooter,
        "w-table--sticky-column": this.hasStickyColumn,
        "w-table--dark": this.dark,
        "w-table--light": this.light
      };
    },
    colClasses() {
      return this.headers.map((e) => ({ "w-table__col--highlighted": this.activeSortingKeys[e.key] })) || [];
    },
    isMobile() {
      return ~~this.mobileBreakpoint && this.$waveui.breakpoint.width <= ~~this.mobileBreakpoint;
    },
    hasStickyColumn() {
      return this.headers.find((e) => e.sticky);
    },
    // Faster lookup than array.includes(uid) and also cached.
    selectedRowsByUid() {
      return this.selectedRowsInternal.reduce((e, t) => (e[t] = true) && e, {});
    },
    // Faster lookup than array.includes(uid) and also cached.
    expandedRowsByUid() {
      return this.expandedRowsInternal.reduce((e, t) => (e[t] = true) && e, {});
    }
  },
  methods: {
    headerClasses(e) {
      return {
        "w-table__header--sortable": e.sortable !== false,
        // Can also be falsy with `0`.
        "w-table__header--sticky": e.sticky,
        "w-table__header--resizable": !!this.resizableColumns,
        [`text-${e.align || "left"}`]: true
      };
    },
    headerSortClasses(e) {
      const t = this.activeSortingKeys[e.key];
      return [
        `w-table__header-sort--${t ? "active" : "inactive"}`,
        `w-table__header-sort--${t === "-" ? "desc" : "asc"}`,
        `m${e.align === "right" ? "r" : "l"}1`
      ];
    },
    async sortTable(e) {
      const t = this.activeSortingKeys[e.key];
      t && this.activeSortingKeys[e.key] === "-" ? this.activeSorting = [] : this.activeSorting[0] = (t ? "-" : "+") + e.key, this.$emit("update:sort", this.activeSorting), typeof this.sortFunction == "function" ? await this.sortFunction(this.activeSorting) : typeof this.fetch == "function" && await this.callApiFetch();
    },
    doSelectRow(e, t) {
      const i = this.expandableRows === "" ? true : this.expandableRows, s = this.selectableRows === "" ? true : this.selectableRows;
      if (i) {
        const n = this.expandedRowsByUid[e._uid] === void 0;
        n ? this.expandableRows.toString() === "1" ? this.expandedRowsInternal = [e._uid] : this.expandedRowsInternal.push(e._uid) : this.expandedRowsInternal = this.expandedRowsInternal.filter((l) => l !== e._uid), this.$emit(
          "row-expand",
          {
            item: e,
            index: t,
            expanded: n,
            expandedRows: this.expandedRowsInternal.map((l) => this.filteredItems[l])
          }
        ), this.$emit("update:expanded-rows", this.expandedRowsInternal);
      } else if (s) {
        let n = false;
        const l = this.selectedRowsByUid[e._uid] === void 0;
        l ? (this.selectableRows.toString() === "1" ? this.selectedRowsInternal = [e._uid] : this.selectedRowsInternal.push(e._uid), n = true) : (!this.forceSelection || this.selectedRowsInternal.length > 1) && (this.selectedRowsInternal = this.selectedRowsInternal.filter((a) => a !== e._uid), n = true), n && (this.$emit(
          "row-select",
          {
            item: e,
            index: t,
            selected: l,
            selectedRows: this.selectedRowsInternal.map((a) => this.filteredItems[a])
          }
        ), this.$emit("update:selected-rows", this.selectedRowsInternal));
      }
      this.$emit("row-click", { item: e, index: t });
    },
    // Attach 1 single event listener on the table rather than 1 on each resizer.
    onMouseDown(e) {
      e.target.classList.contains("w-table__col-resizer") && (this.colResizing.columnIndex = +e.target.parentNode.cellIndex, this.colResizing.startCursorX = e.pageX, this.colResizing.columnEl = this.$el.querySelector(`col:nth-child(${this.colResizing.columnIndex + 1})`), this.colResizing.nextColumnEl = this.colResizing.columnEl.nextSibling, this.colResizing.colWidth = this.colResizing.columnEl.offsetWidth, this.colResizing.nextColWidth = this.colResizing.nextColumnEl.offsetWidth, document.addEventListener("mousemove", this.onResizerMouseMove), document.addEventListener("mouseup", this.onResizerMouseUp));
    },
    // Attach 1 single event listener on the table rather than 1 on each resizer.
    onMouseOver({ target: e }) {
      e.classList.contains("w-table__col-resizer") && (this.colResizing.hover = +e.parentNode.cellIndex);
    },
    // Attach 1 single event listener on the table rather than 1 on each resizer.
    onMouseOut({ target: e }) {
      e.classList.contains("w-table__col-resizer") && (this.colResizing.hover = false);
    },
    /**
         * Notes:
         * Make sure there is no change of variable that would cause a DOM refresh,
         * and glitch while dragging.
         * this.$set(this.headers[columnIndex], 'width', colWidth + deltaX)
    
         * If using the width attribute with variable (so data-driven) and not `style.width`,
         * any later change of variable would cause a DOM refresh, and lose the current DOM state
         * (losing the 2 columns width). So do a direct DOM manipulation using `.style.width`.
         */
    onResizerMouseMove(e) {
      const { startCursorX: t, columnEl: i, nextColumnEl: s, colWidth: n, nextColWidth: l } = this.colResizing;
      this.colResizing.dragging = true;
      const a = e.pageX - t, r = n + l, d = n + a, h = l - a;
      i.style.width = n + a + "px", s.style.width = l - a + "px";
      const y = a < 0 && i.offsetWidth > d || i.offsetWidth <= we, S = a > 0 && s.offsetWidth > h;
      if (y) {
        const g = Math.max(i.offsetWidth, we);
        i.style.width = g + "px", s.style.width = r - g + "px";
      } else S && (i.style.width = r - s.offsetWidth + "px", s.style.width = s.offsetWidth + "px");
    },
    onResizerMouseUp() {
      document.removeEventListener("mousemove", this.onResizerMouseMove), document.removeEventListener("mouseup", this.onResizerMouseUp), setTimeout(() => {
        const e = [...this.$refs.colgroup.children].map((t) => {
          var i;
          return ((i = t.style) == null ? void 0 : i.width) || t.offsetWidth;
        });
        this.$emit("column-resize", { index: this.colResizing.columnIndex, widths: e }), this.colResizing.dragging = false, this.colResizing.columnIndex = null, this.colResizing.startCursorX = null, this.colResizing.columnEl = null, this.colResizing.nextColumnEl = null, this.colResizing.colWidth = null, this.colResizing.nextColWidth = null;
      }, 0);
    },
    initPagination() {
      var s, n;
      const e = ((s = this.pagination) == null ? void 0 : s.itemsPerPage) ?? 20, t = ((n = this.pagination) == null ? void 0 : n.itemsPerPageOptions) || [20, 100, { label: "All", value: 0 }];
      t.find((l) => ((l == null ? void 0 : l.value) ?? l) === +e) || t.push(e), this.paginationConfig.itemsPerPageOptions = t.map((l) => ({
        label: ["string", "number"].includes(typeof l) ? l.toString() : l.label || l.value,
        value: ["string", "number"].includes(typeof l) ? ~~l : l.value ?? l.label
      })), this.paginationConfig.itemsPerPageOptions.sort((l, a) => l.value < a.value ? -1 : 1);
      const i = this.paginationConfig.itemsPerPageOptions.shift();
      this.paginationConfig.itemsPerPageOptions.push(i), this.updatePaginationConfig({
        itemsPerPage: e,
        page: this.pagination.page || 1,
        total: this.pagination.total || this.items.length
      });
    },
    /**
     * Updates the pagination object and fills up missing variables to always maintain the paginationConfig
     * object accurate for external use (read and write).
     * The following vars will always be up to date and if one changes from outside the rest of them
     * will update accordingly: itemsPerPage, itemsPerPageOptions, start, end, page, total.
     *
     * @param {Object} config The config object defining the pagination.
     *  This object can have at most itemsPerPage, page, total, and it will define the start, end and
     *  pagesCount from the given or current itemsPerPage, page and total.
     *  - If a total is given for update, trust it blindly (could come from a backend), and recompute
     *    the rest as long as itemsPerPage is defined.
     *  - If a page is given for update, it will navigate to that page.
     *  - If an itemsPerPage is given for update, it will navigate to that page.
     */
    updatePaginationConfig({ itemsPerPage: e, page: t, total: i }) {
      if (i && (this.paginationConfig.total = i), e !== void 0) {
        this.paginationConfig.itemsPerPage = e, e = e || this.paginationConfig.total, this.paginationConfig.page = t || this.paginationConfig.page || 1, t = this.paginationConfig.page, i = this.paginationConfig.total;
        const s = e * t;
        this.paginationConfig.start = 1, this.paginationConfig.end = i >= s ? s : i % s, this.paginationConfig.pagesCount = Math.ceil(i / e);
      }
      t && this.goToPage(t);
    },
    /**
     * Goes to a given page or to the next or previous page.
     *
     * @param {Number|String} page a number to go to a specific page or `-1`, `+1` for prev & next page.
     */
    async goToPage(e) {
      ["-1", "+1"].includes(e) ? this.paginationConfig.page += +e : this.paginationConfig.page = e;
      const { itemsPerPage: t, total: i } = this.paginationConfig;
      this.paginationConfig.page = Math.max(1, this.paginationConfig.page), this.paginationConfig.start = t * (this.paginationConfig.page - 1) + 1, this.paginationConfig.end = this.paginationConfig.start - 1 + (t || i), typeof this.fetch == "function" && await this.callApiFetch();
    },
    /**
     * Call a user provided fetch function in order to fetch table items from an API.
     * While waiting for the call to resolve, nothing in the table will change.
     */
    async callApiFetch() {
      const { page: e, start: t, end: i, total: s, itemsPerPage: n } = this.paginationConfig;
      return await this.fetch({
        page: e,
        start: t,
        end: i || s,
        total: s,
        itemsPerPage: n || s,
        sorting: this.activeSorting
      });
    }
  },
  created() {
    this.sort ? this.activeSorting = Array.isArray(this.sort) ? this.sort : [this.sort] : this.activeSorting = [], (this.expandedRows || []).length && (this.expandedRowsInternal = this.expandedRows), (this.selectedRows || []).length && (this.selectedRowsInternal = this.selectedRows), this.pagination && this.initPagination();
  },
  watch: {
    sort(e) {
      e ? this.activeSorting = Array.isArray(e) ? e : [e] : this.activeSorting = [];
    },
    expandableRows(e) {
      e ? e.toString() === "1" && (this.expandedRowsInternal = this.expandedRowsInternal.slice(0, 1)) : this.expandedRowsInternal = [];
    },
    expandedRows(e) {
      this.expandedRowsInternal = Array.isArray(e) && e.length ? this.expandedRows : [];
    },
    selectableRows(e) {
      e ? e.toString() === "1" && (this.selectedRowsInternal = this.selectedRowsInternal.slice(0, 1)) : this.selectedRowsInternal = [];
    },
    selectedRows(e) {
      this.selectedRowsInternal = Array.isArray(e) && e.length ? this.selectedRows : [];
    },
    "pagination.page"(e) {
      this.updatePaginationConfig({ page: e });
    },
    "pagination.itemsPerPage"(e) {
      this.updatePaginationConfig({ itemsPerPage: e });
    },
    "pagination.total"(e) {
      this.updatePaginationConfig({ total: e });
    }
  }
};
var mo = v(fo, [["render", po]]);
var go = ["role", "aria-pressed", "tabindex"];
function bo(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("span", {
    class: normalizeClass(["w-tag", l.classes]),
    onClick: t[1] || (t[1] = (a) => {
      e.$emit("update:modelValue", !i.modelValue), e.$emit("input", !i.modelValue);
    }),
    onKeypress: t[2] || (t[2] = withKeys((a) => {
      e.$emit("update:modelValue", !i.modelValue), e.$emit("input", !i.modelValue);
    }, ["enter"])),
    role: i.modelValue !== -1 && "button",
    "aria-pressed": i.modelValue !== -1 && (i.modelValue ? "true" : "false"),
    tabindex: i.modelValue !== -1 && 0,
    style: normalizeStyle(l.styles)
  }, [
    renderSlot(e.$slots, "default"),
    i.closable && i.modelValue ? (openBlock(), createElementBlock("i", {
      key: 0,
      class: "w-icon w-tag__closable wi-cross",
      onClick: t[0] || (t[0] = withModifiers((a) => {
        e.$emit("update:modelValue", false), e.$emit("input", false);
      }, ["stop"])),
      role: "icon",
      "aria-hidden": "true"
    })) : createCommentVNode("", true)
  ], 46, go);
}
var yo = {
  name: "w-tag",
  props: {
    modelValue: { type: [Boolean, Number], default: -1 },
    color: { type: String },
    bgColor: { type: String },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    round: { type: Boolean },
    closable: { type: Boolean },
    outline: { type: Boolean },
    noBorder: { type: Boolean },
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    width: { type: [Number, String] },
    height: { type: [Number, String] },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: ["input", "update:modelValue"],
  computed: {
    presetSize() {
      return this.xs && "xs" || this.sm && "sm" || this.lg && "lg" || this.xl && "xl" || "md";
    },
    classes() {
      return {
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        [`size--${this.presetSize}`]: true,
        "w-tag--dark": this.dark,
        "w-tag--light": this.light,
        "w-tag--clickable": this.modelValue !== -1,
        "w-tag--outline": this.outline,
        "w-tag--no-border": this.noBorder || this.shadow,
        "w-tag--tile": this.tile,
        "w-tag--round": this.round,
        "w-tag--shadow": this.shadow
      };
    },
    styles() {
      return {
        width: (isNaN(this.width) ? this.width : `${this.width}px`) || null,
        height: (isNaN(this.height) ? this.height : `${this.height}px`) || null
      };
    }
  }
};
var wo = v(yo, [["render", bo]]);
var ko = ["for"];
var vo = ["id", "name", "placeholder", "rows", "cols", "readonly", "aria-readonly", "disabled", "required", "tabindex"];
var Co = ["for"];
function So(e, t, i, s, n, l) {
  const a = resolveComponent("w-icon");
  return openBlock(), createBlock(resolveDynamicComponent(e.formRegister ? "w-form-element" : "div"), mergeProps({ ref: "formEl" }, e.formRegister && { validators: e.validators, inputValue: n.inputValue, disabled: e.isDisabled, readonly: e.isReadonly, isFocused: n.isFocused }, {
    valid: e.valid,
    "onUpdate:valid": t[6] || (t[6] = (r) => e.valid = r),
    wrap: l.hasLabel && i.labelPosition !== "inside",
    onReset: t[7] || (t[7] = (r) => {
      e.$emit("update:modelValue", n.inputValue = ""), e.$emit("input", "");
    }),
    class: l.classes,
    style: e.$attrs.style
  }), {
    default: withCtx(() => [
      i.labelPosition === "left" ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-textarea__label w-textarea__label--left w-form-el-shakable", e.labelClasses]),
          for: `w-textarea--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, ko)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true),
      createBaseVNode("div", {
        class: normalizeClass(["w-textarea__textarea-wrap", l.inputWrapClasses])
      }, [
        renderSlot(e.$slots, "icon-left", {
          inputId: `w-textarea--${e._.uid}`
        }, () => [
          i.innerIconLeft ? (openBlock(), createBlock(a, {
            key: 0,
            class: "w-textarea__icon w-textarea__icon--inner-left",
            tag: "label",
            for: `w-textarea--${e._.uid}`,
            onClick: t[0] || (t[0] = (r) => e.$emit("click:inner-icon-left", r))
          }, {
            default: withCtx(() => [
              createTextVNode(toDisplayString(i.innerIconLeft), 1)
            ]),
            _: 1
          }, 8, ["for"])) : createCommentVNode("", true)
        ]),
        withDirectives(createBaseVNode("textarea", mergeProps({
          class: "w-textarea__textarea",
          ref: "textarea",
          "onUpdate:modelValue": t[1] || (t[1] = (r) => n.inputValue = r)
        }, l.attrs, {
          onInput: t[2] || (t[2] = (...r) => l.onInput && l.onInput(...r)),
          onFocus: t[3] || (t[3] = (...r) => l.onFocus && l.onFocus(...r)),
          onBlur: t[4] || (t[4] = (...r) => l.onBlur && l.onBlur(...r)),
          id: `w-textarea--${e._.uid}`,
          name: e.inputName,
          placeholder: i.placeholder || null,
          rows: i.rows || null,
          cols: i.cols || null,
          readonly: e.isReadonly || null,
          "aria-readonly": e.isReadonly ? "true" : "false",
          disabled: e.isDisabled || null,
          required: e.required || null,
          tabindex: e.tabindex || null
        }), null, 16, vo), [
          [vModelText, n.inputValue]
        ]),
        i.labelPosition === "inside" && l.showLabelInside ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
          e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
            key: 0,
            class: normalizeClass(["w-textarea__label w-textarea__label--inside w-form-el-shakable", e.labelClasses])
          }, [
            renderSlot(e.$slots, "default", {}, () => [
              createTextVNode(toDisplayString(i.label), 1)
            ])
          ], 2)) : createCommentVNode("", true)
        ], 64)) : createCommentVNode("", true),
        renderSlot(e.$slots, "icon-right", {
          inputId: `w-textarea--${e._.uid}`
        }, () => [
          i.innerIconRight ? (openBlock(), createBlock(a, {
            key: 0,
            class: "w-textarea__icon w-textarea__icon--inner-right",
            tag: "label",
            for: `w-textarea--${e._.uid}`,
            onClick: t[5] || (t[5] = (r) => e.$emit("click:inner-icon-right", r))
          }, {
            default: withCtx(() => [
              createTextVNode(toDisplayString(i.innerIconRight), 1)
            ]),
            _: 1
          }, 8, ["for"])) : createCommentVNode("", true)
        ])
      ], 2),
      i.labelPosition === "right" ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
        e.$slots.default || i.label ? (openBlock(), createElementBlock("label", {
          key: 0,
          class: normalizeClass(["w-textarea__label w-textarea__label--right w-form-el-shakable", e.labelClasses]),
          for: `w-textarea--${e._.uid}`
        }, [
          renderSlot(e.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(i.label), 1)
          ])
        ], 10, Co)) : createCommentVNode("", true)
      ], 64)) : createCommentVNode("", true)
    ]),
    _: 3
  }, 16, ["valid", "wrap", "class", "style"]);
}
var Bo = {
  name: "w-textarea",
  mixins: [E],
  inheritAttrs: false,
  // The attrs should only be added to the textarea not the wrapper.
  props: {
    modelValue: { default: "" },
    label: { type: String },
    labelPosition: { type: String, default: "inside" },
    innerIconLeft: { type: String },
    innerIconRight: { type: String },
    // When label is inside, allows to move the label above on focus or when filled.
    staticLabel: { type: Boolean },
    placeholder: { type: String },
    color: { type: String, default: "primary" },
    bgColor: { type: String },
    labelColor: { type: String, default: "primary" },
    outline: { type: Boolean },
    shadow: { type: Boolean },
    noAutogrow: { type: Boolean },
    resizable: { type: Boolean },
    // Toggle the HTML built-in bottom right corner resize handle.
    tile: { type: Boolean },
    rows: { type: [Number, String], default: 3 },
    cols: { type: [Number, String] },
    dark: { type: Boolean },
    light: { type: Boolean }
    // Props from mixin: name, disabled, readonly, required, tabindex, validators.
    // Computed from mixin: inputName, isDisabled & isReadonly.
  },
  emits: ["input", "update:modelValue", "focus", "blur", "click:inner-icon-left", "click:inner-icon-right"],
  data() {
    return {
      inputValue: this.modelValue,
      isFocused: false,
      // When autogrow, calculate the height from the number of carriage return & font size.
      height: null,
      lineHeight: null,
      paddingY: null
    };
  },
  computed: {
    attrs() {
      const { class: e, style: t, ...i } = this.$attrs;
      return i;
    },
    hasValue() {
      return this.inputValue || this.inputValue === 0;
    },
    hasLabel() {
      return this.label || this.$slots.default;
    },
    showLabelInside() {
      return !this.staticLabel || !this.hasValue && !this.placeholder;
    },
    classes() {
      return {
        "w-textarea": true,
        "w-textarea--disabled": this.isDisabled,
        "w-textarea--readonly": this.isReadonly,
        [`w-textarea--${this.hasValue ? "filled" : "empty"}`]: true,
        "w-textarea--focused": this.isFocused && !this.isReadonly,
        "w-textarea--dark": this.dark,
        "w-textarea--light": this.light,
        "w-textarea--resizable": this.resizable,
        "w-textarea--floating-label": this.hasLabel && this.labelPosition === "inside" && !this.staticLabel,
        "w-textarea--no-padding": !this.outline && !this.bgColor && !this.shadow,
        "w-textarea--has-placeholder": this.placeholder,
        "w-textarea--inner-icon-left": this.innerIconLeft,
        "w-textarea--inner-icon-right": this.innerIconRight,
        // With the inheritAttrs set to false any class on the component would be lost, so add it back.
        [this.$attrs.class]: !!this.$attrs.class
      };
    },
    inputWrapClasses() {
      return {
        [this.valid === false ? this.validationColor : this.color]: this.color || this.valid === false,
        [`${this.bgColor}--bg`]: this.bgColor,
        "w-textarea__textarea-wrap--tile": this.tile,
        // Box adds a padding on input. If there is a bgColor or shadow, a padding is needed.
        "w-textarea__textarea-wrap--box": this.outline || this.bgColor || this.shadow,
        "w-textarea__textarea-wrap--underline": !this.outline,
        "w-textarea__textarea-wrap--shadow": this.shadow,
        "w-textarea__textarea-wrap--no-padding": !this.outline && !this.bgColor && !this.shadow
      };
    },
    textareaStyles() {
      return this.noAutogrow || this.resizable ? {} : {
        height: this.height ? `${this.height}px` : null
      };
    }
  },
  methods: {
    onInput() {
      !this.noAutogrow && !this.resizable && this.computeHeight(), this.$emit("update:modelValue", this.inputValue), this.$emit("input", this.inputValue);
    },
    onFocus(e) {
      this.isFocused = true, this.$emit("focus", e);
    },
    onBlur(e) {
      this.isFocused = false, this.$emit("blur", e);
    },
    computeHeight() {
      this.$refs.textarea.style.height = "";
      const e = (this.$refs.textarea.scrollHeight - this.paddingY) / this.lineHeight, t = Math.max(e, this.rows) * this.lineHeight + this.paddingY;
      this.$refs.textarea.style.height = t + "px";
    },
    getLineHeight() {
      const e = window.getComputedStyle(this.$refs.textarea, null);
      this.lineHeight = parseFloat(e.getPropertyValue("line-height")), this.paddingY = parseFloat(e.getPropertyValue("padding-top")), this.paddingY += parseFloat(e.getPropertyValue("padding-bottom"));
    }
  },
  mounted() {
    !this.noAutogrow && !this.resizable && (this.getLineHeight(), this.computeHeight());
  },
  watch: {
    modelValue(e) {
      this.inputValue = e, this.$nextTick(this.computeHeight);
    },
    resizable(e) {
      e ? this.height = null : this.noAutogrow || this.getLineHeight();
    },
    noAutogrow(e) {
      e ? this.getLineHeight() : this.height = null;
    }
  }
};
var _o = v(Bo, [["render", So]]);
var $o = ["innerHTML"];
var Io = ["innerHTML"];
function Vo(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("ul", {
    class: normalizeClass(["w-timeline", l.classes])
  }, [
    (openBlock(true), createElementBlock(Fragment, null, renderList(i.items, (a, r) => (openBlock(), createElementBlock("li", {
      class: "w-timeline-item",
      key: r
    }, [
      (openBlock(), createBlock(resolveDynamicComponent(a[i.itemIconKey] || i.icon ? "w-icon" : "div"), {
        class: normalizeClass(["w-timeline-item__bullet", { [a[i.itemColorKey] || i.color]: a[i.itemColorKey] || i.color }])
      }, {
        default: withCtx(() => [
          createTextVNode(toDisplayString(a[i.itemIconKey] || i.icon), 1)
        ]),
        _: 2
      }, 1032, ["class"])),
      e.$slots[`item.${r + 1}`] ? renderSlot(e.$slots, `item.${r + 1}`, {
        key: 1,
        item: a,
        index: r + 1
      }) : renderSlot(e.$slots, "item", {
        key: 0,
        item: a,
        index: r + 1
      }, () => [
        createBaseVNode("div", {
          class: normalizeClass(["w-timeline-item__title", { [a[i.itemColorKey] || i.color]: a[i.itemColorKey] || i.color }]),
          innerHTML: a[i.itemTitleKey]
        }, null, 10, $o),
        createBaseVNode("div", {
          class: "w-timeline-item__content",
          innerHTML: a[i.itemContentKey]
        }, null, 8, Io)
      ])
    ]))), 128))
  ], 2);
}
var To = {
  name: "w-timeline",
  props: {
    items: { type: [Array, Number], required: true },
    color: { type: String },
    icon: { type: String },
    itemTitleKey: { type: String, default: "title" },
    itemContentKey: { type: String, default: "content" },
    itemIconKey: { type: String, default: "icon" },
    itemColorKey: { type: String, default: "color" },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  computed: {
    classes() {
      return {
        "w-timeline--dark": this.dark,
        "w-timeline--light": this.light
      };
    }
  }
};
var Lo = v(To, [["render", Vo]]);
function Ro(e, t, i, s, n, l) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["w-toolbar", l.classes]),
    style: normalizeStyle(l.styles)
  }, [
    renderSlot(e.$slots, "default")
  ], 6);
}
var xo = {
  name: "w-toolbar",
  props: {
    color: { type: String },
    bgColor: { type: String },
    absolute: { type: Boolean },
    fixed: { type: Boolean },
    bottom: { type: Boolean },
    vertical: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    width: { type: [Number, String], default: null },
    height: { type: [Number, String], default: null },
    noBorder: { type: Boolean },
    shadow: { type: Boolean },
    dark: { type: Boolean },
    light: { type: Boolean }
  },
  emits: [],
  computed: {
    // Return the height value if defined, or false otherwise.
    toolbarHeight() {
      const e = this.height;
      return e && parseInt(e) == e ? e + "px" : e;
    },
    // Return the width value if defined, or false otherwise.
    toolbarWidth() {
      const e = this.width;
      return e && parseInt(e) == e ? e + "px" : e;
    },
    classes() {
      return {
        [this.color]: !!this.color,
        [`${this.bgColor}--bg`]: !!this.bgColor,
        "w-toolbar--dark": this.dark,
        "w-toolbar--light": this.light,
        "w-toolbar--absolute": !!this.absolute,
        "w-toolbar--fixed": !!this.fixed,
        [`w-toolbar--${this.bottom ? "bottom" : "top"}`]: !this.vertical,
        [`w-toolbar--vertical w-toolbar--${this.right ? "right" : "left"}`]: this.vertical,
        "w-toolbar--no-border": this.noBorder,
        "w-toolbar--shadow": !!this.shadow
      };
    },
    styles() {
      return {
        height: this.height && !this.vertical ? this.toolbarHeight : null,
        width: this.width && this.vertical ? this.toolbarWidth : null
      };
    }
  }
};
var Po = v(xo, [["render", Ro]]);
function Oo(e, t, i, s, n, l) {
  return openBlock(), createElementBlock(Fragment, null, [
    renderSlot(e.$slots, "activator", { on: l.activatorEventHandlers }),
    createVNode(Transition, {
      name: l.transitionName,
      appear: ""
    }, {
      default: withCtx(() => [
        e.detachableVisible ? (openBlock(), createElementBlock("div", {
          class: normalizeClass(["w-tooltip", l.classes]),
          ref: "detachable",
          key: e._.uid,
          style: normalizeStyle(l.styles)
        }, [
          renderSlot(e.$slots, "default")
        ], 6)) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["name"])
  ], 64);
}
var zo = {
  name: "w-tooltip",
  mixins: [Be],
  props: {
    modelValue: {},
    showOnClick: { type: Boolean },
    color: { type: String },
    bgColor: { type: String },
    noBorder: { type: Boolean },
    shadow: { type: Boolean },
    tile: { type: Boolean },
    round: { type: Boolean },
    transition: { type: String },
    tooltipClass: { type: [String, Object, Array] },
    persistent: { type: Boolean },
    delay: { type: Number },
    dark: { type: Boolean },
    light: { type: Boolean },
    caption: { type: Boolean },
    // Apply the caption class and style (grey, italic, small).
    xs: { type: Boolean },
    sm: { type: Boolean },
    md: { type: Boolean },
    lg: { type: Boolean },
    xl: { type: Boolean },
    enableTouch: { type: Boolean }
    // Other props in the detachable mixin:
    // detachTo, appendTo, fixed, top, bottom, left, right, alignTop, alignBottom, alignLeft,
    // alignRight, noPosition, zIndex, activator.
  },
  emits: ["input", "update:modelValue", "open", "close"],
  data: () => ({
    detachableVisible: false,
    hoveringActivator: false,
    // The tooltip computed top & left coordinates.
    detachableCoords: {
      top: 0,
      left: 0
    },
    detachableEl: null,
    timeoutId: null
  }),
  computed: {
    /**
     * Other computed in the detachable mixin:
     * - `appendToTarget`
     * - `detachableParentEl`
     * - `activatorEl`
     * - `position`
     * - `alignment`
     **/
    tooltipClasses() {
      return M(this.tooltipClass);
    },
    transitionName() {
      const e = this.position.replace(/top|bottom/, (t) => ({ top: "up", bottom: "down" })[t]);
      return this.transition || `w-tooltip-slide-fade-${e}`;
    },
    size() {
      return this.xs && "xs" || this.sm && "sm" || this.sm && "md" || this.lg && "lg" || this.xl && "xl" || (this.caption ? "sm" : "md");
    },
    classes() {
      return {
        [this.color]: this.color,
        [`${this.bgColor}--bg`]: this.bgColor,
        ...this.tooltipClasses,
        [`w-tooltip--${this.position}`]: !this.noPosition,
        [`w-tooltip--align-${this.alignment}`]: !this.noPosition && this.alignment,
        "w-tooltip--dark": this.dark,
        "w-tooltip--light": this.light,
        "w-tooltip--tile": this.tile,
        "w-tooltip--round": this.round,
        caption: this.caption,
        [`size--${this.size}`]: true,
        "w-tooltip--shadow": this.shadow,
        "w-tooltip--fixed": this.fixed,
        "w-tooltip--no-border": this.noBorder || this.bgColor,
        "w-tooltip--custom-transition": this.transition
      };
    },
    // The tooltip styles.
    styles() {
      return {
        zIndex: this.zIndex || this.zIndex === 0 || null,
        top: this.detachableCoords.top && `${~~this.detachableCoords.top}px` || null,
        left: this.detachableCoords.left && `${~~this.detachableCoords.left}px` || null,
        "--w-tooltip-bg-color": this.$waveui.colors[this.bgColor] || "rgb(var(--w-base-bg-color-rgb))"
      };
    },
    activatorEventHandlers() {
      let e = {};
      const t = typeof window < "u" && "ontouchstart" in window;
      return !this.showOnClick && !t ? e = {
        focus: this.open,
        blur: this.close,
        mouseenter: (i) => {
          this.hoveringActivator = true, this.open(i);
        },
        mouseleave: (i) => {
          this.hoveringActivator = false, this.close();
        }
      } : (this.enableTouch || this.showOnClick) && (e = { click: this.toggle }), e;
    }
  },
  methods: {
    /**
     * Other methods in the `detachable` mixin:
     * - `open`
     * - `getActivatorCoordinates`
     * - `computeDetachableCoords`
     * - `onResize`
     * - `onOutsideMousedown`
     * - `insertInDOM`
     * - `removeFromDOM`
     **/
    // ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
    toggle(e) {
      let t = this.detachableVisible;
      typeof window < "u" && "ontouchstart" in window ? !this.enableTouch && !this.showOnClick ? t = false : t = !t : e.type === "click" && this.showOnClick ? t = !t : ["mouseenter", "focus"].includes(e.type) && !this.showOnClick ? t = true : ["mouseleave", "blur"].includes(e.type) && !this.showOnClick && (t = false), this.timeoutId = clearTimeout(this.timeoutId), t ? this.open(e) : this.close();
    },
    /**
     * Closes the tooltip. Can happen on:
     * - click of activator
     * - hover outside if showOnHover
     * - click inside tooltip if hideOnTooltipClick.
     * / ! \ This function uses the DOM - NO SSR (only trigger from beforeMount and later).
     *
     * @param {Boolean} force when showOnHover is set to true, hovering tooltip should keep it open.
     *                        But if hideOnTooltipClick is also set to true, this should force close
     *                        even while hovering the tooltip.
     */
    async close(e = false) {
      this.detachableVisible && (this.showOnHover && !e && (await new Promise((t) => setTimeout(t, 10)), this.showOnHover && this.hoveringActivator) || (this.$emit("update:modelValue", this.detachableVisible = false), this.$emit("input", false), this.$emit("close"), document.removeEventListener("mousedown", this.onOutsideMousedown), window.removeEventListener("resize", this.onResize)));
    }
  }
  // watch, mounted & beforeDestroy hooks are set in the detachable.js mixin.
};
var Mo = v(zo, [["render", Oo]]);
function Eo(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: "bounce" }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16);
}
var Ao = {
  name: "w-transition-bounce",
  props: {
    appear: { type: Boolean },
    duration: { type: [Number, String] }
  }
};
var Wo = v(Ao, [["render", Eo]]);
function Ko(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, {
    name: "expand",
    mode: "out-in",
    css: false,
    onBeforeAppear: l.beforeAppear,
    onAppear: l.appear,
    onAfterAppear: l.afterAppear,
    onBeforeEnter: l.beforeEnter,
    onEnter: l.enter,
    onAfterEnter: l.afterEnter,
    onBeforeLeave: l.beforeLeave,
    onLeave: l.leave,
    onAfterLeave: l.afterLeave
  }, {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 8, ["onBeforeAppear", "onAppear", "onAfterAppear", "onBeforeEnter", "onEnter", "onAfterEnter", "onBeforeLeave", "onLeave", "onAfterLeave"]);
}
var No = {
  name: "w-transition-expand",
  props: {
    x: { type: Boolean },
    y: { type: Boolean },
    duration: { type: Number, default: 250 }
  },
  data: () => ({
    el: {
      savedState: false,
      originalStyles: "",
      width: 0,
      height: 0,
      marginLeft: 0,
      marginRight: 0,
      marginTop: 0,
      marginBottom: 0,
      paddingLeft: 0,
      paddingRight: 0,
      paddingTop: 0,
      paddingBottom: 0,
      borderLeftWidth: 0,
      borderRightWidth: 0,
      borderTopWidth: 0,
      borderBottomWidth: 0
    },
    cleanTransitionCycle: true
  }),
  computed: {
    animX() {
      return this.x || !this.y;
    },
    animY() {
      return this.y || !this.x;
    }
  },
  methods: {
    beforeAppear(e) {
      this.cleanTransitionCycle && this.saveOriginalInlineStyles(e), this.cleanTransitionCycle = false;
    },
    appear(e, t) {
      this.show(e), setTimeout(t, this.duration), this.cleanTransitionCycle = false;
    },
    afterAppear(e) {
      this.applyOriginalStyles(e), e.style.cssText = e.style.cssText.replace("display: none;", ""), this.cleanTransitionCycle = false;
    },
    beforeEnter(e) {
      this.cleanTransitionCycle && this.saveOriginalInlineStyles(e), this.cleanTransitionCycle = false;
    },
    enter(e, t) {
      this.show(e), setTimeout(t, this.duration), this.cleanTransitionCycle = false;
    },
    afterEnter(e) {
      this.applyOriginalStyles(e), e.style.cssText = e.style.cssText.replace("display: none;", ""), this.cleanTransitionCycle = false;
    },
    beforeLeave(e) {
      this.el.savedState || this.saveComputedStyles(e), this.beforeHide(e), this.cleanTransitionCycle = false;
    },
    leave(e, t) {
      this.hide(e), setTimeout(t, this.duration), this.cleanTransitionCycle = false;
    },
    afterLeave(e) {
      this.applyOriginalStyles(e), this.cleanTransitionCycle = true, this.el.savedState = false;
    },
    applyHideStyles(e) {
      this.animX && (e.style.width = 0, e.style.marginLeft = 0, e.style.marginRight = 0, e.style.paddingLeft = 0, e.style.paddingRight = 0, e.style.borderLeftWidth = 0, e.style.borderRightWidth = 0), this.animY && (e.style.height = 0, e.style.marginTop = 0, e.style.marginBottom = 0, e.style.paddingTop = 0, e.style.paddingBottom = 0, e.style.borderTopWidth = 0, e.style.borderBottomWidth = 0), e.style.overflow = "hidden";
    },
    applyShowStyles(e) {
      this.animX && (e.style.width = this.el.width + "px", e.style.marginLeft = this.el.marginLeft, e.style.marginRight = this.el.marginRight, e.style.paddingLeft = this.el.paddingLeft, e.style.paddingRight = this.el.paddingRight, e.style.borderLeftWidth = this.el.borderLeftWidth, e.style.borderRightWidth = this.el.borderRightWidth), this.animY && (e.style.height = this.el.height + "px", e.style.marginTop = this.el.marginTop, e.style.marginBottom = this.el.marginBottom, e.style.paddingTop = this.el.paddingTop, e.style.paddingBottom = this.el.paddingBottom, e.style.borderTopWidth = this.el.borderTopWidth, e.style.borderBottomWidth = this.el.borderBottomWidth), e.style.transition = this.duration + "ms ease-in-out";
    },
    applyOriginalStyles(e) {
      e.style.cssText = this.el.originalStyles;
    },
    saveOriginalInlineStyles(e) {
      this.el.originalStyles = e.style.cssText;
    },
    show(e) {
      this.saveComputedStyles(e), this.applyHideStyles(e), setTimeout(() => this.applyShowStyles(e), 20);
    },
    beforeHide(e) {
      this.applyShowStyles(e);
    },
    hide(e) {
      setTimeout(() => this.applyHideStyles(e), 20);
    },
    saveComputedStyles(e) {
      const t = window.getComputedStyle(e, null);
      this.animX && (this.el.width = e.offsetWidth, this.el.marginLeft = t.getPropertyValue("marginLeft"), this.el.marginRight = t.getPropertyValue("marginRight"), this.el.paddingLeft = t.getPropertyValue("paddingLeft"), this.el.paddingRight = t.getPropertyValue("paddingRight"), this.el.borderLeftWidth = t.getPropertyValue("borderLeftWidth"), this.el.borderRightWidth = t.getPropertyValue("borderRightWidth")), this.animY && (this.el.height = e.offsetHeight, this.el.marginTop = t.getPropertyValue("marginTop"), this.el.marginBottom = t.getPropertyValue("marginBottom"), this.el.paddingTop = t.getPropertyValue("paddingTop"), this.el.paddingBottom = t.getPropertyValue("paddingBottom"), this.el.borderTopWidth = t.getPropertyValue("borderTopWidth"), this.el.borderBottomWidth = t.getPropertyValue("borderBottomWidth")), this.el.savedState = true;
    }
  }
};
var Do = v(No, [["render", Ko]]);
function Ho(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: "fade" }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16);
}
var Fo = {
  name: "w-transition-fade",
  props: {
    appear: { type: Boolean },
    duration: { type: [Number, String] }
  }
};
var jo = v(Fo, [["render", Ho]]);
function Uo(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: "scale" }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16);
}
var qo = {
  name: "w-transition-scale",
  props: {
    appear: { type: Boolean },
    duration: { type: [Number, String] }
  }
};
var Xo = v(qo, [["render", Uo]]);
function Yo(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: "scale-fade" }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16);
}
var Go = {
  name: "w-transition-scale-fade",
  props: {
    appear: { type: Boolean },
    duration: { type: [Number, String] }
  }
};
var Jo = v(Go, [["render", Yo]]);
function Qo(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: l.transitionName }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16, ["name"]);
}
var Zo = {
  name: "w-transition-slide",
  props: {
    appear: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    up: { type: Boolean },
    down: { type: Boolean },
    duration: { type: [Number, String] }
  },
  computed: {
    direction() {
      return this.up && "up" || this.down && "down" || this.left && "left" || this.right && "right" || "down";
    },
    transitionName() {
      return `slide-${this.direction}`;
    }
  }
};
var ea = v(Zo, [["render", Qo]]);
function ta(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: l.transitionName }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16, ["name"]);
}
var ia = {
  name: "w-transition-slide-fade",
  props: {
    appear: { type: Boolean },
    left: { type: Boolean },
    right: { type: Boolean },
    up: { type: Boolean },
    down: { type: Boolean },
    duration: { type: [Number, String] }
  },
  computed: {
    direction() {
      return this.up && "up" || this.down && "down" || this.left && "left" || this.right && "right" || "down";
    },
    transitionName() {
      return `slide-fade-${this.direction}`;
    }
  }
};
var la = v(ia, [["render", ta]]);
function sa(e, t, i, s, n, l) {
  return openBlock(), createBlock(Transition, mergeProps({ name: "twist" }, e.$props), {
    default: withCtx(() => [
      renderSlot(e.$slots, "default")
    ]),
    _: 3
  }, 16);
}
var oa = {
  name: "w-transition-twist",
  props: {
    appear: { type: Boolean },
    duration: { type: [Number, String] }
  }
};
var aa = v(oa, [["render", sa]]);
var na = ["innerHTML"];
var ra = {
  key: 1,
  class: "ml1"
};
function da(e, t, i, s, n, l) {
  const a = resolveComponent("w-button"), r = resolveComponent("w-icon"), d = resolveComponent("w-tree", true);
  return openBlock(), createElementBlock("ul", {
    class: normalizeClass(["w-tree", l.classes])
  }, [
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.currentDepthItems, (h, y) => (openBlock(), createElementBlock("li", {
      class: normalizeClass(["w-tree__item", l.itemClasses(h)]),
      key: y
    }, [
      (openBlock(), createBlock(resolveDynamicComponent(l.getTreeItemComponent(h)), mergeProps({
        class: "w-tree__item-label",
        ref_for: true
      }, h.route && { [!e.$router || l.hasExternalLink(h) ? "href" : "to"]: h.route }, {
        onClick: (S) => !i.disabled && !h.disabled && l.onLabelClick(h, S),
        onKeydown: (S) => !i.disabled && !h.disabled && l.onLabelKeydown(h, S),
        tabindex: l.getTreeItemTabindex(h)
      }), {
        default: withCtx(() => [
          (h.children || h.branch) && (i.expandOpenIcon && h.open || i.expandIcon) && !(i.unexpandableEmpty && !h.children) ? (openBlock(), createBlock(a, {
            key: 0,
            class: "w-tree__item-expand",
            onClick: withModifiers((S) => !i.disabled && !h.disabled && l.onLabelClick(h, S), ["stop"]),
            color: "inherit",
            icon: h.open && i.expandOpenIcon || i.expandIcon,
            "icon-props": { rotate90a: !h.open },
            tabindex: -1,
            disabled: i.disabled || h.disabled,
            text: "",
            sm: ""
          }, null, 8, ["onClick", "icon", "icon-props", "disabled"])) : createCommentVNode("", true),
          renderSlot(e.$slots, "item", {
            item: h.originalItem,
            depth: i.depth,
            path: h.path,
            open: h.open
          }, () => {
            var S;
            return [
              l.itemIcon(h) ? (openBlock(), createBlock(r, {
                key: 0,
                class: "w-tree__item-icon",
                color: h.originalItem[i.itemIconColorKey] || i.iconColor
              }, {
                default: withCtx(() => [
                  createTextVNode(toDisplayString(l.itemIcon(h)), 1)
                ]),
                _: 2
              }, 1032, ["color"])) : createCommentVNode("", true),
              createBaseVNode("span", {
                innerHTML: h.label
              }, null, 8, na),
              i.counts && (h.children || h.branch) ? (openBlock(), createElementBlock("span", ra, "(" + toDisplayString(((S = h.originalItem.children) == null ? void 0 : S.length) || 0) + ")", 1)) : createCommentVNode("", true)
            ];
          })
        ]),
        _: 2
      }, 1040, ["onClick", "onKeydown", "tabindex"])),
      (openBlock(), createBlock(resolveDynamicComponent(i.noTransition ? "div" : "w-transition-expand"), {
        y: !i.noTransition || null,
        onAfterEnter: (S) => e.$emit("open", l.emitPayload(h)),
        onAfterLeave: (S) => e.$emit("close", l.emitPayload(h))
      }, {
        default: withCtx(() => [
          h.children && h.open ? (openBlock(), createBlock(d, mergeProps({
            key: 0,
            ref_for: true
          }, e.$props, {
            depth: i.depth + 1,
            data: h.originalItem.children,
            parent: h,
            onBeforeOpen: t[0] || (t[0] = (S) => e.$emit("before-open", S)),
            onOpen: t[1] || (t[1] = (S) => e.$emit("open", S)),
            onBeforeClose: t[2] || (t[2] = (S) => e.$emit("before-close", S)),
            onClose: t[3] || (t[3] = (S) => e.$emit("close", S)),
            onClick: t[4] || (t[4] = (S) => e.$emit("click", S)),
            onSelect: t[5] || (t[5] = (S) => e.$emit("select", S)),
            "onUpdate:modelValue": t[6] || (t[6] = (S) => e.$emit("update:model-value", S))
          }), {
            item: withCtx(({ item: S, depth: g, path: V, open: I }) => [
              renderSlot(e.$slots, "item", {
                item: S,
                depth: g,
                path: V,
                open: I
              })
            ]),
            _: 2
          }, 1040, ["depth", "data", "parent"])) : createCommentVNode("", true)
        ]),
        _: 2
      }, 1064, ["y", "onAfterEnter", "onAfterLeave"]))
    ], 2))), 128))
  ], 2);
}
var ua = {
  name: "w-tree",
  props: {
    modelValue: { type: [Object, Array] },
    data: { type: [Object, Array], required: true },
    depth: { type: Number, default: 0 },
    // To get the context from nested items.
    parent: { type: Object, default: null },
    // To get the context from nested items.
    branchClass: { type: String },
    leafClass: { type: String },
    branchIcon: { type: String },
    branchOpenIcon: { type: String },
    leafIcon: { type: String },
    expandIcon: { type: [Boolean, String], default: "wi-triangle-down" },
    expandOpenIcon: { type: [Boolean, String] },
    expandAll: { type: Boolean },
    unexpandableEmpty: { type: Boolean },
    disabled: { type: Boolean },
    noTransition: { type: Boolean },
    selectable: { type: Boolean },
    // By default it only reacts to items count change (added or deleted items) not property of items change.
    deepReactivity: { type: Boolean },
    counts: { type: Boolean },
    itemIconKey: { type: String, default: "icon" },
    // Support a different icon per item.
    iconColor: { type: String },
    // Applies a color on all the label item icons.
    itemLabelKey: { type: String, default: "label" },
    // Specify a different key for the item label.
    itemIconColorKey: { type: String, default: "iconColor" },
    // Applies a specific color on each label item icons.
    itemRouteKey: { type: String, default: "route" },
    // Uses a router link if the item has the `route` key.
    itemDisabledKey: { type: String, default: "disabled" },
    // Disables the item click and selection.
    itemOpenKey: { type: String, default: "open" }
    // Open the item by default.
  },
  emits: ["update:model-value", "before-open", "open", "before-close", "close", "click", "select"],
  data: () => ({
    currentDepthItems: [],
    // A clone of the data prop with additional info per item.
    dataPropUnwatch: null
    // Holds the unwatch handler of the data prop.
  }),
  computed: {
    classes() {
      return {
        [`w-tree--depth${this.depth}`]: true,
        "w-tree--expand-icon": this.expandIcon && !this.depth,
        "w-tree--selectable": this.selectable,
        "w-tree--disabled": this.disabled && !this.depth,
        "w-tree--no-expand-button": !this.expandIcon
      };
    }
  },
  methods: {
    //  From data watcher, retain the oldItems open state.
    updateCurrentDepthTree(e, t = []) {
      if (this.currentDepthItems = [], !Array.isArray(e) && typeof e != "object")
        return j(`[w-tree] the tree items must be of type array or object, ${typeof e} received.`);
      Array.isArray(e) || (e = [e]), e.forEach((i, s) => {
        var l, a;
        const n = {
          originalItem: i,
          // Store the original item to return it on event emits.
          _uid: this.depth.toString() + (s + 1),
          label: i[this.itemLabelKey],
          children: !!i.children,
          // The children tree remains available in originalItem.
          branch: i.branch,
          route: i[this.itemRouteKey],
          disabled: i[this.itemDisabledKey],
          selected: ((l = t[s]) == null ? void 0 : l.selected) || false,
          depth: this.depth,
          open: !!((a = t[s]) != null && a.open || this.expandAll || i[this.itemOpenKey]),
          parent: this.parent || null,
          path: []
          // Ancestors path from root to leaf including self.
        };
        n.path = this.getTreeItemPath(n), this.currentDepthItems.push(n);
      });
    },
    getTreeItemComponent(e) {
      return !this.disabled && !e.disabled && e.route ? !this.$router || this.hasExternalLink(e) ? "a" : "router-link" : "div";
    },
    getTreeItemTabindex(e) {
      return !this.disabled && !e.disabled && (e.children || e.branch || this.selectable) && !(this.unexpandableEmpty && !e.children) ? 0 : null;
    },
    /**
     * Get the tree path of the given item.
     * The full ancestors items are stored in the array and not only their `originalItem`s in case
     * it is mutated before we return it to the user through slots and emitted events.
     * Before it is returned to the user, this array is mapped to only give the `originalItem`s.
     *
     * @param {Object} item the tree item to get the ancestors path for.
     * @return an array of item objects from the root to the leaf (including the item itself).
     */
    getTreeItemPath(e) {
      const t = [e];
      let i = e.parent;
      for (; i; )
        t.push(i), i = i.parent;
      return t.reverse(), t;
    },
    getTreeItemPathForOutput(e) {
      return e.path.map((t) => t.originalItem);
    },
    /**
     * Expand/collapse the given tree item when possible (not disabled, has children).
     *
     * @param {Object} item the item to expand.
     * @param {Boolean|Undefined} open when a boolean is received, force a state (open or close).
     */
    expandDepth(e, t) {
      typeof t == "boolean" ? e.open = t : e.open = !e.open;
      const i = this.emitPayload(e);
      return this.$emit(e.open ? "before-open" : "before-close", i), !this.unexpandableEmpty && !e.children && this.$emit(e.open ? "open" : "close", i), true;
    },
    onLabelClick(e, t) {
      e[this.itemRouteKey] && this.$router && !this.hasExternalLink(e) && t.preventDefault(), (e.children || e.branch && !this.unexpandableEmpty) && this.expandDepth(e), this.selectable && (e.selected = !e.selected);
      const s = this.emitPayload(e, t);
      this.$emit("click", s), this.emitItemSelection(e, t);
    },
    onLabelKeydown(e, t) {
      if (!(t.metaKey || t.ctrlKey || t.altKey || t.shiftKey) && [13, 32, 37, 38, 39, 40].includes(t.keyCode) && ((e.children || e.branch) && ([13, 32].includes(t.keyCode) ? this.expandDepth(e) && t.preventDefault() : t.keyCode === 37 ? this.expandDepth(e, false) && t.preventDefault() : t.keyCode === 39 && this.expandDepth(e, true) && t.preventDefault()), [38, 40].includes(t.keyCode))) {
        const s = this.$el.closest(".w-tree--depth0").querySelectorAll('.w-tree__item-label[tabindex="0"]'), n = t.target.closest(".w-tree__item-label"), l = t.keyCode === 38 ? -1 : 1;
        [...s].some((a, r) => a.isSameNode(n) ? (s[r + l] && s[r + l].focus(), true) : false);
      }
      t.keyCode === 13 && (this.selectable && (e.selected = !e.selected), this.emitItemSelection(e, t));
    },
    emitItemSelection(e, t) {
      const i = this.emitPayload(e, t);
      this.$emit("update:model-value", i), this.selectable && this.$emit("select", i);
    },
    emitPayload(e, t) {
      const i = {
        item: e.originalItem,
        depth: this.depth,
        path: this.getTreeItemPathForOutput(e)
      };
      return t && (i.e = t), (e.children || e.branch && !this.unexpandableEmpty) && (i.open = e.open), this.selectable && (i.selected = e.selected), i;
    },
    /**
     * Returns the previous sibling matching the given selector, or false if not found.
     *
     * @param {Object} node the DOM node to find sibling for.
     * @param {String} selector any valid DOM selector to match the siblings.
     */
    getPreviousSibling(e, t) {
      for (; t && (e = e.previousElementSibling); )
        if (e.matches(t)) return e;
      return false;
    },
    /**
     * Returns the next sibling matching the given selector, or false if not found.
     *
     * @param {Object} node the DOM node to find sibling for.
     * @param {String} selector any valid DOM selector to match the siblings.
     */
    getNextSibling(e, t) {
      for (; t && (e = e.nextElementSibling); )
        if (e.matches(t)) return e;
      return false;
    },
    focusTreeItem(e) {
      e && e.querySelector(".w-tree__item-label").focus();
    },
    itemIcon(e) {
      return e.originalItem[this.itemIconKey] || !e.children && !e.branch && this.leafIcon || (e.children || e.branch) && (e.open && this.branchOpenIcon || this.branchIcon);
    },
    hasExternalLink(e) {
      return /^(https?:)?\/\/|mailto:|tel:/.test(e[this.itemRouteKey]);
    },
    itemClasses(e) {
      return {
        [e.children || e.branch ? "w-tree__item--branch" : "w-tree__item--leaf"]: true,
        "w-tree__item--disabled": e[this.itemDisabledKey],
        "w-tree__item--selected": e.selected,
        "w-tree__item--empty": e.branch && !e.children,
        "w-tree__item--unexpandable": e.branch && !e.children && this.unexpandableEmpty
      };
    }
  },
  created() {
    this.updateCurrentDepthTree(this.data), this.dataPropUnwatch = this.$watch(
      "data",
      // The open property of each item has to be retained from this.currentDepthItems in order to stay
      // in the same state after DOM repaint.
      (e) => this.updateCurrentDepthTree(e, this.currentDepthItems),
      { deep: !!this.deepReactivity }
      // Deep watching is more resource consuming. Only enable on user demand.
    );
  },
  unmounted() {
    this.dataPropUnwatch();
  }
};
var ha = v(ua, [["render", da]]);
var ca = Object.freeze(Object.defineProperty({
  __proto__: null,
  WAccordion: at,
  WAlert: ut,
  WApp: Bt,
  WAutocomplete: kt,
  WBadge: Vt,
  WBreadcrumbs: xt,
  WButton: Kt,
  WCard: Ft,
  WCheckbox: Qt,
  WCheckboxes: ii,
  WConfirm: oi,
  WDatePicker: ri,
  WDialog: hi,
  WDivider: mi,
  WDrawer: ki,
  WFlex: Si,
  WForm: Ii,
  WFormElement: Li,
  WGrid: Pi,
  WIcon: Mi,
  WImage: Ni,
  WInput: Qi,
  WList: tl,
  WMenu: sl,
  WNotification: nl,
  WOverlay: ul,
  WParallax: fl,
  WProgress: Cl,
  WRadio: Ll,
  WRadios: Ol,
  WRating: Wl,
  WScrollable: Hl,
  WSelect: Gl,
  WSlider: us,
  WSpinner: fs,
  WSteps: bs,
  WSwitch: _s,
  WTable: mo,
  WTabs: As,
  WTag: wo,
  WTextarea: _o,
  WTimeline: Lo,
  WToolbar: Po,
  WTooltip: Mo,
  WTransitionBounce: Wo,
  WTransitionExpand: Do,
  WTransitionFade: jo,
  WTransitionScale: Xo,
  WTransitionScaleFade: Jo,
  WTransitionSlide: ea,
  WTransitionSlideFade: la,
  WTransitionTwist: aa,
  WTree: ha
}, Symbol.toStringTag, { value: "Module" }));
var pa = q.install;
q.install = (e, t = {}) => pa.call(q, e, { components: ca, ...t });
export {
  q as default
};
//# sourceMappingURL=wave-ui.js.map
